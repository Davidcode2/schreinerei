---
phase: 32-media-viewer
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/sites/infrastructure/site_repository.rs
  - src/modules/sites/application/site_service.rs
  - src/modules/sites/api/routes.rs
  - frontend/src/types/generated.ts
autonomous: true
requirements:
  - VIEW-05
must_haves:
  truths:
    - "Viewer-capable activities expose a human-readable creator name instead of only a raw user_id."
    - "Frontend receives stable attachment identifiers and protected media URLs needed to open direct viewer links."
  artifacts:
    - path: "src/modules/sites/infrastructure/site_repository.rs"
      provides: "Tenant-scoped activity query with creator display-name resolution"
    - path: "src/modules/sites/api/routes.rs"
      provides: "ActivityResponse creator_name contract exported to TypeScript"
    - path: "frontend/src/types/generated.ts"
      provides: "Regenerated activity DTO including creator_name"
  key_links:
    - from: "src/modules/sites/infrastructure/site_repository.rs"
      to: "src/modules/sites/api/routes.rs"
      via: "ActivityRow -> Activity -> ActivityResponse creator_name mapping"
      pattern: "creator_name"
    - from: "src/modules/sites/api/routes.rs"
      to: "frontend/src/types/generated.ts"
      via: "ts-rs export"
      pattern: "creator_name"
---

<objective>
Establish the backend viewer contract needed by the fullscreen media viewer.

Purpose: give the frontend the creator metadata and stable attachment-backed activity data it needs before any route-backed viewer UI is wired.
Output: attachment-aware activity DTOs with `creator_name` exported to the frontend.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-media-viewer/32-RESEARCH.md
@.planning/phases/32-media-viewer/32-UI-SPEC.md
@.planning/phases/31-document-upload-rework/31-document-upload-rework-01-SUMMARY.md

<interfaces>
From `src/modules/sites/api/routes.rs`:

```rust
pub struct ActivityResponse {
    pub id: String,
    pub site_id: String,
    pub user_id: String,
    pub activity_type: String,
    pub content: Option<String>,
    pub photo_url: Option<String>,
    pub attachments: Vec<SiteActivityAttachmentResponse>,
    pub created_at: String,
}
```

From `frontend/src/types/generated.ts`:

```ts
export type ActivityResponse = {
  id: string,
  site_id: string,
  user_id: string,
  activity_type: string,
  content: string | null,
  photo_url: string | null,
  attachments: Array<SiteActivityAttachmentResponse>,
  created_at: string,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: extend activity reads with creator display names</name>
  <files>src/modules/sites/infrastructure/site_repository.rs, src/modules/sites/application/site_service.rs, src/modules/sites/api/routes.rs</files>
  <read_first>src/modules/sites/infrastructure/site_repository.rs, src/modules/sites/application/site_service.rs, src/modules/sites/api/routes.rs, .planning/phases/32-media-viewer/32-RESEARCH.md, .planning/phases/32-media-viewer/32-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: listing activities returns `creator_name` populated from a tenant-local user display value.
    - Test 2: when `users.name` is null/empty, the response falls back to `users.email`, then `user_id` text.
    - Test 3: existing attachment hydration and photo/document activity ordering remain unchanged.
  </behavior>
  <action>Update the activity read path so the repository query joins local users on tenant-scoped `site_activities.user_id`, selects `creator_name` with `COALESCE(NULLIF(users.name, ''), users.email, site_activities.user_id::text)`, carries that field through the Activity row/domain mapping, and adds `creator_name: String` to `ActivityResponse`. Keep the existing attachment hydration path intact, keep every query tenant-scoped, and do not introduce any unauthenticated media URL logic. This implements VIEW-05 and preserves the Phase 29/31 attachment invariants.</action>
  <acceptance_criteria>
    - `src/modules/sites/infrastructure/site_repository.rs` contains a tenant-scoped join/select for `creator_name`.
    - `src/modules/sites/api/routes.rs` defines `creator_name` on `ActivityResponse` and sets it in `impl From<Activity> for ActivityResponse`.
    - No new route returns raw storage keys or unauthenticated blob URLs.
  </acceptance_criteria>
  <verify>
    <automated>cargo test creator_name -- --nocapture &amp;&amp; cargo test list_activities -- --nocapture</automated>
  </verify>
  <done>Backend activity responses include a human-readable creator name for every viewerable activity without breaking existing list behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: regenerate frontend DTOs for the viewer contract</name>
  <files>src/modules/sites/api/routes.rs, frontend/src/types/generated.ts</files>
  <read_first>src/modules/sites/api/routes.rs, frontend/src/types/generated.ts, .planning/phases/31-document-upload-rework/31-document-upload-rework-01-SUMMARY.md</read_first>
  <behavior>
    - Test 1: generated frontend activity types include `creator_name`.
    - Test 2: exported attachment response types remain unchanged for `attachment_id`, `filename`, `mime_type`, `url`, and `thumbnail_url`.
  </behavior>
  <action>Run the repository’s existing Rust/ts-rs generation path after adding `creator_name` so `frontend/src/types/generated.ts` stays in sync. Preserve the existing `ActivityResponse`, `SiteActivityAttachmentResponse`, and upload response exports; only extend the activity contract with the new creator field required by VIEW-05.</action>
  <acceptance_criteria>
    - `frontend/src/types/generated.ts` contains `creator_name: string` on `ActivityResponse`.
    - `frontend/src/types/generated.ts` still exports `attachment_id`, `filename`, `mime_type`, `url`, and `thumbnail_url` for attachment responses.
  </acceptance_criteria>
  <verify>
    <automated>cargo test &amp;&amp; cargo fmt --check</automated>
  </verify>
  <done>Frontend-generated DTOs match the backend viewer contract and are ready for route-backed viewer UI work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → SPA viewer | Server-provided activity metadata becomes user-visible viewer sidebar content |
| DB join → API DTO | Tenant-scoped user identity data is merged into activity responses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | I | `list_activities` query | mitigate | Keep `users` join tenant-scoped and fall back to `user_id::text` only for the same activity row; never join across tenants. |
| T-32-02 | T | `ActivityResponse.creator_name` | mitigate | Populate `creator_name` from server-side query fields only; do not trust client-supplied values. |
| T-32-03 | D | activity list route | accept | Added field is low-cost string metadata on an existing bounded list query (`limit <= 100`). |
</threat_model>

<verification>
- `cargo test creator_name -- --nocapture`
- `cargo test list_activities -- --nocapture`
- `cargo test`
- `cargo fmt --check`
</verification>

<success_criteria>
- Activity API responses expose `creator_name`.
- Generated frontend types include `creator_name`.
- Existing attachment metadata and tenant-scoped media URLs remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/32-media-viewer/32-media-viewer-01-SUMMARY.md`
</output>
