---
phase: 32-media-viewer
plan: "03"
type: execute
wave: 3
depends_on:
  - 32-01
  - 32-02
files_modified:
  - frontend/src/pages/sites/ActivityFeed.tsx
  - frontend/src/pages/sites/ActivityFeed.test.tsx
autonomous: true
requirements:
  - VIEW-01
  - VIEW-02
  - VIEW-03
must_haves:
  truths:
    - "Clicking an image preview in the feed opens the fullscreen viewer."
    - "Clicking a PDF/document tile in the feed opens the fullscreen viewer."
    - "Fallback tiles remain clickable even when preview loading fails."
  artifacts:
    - path: "frontend/src/pages/sites/ActivityFeed.tsx"
      provides: "Clickable viewer-entry tiles for photo and document activities"
    - path: "frontend/src/pages/sites/ActivityFeed.test.tsx"
      provides: "Regression coverage for image/PDF/fallback tile routing"
  key_links:
    - from: "frontend/src/pages/sites/ActivityFeed.tsx"
      to: "frontend/src/pages/sites/mediaViewerRoute.ts"
      via: "buildMediaViewerPath"
      pattern: "Medium öffnen:"
    - from: "frontend/src/pages/sites/ActivityFeed.tsx"
      to: "frontend/src/pages/sites/MediaViewer.tsx"
      via: "viewer route navigation"
      pattern: "/media/"
---

<objective>
Wire the existing activity feed tiles into the fullscreen viewer.

Purpose: finish the end-user entrypoint so camera photos and document attachments both open the new viewer from the feed.
Output: clickable, keyboard-accessible media tiles with regression coverage for image, PDF, and fallback states.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-media-viewer/32-RESEARCH.md
@.planning/phases/32-media-viewer/32-UI-SPEC.md
@.planning/phases/32-media-viewer/32-02-PLAN.md
@.planning/phases/31-document-upload-rework/31-document-upload-rework-03-SUMMARY.md

<interfaces>
From `frontend/src/pages/sites/ActivityFeed.tsx`:

```tsx
function AttachmentTile({ attachment }: { attachment: ActivityAttachment }) {
  if (attachment.mime_type === "application/pdf") {
    return <PdfAttachmentTile attachment={attachment} />
  }

  return <ImageAttachmentTile attachment={attachment} />
}
```

From `frontend/src/pages/sites/mediaViewerRoute.ts` (created in Plan 02):

```ts
buildMediaViewerPath(siteId, activityId, attachmentId, filenameOrFallback)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: make feed media tiles route into the fullscreen viewer</name>
  <files>frontend/src/pages/sites/ActivityFeed.tsx, frontend/src/pages/sites/ActivityFeed.test.tsx</files>
  <read_first>frontend/src/pages/sites/ActivityFeed.tsx, frontend/src/pages/sites/ActivityFeed.test.tsx, frontend/src/pages/sites/mediaViewerRoute.ts, .planning/phases/32-media-viewer/32-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: image attachment tiles render as clickable viewer links with accessible names `Medium öffnen: {filename}`.
    - Test 2: PDF tiles render as clickable viewer links that point to the canonical `/media/` route.
    - Test 3: legacy photo activities route into the viewer using the attachment UUID parsed from `photo_url`.
    - Test 4: preview-failure fallback tiles remain clickable when a valid route target exists.
  </behavior>
  <action>Update `ActivityFeed.tsx` so every viewerable tile wraps the full preview + filename card in a keyboard-focusable `Link` or button+navigation using the helper from `mediaViewerRoute.ts`. Preserve the existing activity card shell and relative timestamp display. Add accent-ring hover/focus states per the UI-SPEC, ensure status-change activities stay non-clickable, and give each interactive tile the accessible name `Medium öffnen: {filename}`. For legacy camera-photo activities, parse the attachment UUID from `photo_url` and route them with the fallback slug/title `aktivitaetsfoto`. Keep the `Vorschau nicht verfügbar` fallback tile visible and clickable when routing metadata exists. Add or update tests in `ActivityFeed.test.tsx` for image, PDF, legacy photo, and fallback-link behavior. This closes VIEW-01, VIEW-02, and the feed-entry side of VIEW-03.</action>
  <acceptance_criteria>
    - `frontend/src/pages/sites/ActivityFeed.tsx` contains `Medium öffnen:` accessible labels.
    - Image and PDF tiles route to `/sites/${siteId}/media/${activityId}/${attachmentId}/...`.
    - Status-change cards remain non-clickable.
    - `frontend/src/pages/sites/ActivityFeed.test.tsx` asserts image and PDF viewer route targets plus fallback-tile clickability.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/pages/sites/ActivityFeed.test.tsx src/pages/sites/MediaViewer.test.tsx --reporter=dot &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Feed tiles for images and documents open the fullscreen viewer accessibly and preserve existing fallback behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Feed metadata → route generation | Existing activity/attachment data is transformed into deep-link URLs |
| Keyboard/pointer interaction → router navigation | User interaction opens fullscreen viewer routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-07 | T | feed route generation | mitigate | Generate viewer paths only from server-provided activity IDs, attachment IDs, and sanitized filename slugs via `mediaViewerRoute.ts`. |
| T-32-08 | R | clickable fallback tiles | accept | Fallback tiles open the same authenticated viewer target; no privileged mutation occurs. |
| T-32-09 | E | status-change cards | mitigate | Keep status-change cards non-interactive so non-media records never route into the viewer surface. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/pages/sites/ActivityFeed.test.tsx src/pages/sites/MediaViewer.test.tsx --reporter=dot`
- `cd frontend && npx tsc --noEmit`
</verification>

<success_criteria>
- Feed image tiles open the fullscreen viewer.
- Feed PDF tiles open the fullscreen viewer.
- Broken-preview fallback tiles stay visible and still open the fullscreen viewer when metadata exists.
</success_criteria>

<output>
After completion, create `.planning/phases/32-media-viewer/32-media-viewer-03-SUMMARY.md`
</output>
