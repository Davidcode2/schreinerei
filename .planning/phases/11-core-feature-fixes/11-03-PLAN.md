---
phase: 11-core-feature-fixes
plan: 03
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - src/modules/sites/infrastructure/site_repository.rs
  - src/modules/sites/application/site_service.rs
  - src/modules/fleet/api/routes.rs
autonomous: true
requirements: [CORE-03, CORE-04]
user_setup: []
---

<objective>
Fix time entry creation and calendar query validation errors.

Purpose: Enable time tracking and calendar views.
Output: Working time entry creation and calendar display.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-core-feature-fixes/BUG-REPORT.md

# Key source files
@src/modules/sites/api/routes.rs
@src/modules/sites/application/site_service.rs
@src/modules/sites/infrastructure/site_repository.rs
@src/modules/fleet/api/routes.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix time entry user_id FK violation</name>
  <files>
    - src/modules/sites/application/site_service.rs
    - src/modules/sites/infrastructure/site_repository.rs
  </files>
  <read_first>
    - src/modules/sites/application/site_service.rs
    - src/modules/sites/infrastructure/site_repository.rs
    - src/modules/iam/infrastructure/user_repository.rs
  </read_first>
  <action>
Apply the same user ID resolution pattern from 11-01 to time entries:

1. Add pool to SiteService:

```rust
pub struct SiteService {
    site_repo: SiteRepository,
    pool: PgPool,
}

impl SiteService {
    pub fn new(site_repo: SiteRepository) -> Self {
        let pool = site_repo.pool.clone();
        Self { site_repo, pool }
    }
}
```

2. In `create_time_entry`, resolve user ID:

```rust
pub async fn create_time_entry(
    &self,
    create: CreateTimeEntry,
    ctx: &TenantContext,
) -> Result<TimeEntry, AppError> {
    // Resolve local user ID
    let user_repo = UserRepository::new(self.pool.clone());
    let local_user_id = user_repo
        .find_or_create_by_keycloak_id(
            &ctx.user_id.to_string(),
            ctx.tenant_id,
            &ctx.email,
            if ctx.is_admin() { Role::Admin } else { Role::Employee },
        )
        .await?
        .id;

    let entry = self.site_repo
        .create_time_entry(&create, local_user_id, ctx.tenant_id)
        .await?;
    
    // ... rest of method
}
```
  </action>
  <verify>
    <automated>cargo check 2>&1 | grep -c "error" | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    - SiteService has pool access
    - create_time_entry resolves user ID
    - Code compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix calendar date format parsing</name>
  <files>
    - src/modules/fleet/api/routes.rs
  </files>
  <read_first>
    - src/modules/fleet/api/routes.rs (get_calendar handler)
  </read_first>
  <action>
The calendar endpoint expects RFC3339 format but frontend sends date-only format.
Fix the parsing to accept both formats:

```rust
pub async fn get_calendar(
    State(state): State<AppState>,
    auth: AuthenticatedUser,
    Query(query): Query<CalendarQuery>,
) -> Result<impl IntoResponse, AppError> {
    let service = FleetService::new(FleetRepository::new(state.pool));
    let ctx = TenantContext::from_auth(&auth);
    
    // Try RFC3339 first, then date-only format
    let start_date = DateTime::parse_from_rfc3339(&query.start_date)
        .map(|dt| dt.with_timezone(&Utc))
        .or_else(|_| {
            NaiveDate::parse_from_str(&query.start_date, "%Y-%m-%d")
                .map(|d| d.and_hms_opt(0, 0, 0).unwrap().and_utc())
        })
        .map_err(|_| AppError::Validation("Invalid start_date format".to_string()))?;
    
    let end_date = DateTime::parse_from_rfc3339(&query.end_date)
        .map(|dt| dt.with_timezone(&Utc))
        .or_else(|_| {
            NaiveDate::parse_from_str(&query.end_date, "%Y-%m-%d")
                .map(|d| d.and_hms_opt(23, 59, 59).unwrap().and_utc())
        })
        .map_err(|_| AppError::Validation("Invalid end_date format".to_string()))?;
    
    let resource_type = query.resource_type
        .map(|s| s.parse::<ResourceType>())
        .transpose()
        .map_err(|e: String| AppError::Validation(e))?;
    
    let entries = service.get_calendar(start_date, end_date, resource_type, &ctx).await?;
    let response = CalendarResponse {
        resources: entries.into_iter().map(CalendarEntryResponse::from).collect(),
    };
    
    Ok(Json(response))
}
```

Add necessary imports:
```rust
use chrono::{DateTime, Utc, NaiveDate};
```
  </action>
  <verify>
    <automated>cargo check 2>&1 | grep -c "error" | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    - Calendar accepts both RFC3339 and date-only formats
    - Code compiles without errors
  </done>
</task>

</tasks>

<verification>
1. Run `cargo check` to verify compilation
2. Test calendar endpoint with date-only format
3. Test time entry creation
</verification>

<success_criteria>
1. Time entry creation succeeds without FK violation
2. Calendar query accepts date-only format (YYYY-MM-DD)
3. Code compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-core-feature-fixes/11-03-SUMMARY.md`
</output>
