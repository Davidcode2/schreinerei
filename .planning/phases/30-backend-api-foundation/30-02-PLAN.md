---
phase: 30-backend-api-foundation
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/modules/inventory/api/routes.rs
  - src/modules/inventory/api.rs
  - frontend/src/types/inventory.ts
  - frontend/src/types/generated.ts
autonomous: true
requirements: []
  # Phase 30 is an enabler phase — requirements map to Phase 31-32

must_haves:
  truths:
    - "PATCH /api/v1/inventory/categories/{id} updates category name with validation and returns updated category"
    - "DELETE /api/v1/inventory/categories/{id} returns 409 Conflict when materials reference the category"
    - "DELETE /api/v1/inventory/categories/{id} returns 200 when category has no materials"
    - "PATCH /api/v1/inventory/materials/{id} accepts location and min_quantity partial updates"
    - "POST /api/v1/inventory/materials/{id}/stock-in records positive quantity with notes"
    - "GET /api/v1/inventory/materials/{id}/history returns entry_type, user_name, and category_name for each entry"
  artifacts:
    - path: "src/modules/inventory/api/routes.rs"
      provides: "API route handlers for PATCH category, DELETE category, PATCH material, POST stock-in, GET enriched history"
      exports: ["update_category", "delete_category", "update_material", "stock_in_material", "get_enriched_material_history"]
    - path: "frontend/src/types/inventory.ts"
      provides: "Frontend TypeScript types matching new API DTOs"
      contains: "EnrichedStockHistoryEntry"
  key_links:
    - from: "src/modules/inventory/api/routes.rs"
      to: "src/modules/inventory/application/inventory_service.rs"
      via: "service method calls"
      pattern: "service\\."
    - from: "src/modules/inventory/api/routes.rs"
      to: "src/modules/inventory/api/routes.rs"
      via: "router registration"
      pattern: "\\.route\\("
---

<objective>
Create API endpoint handlers and route registration for category update/delete, material patch, stock-in, and enriched history. Generate TypeScript types.

Purpose: Expose the domain commands from Plan 01 as REST API endpoints that Phase 31-32 frontend code will consume.

Output: Working API endpoints with ts-rs type generation, registered in the router.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-backend-api-foundation/30-01-SUMMARY.md
@src/modules/inventory/api/routes.rs
@src/modules/inventory/application/inventory_service.rs
@src/modules/inventory/domain/category.rs
@src/modules/inventory/domain/material.rs
@src/modules/inventory/domain/stock_entry.rs
@src/modules/inventory/api.rs
@frontend/src/types/inventory.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Add API route handlers for category update/delete, material patch, stock-in, and enriched history</name>
  <files>
    src/modules/inventory/api/routes.rs
  </files>
  <read_first>
    src/modules/inventory/api/routes.rs,
    src/modules/inventory/domain/category.rs,
    src/modules/inventory/domain/material.rs,
    src/modules/inventory/domain/stock_entry.rs,
    src/modules/inventory/application/inventory_service.rs,
    src/modules/inventory/infrastructure/material_repository.rs
  </read_first>
  <action>
1. Add DTOs in `routes.rs` with `#[derive(TS)]` and `#[ts(export, export_to = "frontend/src/types/generated.ts")]`:

   a. `UpdateCategoryRequest` — `name: Option<String>`, `description: Option<String>`
   
   b. `UpdateMaterialRequest` — `location: Option<String>`, `min_quantity: Option<i32>`, `clear_location: Option<bool>`
   
   c. `StockInRequest` — `quantity: i32`, `notes: Option<String>`
   
   d. `EnrichedStockHistoryResponse` — matches `EnrichedStockEntry` fields:
      ```
      id: String,
      material_id: String,
      user_id: String,
      user_name: String,
      entry_type: String,
      quantity_change: i32,
      quantity_after: i32,
      notes: Option<String>,
      site_id: Option<String>,
      site_name: Option<String>,
      category_name: String,
      created_at: String,
      ```

2. Add handler functions:

   a. `update_category` — PATCH `/api/v1/inventory/categories/{id}`:
      - Parse `id` path param to `CategoryId`
      - Deserialize `UpdateCategoryRequest` from JSON body
      - Build `UpdateCategory` command from request fields
      - Call `service.update_category(category_id, update, &ctx)`
      - Return `Json(CategoryResponse::from(category))`

   b. `delete_category` — DELETE `/api/v1/inventory/categories/{id}`:
      - Parse `id` path param to `CategoryId`
      - Call `service.delete_category(category_id, &ctx)`
      - Return `Json(json!({"success": true}))`
      - Note: service returns `AppError::Conflict` when materials reference it → maps to 409

   c. `update_material` — PATCH `/api/v1/inventory/materials/{id}`:
      - Parse `id` path param to `MaterialId`
      - Deserialize `UpdateMaterialRequest` from JSON body
      - Build `UpdateMaterial` command with `location`, `min_quantity`, `clear_location` mapping
      - Call `service.update_material(material_id, update, &ctx)`
      - Return `Json(MaterialResponse::from(material))`

   d. `stock_in_material` — POST `/api/v1/inventory/materials/{id}/stock-in`:
      - Parse `id` path param to `MaterialId`
      - Deserialize `StockInRequest` from JSON body
      - Build `StockIn` command: `StockIn { material_id, quantity, notes }`
      - Call `service.stock_in(stock_in, &ctx)`
      - Return `Json(MaterialResponse::from(material))`

   e. `get_enriched_material_history` — GET `/api/v1/inventory/materials/{id}/history/enriched`:
      - Parse `id` path param to `MaterialId`
      - Call `service.list_enriched_history(material_id, &ctx)`
      - Map results to `Vec<EnrichedStockHistoryResponse>`
      - Return `Json(responses)`

3. Update `create_router()` to register new routes:
   ```rust
   .route("/api/v1/inventory/categories/{id}", patch(update_category).delete(delete_category))
   .route("/api/v1/inventory/materials/{id}", patch(update_material))
   .route("/api/v1/inventory/materials/{id}/stock-in", post(stock_in_material))
   .route("/api/v1/inventory/materials/{id}/history/enriched", get(get_enriched_material_history))
   ```
   
   Note: The existing `.route("/api/v1/inventory/categories/{id}", get(get_category))` needs to be updated to also include PATCH and DELETE methods. Change it from `.route("/api/v1/inventory/categories/{id}", get(get_category))` to a multi-method route.

   The existing `.route("/api/v1/inventory/materials/{id}", get(get_material).delete(delete_material))` needs PATCH method added.

4. Implement `From<EnrichedStockEntry> for EnrichedStockHistoryResponse` conversion.

5. Implement `From<UpdateCategoryRequest>` → `UpdateCategory` command mapping.

6. All new handlers must use `AuthenticatedUser` and `TenantContext::from_auth(&auth)` for tenant isolation — follow existing patterns in the file.
  </action>
  <verify>
    <automated>cd /home/jakob/documents/code/schreinerei && cargo check 2>&1 | grep -E "^error" | head -5 ; echo "Exit: $?"</automated>
  </verify>
  <done>
    PATCH /categories/{id} handler validates and delegates to service.update_category
    DELETE /categories/{id} handler delegates to service.delete_category (returns 409 on conflict)
    PATCH /materials/{id} handler delegates to service.update_material with partial fields
    POST /materials/{id}/stock-in handler delegates to service.stock_in (no admin check, all users)
    GET /materials/{id}/history/enriched returns EnrichedStockHistoryResponse with entry_type, user_name, category_name
    All routes registered in create_router()
    All new DTOs have #[derive(TS)] with ts(export)
    cargo check passes with zero errors
  </done>
</task>

<task type="auto">
  <name>task 2: Update frontend TypeScript types and run ts-rs generation</name>
  <files>
    frontend/src/types/inventory.ts
  </files>
  <read_first>
    frontend/src/types/inventory.ts,
    src/modules/inventory/api/routes.rs
  </read_first>
  <action>
1. In `frontend/src/types/inventory.ts`, add these types matching the new API DTOs:

   ```typescript
   // === Category Update ===
   export interface UpdateCategoryRequest {
     name?: string
     description?: string
   }

   // === Material Update ===
   export interface UpdateMaterialRequest {
     location?: string
     min_quantity?: number
     clear_location?: boolean
   }

   // === Stock-In ===
   export interface StockInRequest {
     quantity: number
     notes?: string
   }

   // === Enriched History ===
   export type EntryType = 'withdrawn' | 'adjusted' | 'material_added' | 'location_changed' | 'min_quantity_changed'

   export interface EnrichedStockHistoryEntry {
     id: string
     material_id: string
     user_id: string
     user_name: string
     entry_type: EntryType
     quantity_change: number
     quantity_after: number
     notes: string | null
     site_id: string | null
     site_name: string | null
     category_name: string
     created_at: string
   }
   ```

2. Run `cargo test --features ts-rs/export` to regenerate TypeScript types from backend DTOs into `frontend/src/types/generated.ts`.

3. Verify the generated types file contains `UpdateCategoryRequest`, `UpdateMaterialRequest`, `StockInRequest`, etc. by checking the file or running grep.

4. Do NOT add React Query hooks for the new endpoints yet — those belong in Phase 31 (frontend phase). This task is type-sync only.
  </action>
  <verify>
    <automated>cd /home/jakob/documents/code/schreinerei && cargo test --features ts-rs/export 2>&1 | tail -5 && grep -c "UpdateCategoryRequest\|StockInRequest\|EnrichedStockHistory" frontend/src/types/generated.ts frontend/src/types/inventory.ts 2>/dev/null</automated>
  </verify>
  <done>
    frontend/src/types/inventory.ts has UpdateCategoryRequest, UpdateMaterialRequest, StockInRequest, EntryType, EnrichedStockHistoryEntry
    ts-rs generated types in frontend/src/types/generated.ts match backend DTOs
    All new types exported and importable
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API endpoints | Untrusted input from authenticated users |
| API → Service → Repository | Trusted internal boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06 | Tampering | PATCH category | mitigate | UpdateCategoryRequest fields are Option<String> — COALESCE in SQL prevents null overwrite unless explicit |
| T-30-07 | Tampering | PATCH material | mitigate | clear_location separate from location, min_quantity validated ≥0 |
| T-30-08 | Spoofing | Stock-in endpoint | mitigate | AuthenticatedUser extracted from JWT, user_id resolved locally |
| T-30-09 | Info disclosure | Enriched history | accept | All data scoped by tenant_id from auth context |
</threat_model>

<verification>
- [ ] `cargo check` passes with zero errors
- [ ] All 5 new route handlers exist with proper auth extraction
- [ ] PATCH /categories/{id} updates name/description via COALESCE
- [ ] DELETE /categories/{id} returns 409 when materials reference it
- [ ] PATCH /materials/{id} handles partial updates for location/min_quantity
- [ ] POST /materials/{id}/stock-in accepts positive quantity and notes
- [ ] GET /materials/{id}/history/enriched returns entry_type, user_name, category_name
- [ ] ts-rs types generated and match frontend types
- [ ] All DTOs have proper ts-rs export annotations
</verification>

<success_criteria>
All API endpoints exist and are registered in the router. DTOs have ts-rs exports. Frontend types match backend DTOs. cargo check passes.
</success_criteria>

<output>
After completion, create `.planning/phases/30-backend-api-foundation/30-02-SUMMARY.md`
</output>