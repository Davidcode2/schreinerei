---
phase: 12-backend-domain-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/sites/domain/site.rs
  - src/modules/sites/domain/time_entry.rs
  - src/modules/sites/domain/activity.rs
  - src/modules/fleet/domain/vehicle.rs
  - src/modules/fleet/domain/tool.rs
  - src/modules/fleet/domain/reservation.rs
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "sites::domain::site module has #[cfg(test)] block with tests"
    - "sites::domain::time_entry module has #[cfg(test)] block with tests"
    - "sites::domain::activity module has #[cfg(test)] block with tests"
    - "fleet::domain::vehicle module has #[cfg(test)] block with tests"
    - "fleet::domain::tool module has #[cfg(test)] block with tests"
    - "fleet::domain::reservation module has #[cfg(test)] block with tests"
    - "All tests pass with cargo test"
    - "State machine transitions are fully tested"
  artifacts:
    - path: "src/modules/sites/domain/site.rs"
      provides: "Site domain tests with state machine"
      contains: "#[cfg(test)]"
    - path: "src/modules/sites/domain/time_entry.rs"
      provides: "TimeEntry domain tests"
      contains: "#[cfg(test)]"
    - path: "src/modules/fleet/domain/reservation.rs"
      provides: "Reservation domain tests with overlap detection"
      contains: "#[cfg(test)]"
    - path: "src/modules/fleet/domain/vehicle.rs"
      provides: "Vehicle domain tests with state machine"
      contains: "#[cfg(test)]"
    - path: "src/modules/fleet/domain/tool.rs"
      provides: "Tool domain tests with state machine"
      contains: "#[cfg(test)]"
  key_links:
    - from: "src/modules/sites/domain/site.rs"
      to: "src/common/types.rs"
      via: "use crate::common::types::{SiteStatus, AssignmentRole}"
    - from: "src/modules/fleet/domain/reservation.rs"
      to: "src/common/types.rs"
      via: "use crate::common::types::{ReservationStatus, ResourceType}"
---

<objective>
Add comprehensive inline unit tests to Sites and Fleet domain modules.

Purpose: Establish unit test coverage for state machines, time calculations, and validation logic. These modules contain the most complex business rules (status transitions, overlap detection, time validation).

Output: Test modules in each domain file with #[cfg(test)] attribute covering all business logic methods including state machine transitions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/common/types.rs
</context>

<interfaces>
<!-- Key types and business logic signatures for testing -->

**Sites Domain (src/modules/sites/domain/site.rs):**
```rust
impl Site {
    pub fn can_transition_to(&self, new_status: SiteStatus) -> bool
}
// State machine: Planned -> Active -> Completed -> Archived
impl CreateSite { pub fn validate(&self) -> Result<(), String> }
```

**Sites Domain (src/modules/sites/domain/time_entry.rs):**
```rust
impl CreateTimeEntry {
    pub fn validate(&self) -> Result<(), String>
    // - hours > 0 and <= 24
    // - work_date not in future
}
```

**Fleet Domain (src/modules/fleet/domain/reservation.rs):**
```rust
impl Reservation {
    pub fn overlaps(&self, other: &Reservation) -> bool
    pub fn can_transition_to(&self, new_status: ReservationStatus) -> bool
    pub fn is_active_now(&self) -> bool
}
// State machine: Pending -> Confirmed -> InUse -> Completed, any -> Cancelled
impl CreateReservation { pub fn validate(&self) -> Result<(), String> }
impl UpdateReservation { pub fn validate(&self, current: &Reservation) -> Result<(), String> }
```

**Fleet Domain (src/modules/fleet/domain/vehicle.rs, tool.rs):**
```rust
impl Vehicle {
    pub fn can_transition_to(&self, new_status: ResourceStatus) -> bool
}
// State machine: Available -> Reserved -> InUse -> Available, any -> Maintenance
impl CreateVehicle { pub fn validate(&self) -> Result<(), String> }
impl CreateTool { pub fn validate(&self) -> Result<(), String> }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add tests to sites::domain::site module</name>
  <files>src/modules/sites/domain/site.rs</files>
  <read_first>
    - src/modules/sites/domain/site.rs (current implementation)
    - src/common/types.rs (SiteStatus enum)
  </read_first>
  <action>Add a `#[cfg(test)] mod tests { ... }` block at the end of the file. Create tests for:

1. **Site::can_transition_to() state machine** - test all valid transitions:
   - Planned -> Active: true
   - Planned -> Completed: false
   - Planned -> Archived: false
   - Active -> Completed: true
   - Active -> Planned: false
   - Completed -> Archived: true
   - Completed -> Active: false
   - Same status: true (no change allowed)
   
2. **CreateSite::validate()** - test cases:
   - Valid input passes
   - Empty name returns Err("Site name is required")
   - Empty customer_name returns Err("Customer name is required")
   - end_date before start_date returns Err("End date cannot be before start date")
   - Negative estimated_days returns Err("Estimated days cannot be negative")

Use chrono::NaiveDate for date testing. Create helper function for test Site instances.
</action>
  <behavior>
    - Test: Site with Planned status - can_transition_to(Active) returns true
    - Test: Site with Planned status - can_transition_to(Completed) returns false
    - Test: Site with Active status - can_transition_to(Completed) returns true
    - Test: Site with Active status - can_transition_to(Planned) returns false
    - Test: Site with Completed status - can_transition_to(Archived) returns true
    - Test: Site with Completed status - can_transition_to(Active) returns false
    - Test: Site with Planned status - can_transition_to(Planned) returns true (same status)
    - Test: CreateSite with valid data - validate() returns Ok(())
    - Test: CreateSite with empty name - validate() returns Err("Site name is required")
    - Test: CreateSite with empty customer_name - validate() returns Err("Customer name is required")
    - Test: CreateSite with end_date before start_date - validate() returns Err("End date cannot be before start date")
    - Test: CreateSite with negative estimated_days - validate() returns Err("Estimated days cannot be negative")
  </behavior>
  <verify>
    <automated>cargo test --lib sites::domain::site -- --nocapture 2>&1 | grep -E "(test result|passed|failed)"</automated>
  </verify>
  <done>
    - File contains `#[cfg(test)] mod tests` block
    - All state machine transitions tested
    - All validation methods tested
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests to sites::domain::time_entry module</name>
  <files>src/modules/sites/domain/time_entry.rs</files>
  <read_first>
    - src/modules/sites/domain/time_entry.rs (current implementation)
    - src/common/types.rs (WorkType enum)
  </read_first>
  <action>Add a `#[cfg(test)] mod tests { ... }` block at the end of the file. Create tests for CreateTimeEntry::validate():

1. **Valid hours** - 8.0 hours passes
2. **Zero hours** - returns Err("Hours must be positive")
3. **Negative hours** - returns Err("Hours must be positive")
4. **Hours exceeding 24** - returns Err("Hours cannot exceed 24 per day")
5. **Future work_date** - returns Err("Work date cannot be in the future")
6. **Today work_date** - passes
7. **Past work_date** - passes

Note: For testing, use `chrono::Local::now().date_naive()` to get today. Mock or skip future date test if needed (it depends on real time). Consider using a fixed date for the past test.

Use `use super::*;` to import items.
</action>
  <behavior>
    - Test: CreateTimeEntry { hours: 8.0, work_date: today } - validate() returns Ok(())
    - Test: CreateTimeEntry { hours: 0.0, ... } - validate() returns Err("Hours must be positive")
    - Test: CreateTimeEntry { hours: -1.0, ... } - validate() returns Err("Hours must be positive")
    - Test: CreateTimeEntry { hours: 25.0, ... } - validate() returns Err("Hours cannot exceed 24 per day")
    - Test: CreateTimeEntry { hours: 24.0, work_date: today } - validate() returns Ok(())
    - Test: CreateTimeEntry { work_date: yesterday } - validate() returns Ok(())
    - Test: CreateTimeEntry { work_date: tomorrow } - validate() returns Err("Work date cannot be in the future")
  </behavior>
  <verify>
    <automated>cargo test --lib sites::domain::time_entry -- --nocapture 2>&1 | grep -E "(test result|passed|failed)"</automated>
  </verify>
  <done>
    - File contains `#[cfg(test)] mod tests` block
    - All validation edge cases tested
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Add tests to sites::domain::activity module</name>
  <files>src/modules/sites/domain/activity.rs</files>
  <read_first>
    - src/modules/sites/domain/activity.rs (current implementation)
  </read_first>
  <action>Add a `#[cfg(test)] mod tests { ... }` block at the end of the file. Read the file first to understand what business logic exists. If the module has validation or business methods, test them. If purely data, create at least one test that constructs a valid Activity struct.

Use `use super::*;` to import items.
</action>
  <behavior>
    - Test: Activity can be constructed with valid data
    - Test: Any validation methods present work correctly
  </behavior>
  <verify>
    <automated>cargo test --lib sites::domain::activity -- --nocapture 2>&1 | grep -E "(test result|passed|failed)"</automated>
  </verify>
  <done>
    - File contains `#[cfg(test)] mod tests` block
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 4: Add tests to fleet::domain::reservation module</name>
  <files>src/modules/fleet/domain/reservation.rs</files>
  <read_first>
    - src/modules/fleet/domain/reservation.rs (current implementation)
    - src/common/types.rs (ReservationStatus enum)
  </read_first>
  <action>Add a `#[cfg(test)] mod tests { ... }` block at the end of the file. This is the most complex domain module. Create tests for:

1. **Reservation::overlaps()** - time range intersection:
   - Overlapping ranges return true
   - Adjacent ranges (end == other.start) return false
   - Contained ranges return true
   - Non-overlapping ranges return false

2. **Reservation::can_transition_to() state machine**:
   - Pending -> Confirmed: true
   - Pending -> Cancelled: true
   - Pending -> InUse: false
   - Confirmed -> InUse: true
   - Confirmed -> Cancelled: true
   - Confirmed -> Pending: false
   - InUse -> Completed: true
   - InUse -> Cancelled: false (already started)
   - Cancelled -> anything: false
   - Same status: true

3. **Reservation::is_active_now()** - test with fixed times using chrono::TimeZone::timestamp

4. **CreateReservation::validate()**:
   - end_time after start_time: passes
   - end_time == start_time: fails
   - start_time in past: fails (for new reservations)

5. **UpdateReservation::validate()**:
   - Valid time range: passes
   - Invalid status transition: fails

Create helper functions for test Reservation instances with fixed timestamps.
</action>
  <behavior>
    - Test: Reservation [10:00-12:00] overlaps [11:00-13:00] returns true
    - Test: Reservation [10:00-12:00] overlaps [12:00-14:00] returns false (adjacent)
    - Test: Reservation [10:00-14:00] overlaps [11:00-12:00] returns true (contained)
    - Test: Reservation [10:00-12:00] overlaps [14:00-16:00] returns false (disjoint)
    - Test: Reservation with Pending - can_transition_to(Confirmed) returns true
    - Test: Reservation with Pending - can_transition_to(Cancelled) returns true
    - Test: Reservation with Pending - can_transition_to(InUse) returns false
    - Test: Reservation with Confirmed - can_transition_to(InUse) returns true
    - Test: Reservation with Confirmed - can_transition_to(Cancelled) returns true
    - Test: Reservation with InUse - can_transition_to(Completed) returns true
    - Test: Reservation with Cancelled - can_transition_to(Confirmed) returns false
    - Test: CreateReservation with end_time after start_time - validate() returns Ok(())
    - Test: CreateReservation with end_time == start_time - validate() returns Err("End time must be after start time")
    - Test: UpdateReservation with invalid status transition - validate() returns Err
  </behavior>
  <verify>
    <automated>cargo test --lib fleet::domain::reservation -- --nocapture 2>&1 | grep -E "(test result|passed|failed)"</automated>
  </verify>
  <done>
    - File contains `#[cfg(test)] mod tests` block
    - All overlap detection edge cases tested
    - All state machine transitions tested
    - All validation methods tested
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 5: Add tests to fleet::domain::vehicle module</name>
  <files>src/modules/fleet/domain/vehicle.rs</files>
  <read_first>
    - src/modules/fleet/domain/vehicle.rs (current implementation)
    - src/common/types.rs (ResourceStatus, VehicleType enums)
  </read_first>
  <action>Add a `#[cfg(test)] mod tests { ... }` block at the end of the file. Create tests for:

1. **Vehicle::can_transition_to() state machine**:
   - Available -> Reserved: true
   - Available -> InUse: true
   - Available -> Maintenance: true
   - Reserved -> InUse: true
   - Reserved -> Available: true (cancellation)
   - InUse -> Available: true
   - Any -> Maintenance: true
   - Maintenance -> Available: true
   - Maintenance -> InUse: false
   - Same status: true

2. **CreateVehicle::validate()**:
   - Valid input passes
   - Empty name returns Err("Vehicle name is required")
   - Empty license_plate (if provided) returns Err("License plate cannot be empty if provided")

Use `use super::*;` to import items. Create helper function for test Vehicle instances.
</action>
  <behavior>
    - Test: Vehicle with Available - can_transition_to(Reserved) returns true
    - Test: Vehicle with Available - can_transition_to(InUse) returns true
    - Test: Vehicle with Available - can_transition_to(Maintenance) returns true
    - Test: Vehicle with Reserved - can_transition_to(InUse) returns true
    - Test: Vehicle with Reserved - can_transition_to(Available) returns true
    - Test: Vehicle with InUse - can_transition_to(Available) returns true
    - Test: Vehicle with Maintenance - can_transition_to(Available) returns true
    - Test: Vehicle with Maintenance - can_transition_to(InUse) returns false
    - Test: Vehicle with Cancelled - can_transition_to(any) returns false
    - Test: CreateVehicle with valid data - validate() returns Ok(())
    - Test: CreateVehicle with empty name - validate() returns Err("Vehicle name is required")
  </behavior>
  <verify>
    <automated>cargo test --lib fleet::domain::vehicle -- --nocapture 2>&1 | grep -E "(test result|passed|failed)"</automated>
  </verify>
  <done>
    - File contains `#[cfg(test)] mod tests` block
    - All state machine transitions tested
    - All validation methods tested
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 6: Add tests to fleet::domain::tool module</name>
  <files>src/modules/fleet/domain/tool.rs</files>
  <read_first>
    - src/modules/fleet/domain/tool.rs (current implementation)
    - src/common/types.rs (ResourceStatus enum)
  </read_first>
  <action>Add a `#[cfg(test)] mod tests { ... }` block at the end of the file. The Tool has the same state machine as Vehicle. Create tests for:

1. **Tool::can_transition_to() state machine** - same transitions as Vehicle:
   - Available -> Reserved/InUse/Maintenance: true
   - Reserved -> InUse/Available: true
   - InUse -> Available: true
   - Any -> Maintenance: true
   - Maintenance -> Available: true
   - Same status: true

2. **CreateTool::validate()**:
   - Valid input passes
   - Empty name returns Err("Tool name is required")

Use `use super::*;` to import items.
</action>
  <behavior>
    - Test: Tool with Available - can_transition_to(Reserved) returns true
    - Test: Tool with Available - can_transition_to(InUse) returns true
    - Test: Tool with Available - can_transition_to(Maintenance) returns true
    - Test: Tool with Reserved - can_transition_to(InUse) returns true
    - Test: Tool with Reserved - can_transition_to(Available) returns true
    - Test: Tool with InUse - can_transition_to(Available) returns true
    - Test: Tool with Maintenance - can_transition_to(Available) returns true
    - Test: Tool with Maintenance - can_transition_to(InUse) returns false
    - Test: CreateTool with valid data - validate() returns Ok(())
    - Test: CreateTool with empty name - validate() returns Err("Tool name is required")
  </behavior>
  <verify>
    <automated>cargo test --lib fleet::domain::tool -- --nocapture 2>&1 | grep -E "(test result|passed|failed)"</automated>
  </verify>
  <done>
    - File contains `#[cfg(test)] mod tests` block
    - All state machine transitions tested
    - All validation methods tested
    - All tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unit tests -&gt; Domain code | Tests are isolated within #[cfg(test)] blocks, no external access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | State machine tests | accept | Test data is hardcoded; no external influence |
| T-12-04 | Information Disclosure | Time calculations | accept | Tests use synthetic timestamps, no real data |
</threat_model>

<verification>
1. Run `cargo test --lib` - all tests pass
2. Verify state machine coverage: grep for all can_transition_to tests
3. Verify overlap detection: grep for overlaps tests
4. Timing check: `time cargo test --lib` completes in under 5 seconds total
</verification>

<success_criteria>
- [ ] Sites domain modules have #[cfg(test)] blocks with passing tests
- [ ] Fleet domain modules have #[cfg(test)] blocks with passing tests
- [ ] All state machine transitions tested (Site, Reservation, Vehicle, Tool)
- [ ] Overlap detection tested (Reservation::overlaps)
- [ ] Time validation tested (CreateTimeEntry, CreateReservation)
- [ ] `cargo test --lib` passes with 0 failures
- [ ] Tests complete in under 1 second per module
</success_criteria>

<output>
After completion, create `.planning/phases/12-backend-domain-tests/12-02-SUMMARY.md`
</output>
