---
phase: 12-backend-domain-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/iam/domain/user.rs
  - src/modules/iam/domain/tenant.rs
  - src/modules/inventory/domain/material.rs
  - src/modules/inventory/domain/category.rs
  - src/modules/inventory/domain/order_request.rs
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "iam::domain::user module has #[cfg(test)] block with tests"
    - "iam::domain::tenant module has #[cfg(test)] block with tests"
    - "inventory::domain::material module has #[cfg(test)] block with tests"
    - "inventory::domain::category module has #[cfg(test)] block with tests"
    - "inventory::domain::order_request module has #[cfg(test)] block with tests"
    - "All tests pass with cargo test"
    - "Tests run in under 1 second (pure unit tests, no DB)"
  artifacts:
    - path: "src/modules/iam/domain/user.rs"
      provides: "User domain tests"
      contains: "#[cfg(test)]"
    - path: "src/modules/iam/domain/tenant.rs"
      provides: "Tenant domain tests"
      contains: "#[cfg(test)]"
    - path: "src/modules/inventory/domain/material.rs"
      provides: "Material domain tests"
      contains: "#[cfg(test)]"
    - path: "src/modules/inventory/domain/category.rs"
      provides: "Category domain tests"
      contains: "#[cfg(test)]"
    - path: "src/modules/inventory/domain/order_request.rs"
      provides: "OrderRequest domain tests"
      contains: "#[cfg(test)]"
  key_links:
    - from: "src/modules/iam/domain/user.rs"
      to: "src/common/types.rs"
      via: "use crate::common::types::{TenantId, UserId, Role}"
    - from: "src/modules/inventory/domain/material.rs"
      to: "src/common/types.rs"
      via: "use crate::common::types::{TenantId, MaterialId, CategoryId, Unit}"
---

<objective>
Add comprehensive inline unit tests to IAM and Inventory domain modules.

Purpose: Establish unit test coverage for pure business logic with zero external dependencies. Tests validate business rules, state transitions, and command validation.

Output: Test modules in each domain file with #[cfg(test)] attribute covering all business logic methods.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/common/types.rs
</context>

<interfaces>
<!-- Key types and business logic signatures for testing -->

**IAM Domain (src/modules/iam/domain/user.rs):**
```rust
pub struct User {
    pub id: UserId,
    pub tenant_id: TenantId,
    pub keycloak_user_id: String,
    pub email: String,
    pub name: Option<String>,
    pub role: Role,
}
impl User {
    pub fn is_admin(&self) -> bool
    pub fn is_employee(&self) -> bool
}
impl CreateUser {
    pub fn validate(&self) -> Result<(), String>
}
impl InviteUser {
    pub fn validate(&self) -> Result<(), String>
}
```

**IAM Domain (src/modules/iam/domain/tenant.rs):**
```rust
impl TenantSlug {
    pub fn new(slug: String) -> Result<Self, String>
    pub fn as_str(&self) -> &str
}
```

**Inventory Domain (src/modules/inventory/domain/material.rs):**
```rust
impl Material {
    pub fn is_low_stock(&self) -> bool
    pub fn is_last_unit(&self) -> bool
    pub fn can_withdraw(&self, amount: i32) -> bool
}
impl CreateMaterial { pub fn validate(&self) -> Result<(), String> }
impl WithdrawMaterial { pub fn validate(&self) -> Result<(), String> }
impl AdjustStock { pub fn validate(&self) -> Result<(), String> }
```

**Inventory Domain (src/modules/inventory/domain/category.rs):**
```rust
impl CreateCategory { pub fn validate(&self) -> Result<(), String> }
```

**Inventory Domain (src/modules/inventory/domain/order_request.rs):**
```rust
impl CreateOrderRequest { pub fn validate(&self) -> Result<(), String> }
impl OrderStatus { pub fn as_str(&self) -> &'static str }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add tests to iam::domain::user module</name>
  <files>src/modules/iam/domain/user.rs</files>
  <read_first>
    - src/modules/iam/domain/user.rs (current implementation)
    - src/common/types.rs (Role enum with is_admin/is_employee)
  </read_first>
  <action>Add a `#[cfg(test)] mod tests { ... }` block at the end of the file. Create tests for:

1. **User::is_admin()** - test with Role::Admin returns true, Role::Employee returns false
2. **User::is_employee()** - test with Role::Employee returns true, Role::Admin returns false
3. **CreateUser::validate()** - test cases:
   - Valid input passes (email: "test@example.com", keycloak_user_id: "kc-123")
   - Empty email returns Err("Email is required")
   - Email without '@' returns Err("Invalid email format")
   - Empty keycloak_user_id returns Err("Keycloak user ID is required")
4. **InviteUser::validate()** - test cases:
   - Valid input passes
   - Empty email returns Err("Email is required")
   - Email without '@' returns Err("Invalid email format")

Use `use super::*;` to import items. Use `assert!(result.is_ok())` and `assert!(result.is_err())` for Result checks. Keep tests minimal and focused.
</action>
  <behavior>
    - Test 1: User with Role::Admin - is_admin() returns true
    - Test 2: User with Role::Employee - is_admin() returns false
    - Test 3: User with Role::Employee - is_employee() returns true
    - Test 4: CreateUser with valid data - validate() returns Ok(())
    - Test 5: CreateUser with empty email - validate() returns Err("Email is required")
    - Test 6: CreateUser with "invalid-email" - validate() returns Err("Invalid email format")
    - Test 7: CreateUser with empty keycloak_user_id - validate() returns Err("Keycloak user ID is required")
    - Test 8: InviteUser with valid data - validate() returns Ok(())
    - Test 9: InviteUser with empty email - validate() returns Err("Email is required")
    - Test 10: InviteUser with "no-at-sign" - validate() returns Err("Invalid email format")
  </behavior>
  <verify>
    <automated>cargo test --lib iam::domain::user -- --nocapture 2>&1 | grep -E "(test result|passed|failed)"</automated>
  </verify>
  <done>
    - File contains `#[cfg(test)] mod tests` block
    - All tests pass with `cargo test`
    - Tests cover is_admin(), is_employee(), and validation methods
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests to iam::domain::tenant module</name>
  <files>src/modules/iam/domain/tenant.rs</files>
  <read_first>
    - src/modules/iam/domain/tenant.rs (current implementation)
  </read_first>
  <action>Add a `#[cfg(test)] mod tests { ... }` block at the end of the file. Create tests for TenantSlug::new():

1. **TenantSlug::new() valid** - "my-tenant" returns Ok(TenantSlug)
2. **TenantSlug::new() empty** - "" returns Err("Slug cannot be empty")
3. **TenantSlug::new() too long** - 101+ chars returns Err("Slug cannot exceed 100 characters")
4. **TenantSlug::new() invalid chars** - "my_tenant!" returns Err("Slug can only contain alphanumeric characters and hyphens")
5. **TenantSlug::new() starts with hyphen** - "-tenant" returns Err("Slug cannot start or end with hyphen")
6. **TenantSlug::new() ends with hyphen** - "tenant-" returns Err("Slug cannot start or end with hyphen")
7. **TenantSlug::from_str** - valid string parses correctly

Use `use super::*;` to import items.
</action>
  <behavior>
    - Test 1: TenantSlug::new("valid-slug") returns Ok
    - Test 2: TenantSlug::new("") returns Err("Slug cannot be empty")
    - Test 3: TenantSlug::new("a".repeat(101)) returns Err("Slug cannot exceed 100 characters")
    - Test 4: TenantSlug::new("invalid_slug!") returns Err("Slug can only contain alphanumeric characters and hyphens")
    - Test 5: TenantSlug::new("-starts-hyphen") returns Err("Slug cannot start or end with hyphen")
    - Test 6: TenantSlug::new("ends-hyphen-") returns Err("Slug cannot start or end with hyphen")
    - Test 7: "valid".parse::<TenantSlug>() returns Ok
  </behavior>
  <verify>
    <automated>cargo test --lib iam::domain::tenant -- --nocapture 2>&1 | grep -E "(test result|passed|failed)"</automated>
  </verify>
  <done>
    - File contains `#[cfg(test)] mod tests` block
    - All tests pass
    - Tests cover TenantSlug validation
  </done>
</task>

<task type="auto">
  <name>Task 3: Add tests to inventory::domain::material module</name>
  <files>src/modules/inventory/domain/material.rs</files>
  <read_first>
    - src/modules/inventory/domain/material.rs (current implementation)
    - src/common/types.rs (Unit enum)
  </read_first>
  <action>Add a `#[cfg(test)] mod tests { ... }` block at the end of the file. Create tests for:

1. **Material::is_low_stock()** - quantity &lt;= min_quantity returns true
2. **Material::is_last_unit()** - quantity == 1 returns true
3. **Material::can_withdraw(amount)** - quantity &gt;= amount returns true
4. **CreateMaterial::validate()** - name not empty, quantity &gt;= 0, min_quantity &gt;= 0
5. **WithdrawMaterial::validate()** - quantity &gt; 0
6. **AdjustStock::validate()** - reason not empty

Create a helper function `fn test_material(quantity: i32, min_quantity: i32) -> Material` to reduce boilerplate.
</action>
  <behavior>
    - Test: Material { quantity: 5, min_quantity: 10 } - is_low_stock() returns true
    - Test: Material { quantity: 15, min_quantity: 10 } - is_low_stock() returns false
    - Test: Material { quantity: 1 } - is_last_unit() returns true
    - Test: Material { quantity: 5 } - is_last_unit() returns false
    - Test: Material { quantity: 10 } - can_withdraw(5) returns true
    - Test: Material { quantity: 10 } - can_withdraw(10) returns true
    - Test: Material { quantity: 10 } - can_withdraw(11) returns false
    - Test: CreateMaterial { name: "Wood", quantity: 10, min_quantity: 5 } - validate() returns Ok(())
    - Test: CreateMaterial { name: "", ... } - validate() returns Err("Material name is required")
    - Test: CreateMaterial { quantity: -1, ... } - validate() returns Err("Quantity cannot be negative")
    - Test: CreateMaterial { min_quantity: -1, ... } - validate() returns Err("Minimum quantity cannot be negative")
    - Test: WithdrawMaterial { quantity: 1 } - validate() returns Ok(())
    - Test: WithdrawMaterial { quantity: 0 } - validate() returns Err("Withdrawal quantity must be positive")
    - Test: WithdrawMaterial { quantity: -1 } - validate() returns Err("Withdrawal quantity must be positive")
    - Test: AdjustStock { reason: "found more" } - validate() returns Ok(())
    - Test: AdjustStock { reason: "" } - validate() returns Err("Reason is required for stock adjustment")
  </behavior>
  <verify>
    <automated>cargo test --lib inventory::domain::material -- --nocapture 2>&1 | grep -E "(test result|passed|failed)"</automated>
  </verify>
  <done>
    - File contains `#[cfg(test)] mod tests` block
    - All tests pass
    - Tests cover is_low_stock(), is_last_unit(), can_withdraw(), and all validation methods
  </done>
</task>

<task type="auto">
  <name>Task 4: Add tests to inventory::domain::category module</name>
  <files>src/modules/inventory/domain/category.rs</files>
  <read_first>
    - src/modules/inventory/domain/category.rs (current implementation)
  </read_first>
  <action>Add a `#[cfg(test)] mod tests { ... }` block at the end of the file. Create tests for CreateCategory::validate():

1. **CreateCategory::validate() valid** - name "Platten" returns Ok(())
2. **CreateCategory::validate() empty name** - name "" returns Err("Category name is required")
3. **CreateCategory::validate() whitespace name** - name "   " returns Err("Category name is required")
4. **CreateCategory::validate() too long** - 101+ chars returns Err("Category name too long (max 100 chars)")

Use `use super::*;` to import items.
</action>
  <behavior>
    - Test 1: CreateCategory { name: "Platten" } - validate() returns Ok(())
    - Test 2: CreateCategory { name: "" } - validate() returns Err("Category name is required")
    - Test 3: CreateCategory { name: "   " } - validate() returns Err("Category name is required")
    - Test 4: CreateCategory { name: "a".repeat(101) } - validate() returns Err("Category name too long (max 100 chars)")
  </behavior>
  <verify>
    <automated>cargo test --lib inventory::domain::category -- --nocapture 2>&1 | grep -E "(test result|passed|failed)"</automated>
  </verify>
  <done>
    - File contains `#[cfg(test)] mod tests` block
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 5: Add tests to inventory::domain::order_request module</name>
  <files>src/modules/inventory/domain/order_request.rs</files>
  <read_first>
    - src/modules/inventory/domain/order_request.rs (current implementation)
  </read_first>
  <action>Add a `#[cfg(test)] mod tests { ... }` block at the end of the file. Create tests for:

1. **CreateOrderRequest::validate()** - quantity &gt; 0 passes, quantity &lt;= 0 fails
2. **OrderStatus::as_str()** - each variant returns correct string
3. **OrderStatus::from_str()** - valid strings parse, invalid strings fail
4. **OrderStatus::Display** - to_string() matches as_str()

Use `use super::*;` to import items.
</action>
  <behavior>
    - Test: CreateOrderRequest { quantity: 10 } - validate() returns Ok(())
    - Test: CreateOrderRequest { quantity: 0 } - validate() returns Err("Quantity must be positive")
    - Test: CreateOrderRequest { quantity: -5 } - validate() returns Err("Quantity must be positive")
    - Test: OrderStatus::Pending.as_str() == "pending"
    - Test: OrderStatus::Approved.as_str() == "approved"
    - Test: "pending".parse::&lt;OrderStatus&gt;() returns Ok(OrderStatus::Pending)
    - Test: "approved".parse::&lt;OrderStatus&gt;() returns Ok(OrderStatus::Approved)
    - Test: "invalid".parse::&lt;OrderStatus&gt;() returns Err
  </behavior>
  <verify>
    <automated>cargo test --lib inventory::domain::order_request -- --nocapture 2>&1 | grep -E "(test result|passed|failed)"</automated>
  </verify>
  <done>
    - File contains `#[cfg(test)] mod tests` block
    - All tests pass
    - Tests cover CreateOrderRequest validation and OrderStatus
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unit tests -&gt; Domain code | Tests are isolated within #[cfg(test)] blocks, no external access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | Test input data | accept | Test data is hardcoded and trusted; no external input |
| T-12-02 | Information Disclosure | Test assertions | accept | Tests run locally, no sensitive data exposed |
</threat_model>

<verification>
1. Run `cargo test --lib` - all tests pass
2. Verify test count: `cargo test --lib -- --list 2>&1 | grep -c "test$"`
3. Verify no DB dependencies: `grep -r "sqlx::" src/modules/*/domain/*.rs` should return empty
4. Timing check: `time cargo test --lib` completes in under 5 seconds total
</verification>

<success_criteria>
- [ ] IAM domain modules have #[cfg(test)] blocks with passing tests
- [ ] Inventory domain modules have #[cfg(test)] blocks with passing tests
- [ ] All business logic methods covered (is_admin, is_employee, is_low_stock, is_last_unit, can_withdraw)
- [ ] All validation methods tested with valid and invalid inputs
- [ ] `cargo test --lib` passes with 0 failures
- [ ] Tests complete in under 1 second per module
</success_criteria>

<output>
After completion, create `.planning/phases/12-backend-domain-tests/12-01-SUMMARY.md`
</output>
