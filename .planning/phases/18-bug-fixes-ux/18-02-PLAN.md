---
phase: 18
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/inventory/InventoryListPage.tsx
autonomous: true
requirements:
  - UX-02
user_setup: []
must_haves:
  truths:
    - "User can initiate QR scan by clicking QR code button on inventory page"
    - "QR scanner opens when button is clicked"
    - "User can scan a QR code or enter it manually"
  artifacts:
    - path: "frontend/src/pages/inventory/InventoryListPage.tsx"
      provides: "QR scan button with onClick handler"
      min_lines: 120
  key_links:
    - from: "InventoryListPage.tsx"
      to: "/scan route"
      via: "useNavigate hook"
      pattern: "navigate.*scan"
---

<objective>
Wire the QR code button to open the QR scanner.

Purpose: The QR button currently exists but does nothing. Users expect to be able to scan QR codes to quickly find materials.

Output: Functional QR button that navigates to the scan page.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

# Research Findings

From ISSUE-BACKLOG.md (BUG-INV-002):
> QR code button exists but has no onClick handler - it's a static button.

From research/PITFALLS.md (Pitfall 11):
> QR button in InventoryListPage renders but has no onClick. User clicks, nothing happens. Feature appears broken.

# Current Implementation

InventoryListPage.tsx lines 68-70:
```tsx
<Button variant="outline" size="icon" className="flex-shrink-0">
  <QrCode className="h-4 w-4" />
</Button>
```

No onClick handler - button is static.

# Existing QR Scanner Infrastructure

The app already has:
1. QrScanner component at `components/qr/QrScanner.tsx`
2. ScanPage at `pages/qr/ScanPage.tsx`
3. Route `/scan` defined in App.tsx (line 77)
4. QrResultDialog for handling scan results

The simplest implementation is to navigate to `/scan` route.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add onClick handler to QR button</name>
  <files>frontend/src/pages/inventory/InventoryListPage.tsx</files>
  <action>
Wire the QR button to navigate to the scan page:

1. Add useNavigate import (if not already present):
```tsx
import { useNavigate } from "react-router-dom"
```

2. Add navigate hook inside component:
```tsx
const navigate = useNavigate()
```

3. Update QR button with onClick handler:
```tsx
<Button
  variant="outline"
  size="icon"
  className="flex-shrink-0"
  onClick={() => navigate("/scan")}
>
  <QrCode className="h-4 w-4" />
</Button>
```

The existing `/scan` route (App.tsx line 77) handles:
- Opening QrScanner component
- Processing scan results
- Showing QrResultDialog
- Navigating to material detail page on successful scan

No additional state management needed - ScanPage handles everything.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | head -20</automated>
  </verify>
  <done>QR button navigates to /scan route when clicked, opening the QR scanner</done>
</task>

</tasks>

<verification>
Manual verification steps:
1. Navigate to Inventory page (/inventory)
2. Click the QR code button (next to search input)
3. Verify scanner opens (full-screen camera view)
4. Test scanning a material QR code
5. Test manual code entry option
6. Verify cancel returns to inventory page
</verification>

<success_criteria>
- [x] User can initiate QR scan by clicking QR code button on inventory page
- [x] QR scanner opens when button is clicked
- [x] User can scan a QR code or enter it manually
- [x] Cancel returns to inventory page
</success_criteria>

<output>
After completion, create `.planning/phases/18-bug-fixes-ux/18-02-SUMMARY.md`
</output>
