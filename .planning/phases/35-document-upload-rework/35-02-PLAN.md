---
phase: 35-document-upload-rework
plan: 02
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - frontend/src/pages/sites/CreateNoteModal.tsx
  - frontend/src/pages/sites/CreateNoteModal.test.tsx
  - frontend/src/lib/api/hooks/useSites.ts
  - frontend/src/lib/api/hooks/useSites.test.tsx
  - frontend/src/types/sites.ts
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
must_haves:
  truths:
    - The document modal can submit a note with attachments in one save action.
    - The document modal accepts images and PDFs, including multiple files, and lets users remove individual files before saving.
    - The form can submit attachments without note text but cannot submit when both note and attachments are empty.
  artifacts:
    - path: frontend/src/pages/sites/CreateNoteModal.tsx
      provides: Attachment-aware document composer UI.
    - path: frontend/src/lib/api/hooks/useSites.ts
      provides: Frontend mutations for generic attachment upload and attachment-aware activity creation.
    - path: frontend/src/types/sites.ts
      provides: Local activity/attachment types consumed by UI components.
  key_links:
    - from: frontend/src/pages/sites/CreateNoteModal.tsx
      to: frontend/src/lib/api/hooks/useSites.ts
      via: upload then create activity flow
      pattern: useUploadSiteAttachment|useCreateActivity
---

<objective>
Rebuild the document composer modal into a note + attachments workflow that matches the approved UI contract and the backend attachment API created in Plan 35-01.

Purpose: Users need a single submission path for notes, PDFs, and images in any valid combination.
Output: Updated modal UI, upload/create hooks, tests, and local frontend types.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-document-upload-rework/35-RESEARCH.md
@.planning/phases/35-document-upload-rework/35-UI-SPEC.md
@.planning/phases/35-document-upload-rework/35-01-PLAN.md
@frontend/src/pages/sites/CreateNoteModal.tsx
@frontend/src/lib/api/hooks/useSites.ts

<interfaces>
Use the generated attachment-aware DTOs produced by Plan 35-01; do not invent local request shapes that drift from `frontend/src/types/generated.ts`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: replace note/photo toggle with note + multi-attachment composer</name>
  <files>frontend/src/pages/sites/CreateNoteModal.tsx, frontend/src/pages/sites/CreateNoteModal.test.tsx, frontend/src/types/sites.ts</files>
  <read_first>frontend/src/pages/sites/CreateNoteModal.tsx, frontend/src/pages/sites/CreateNoteModal.test.tsx, frontend/src/types/sites.ts, .planning/phases/35-document-upload-rework/35-UI-SPEC.md, .planning/phases/34-camera-upload-flow/34-01-SUMMARY.md</read_first>
  <behavior>
    - Test 1: modal accepts note-only, attachments-only, and note+attachments submissions.
    - Test 2: image and PDF selections are both retained, invalid files are rejected with one aggregated error message, and valid files remain selected.
    - Test 3: users can remove an individual selected file before submitting.
  </behavior>
  <action>Refactor `frontend/src/pages/sites/CreateNoteModal.tsx` into the document composer described by `35-UI-SPEC.md`: title `Dokumente hinzufügen`, description `Fügen Sie eine Notiz hinzu und laden Sie Bilder oder PDFs in einem Eintrag hoch.`, textarea first with placeholder `Notiz hinzufügen (optional)...`, then a button-triggered multi-file picker labeled `Dateien auswählen` accepting `image/jpeg,image/png,image/webp,application/pdf`, then a selected-file list with image thumbnails or PDF icon/badge rows, inline helper text `Unterstützt Bilder und PDFs.`, and remove buttons named `Datei entfernen: {filename}`. Keep `CameraUploadFlow` separate; do not reintroduce camera-mode toggles into this modal. Update tests to cover DOC-01..DOC-05 behaviors and update `frontend/src/types/sites.ts` with attachment-aware local types that mirror the generated contract instead of continuing to model activities as a single `photo_url` field.</action>
  <acceptance_criteria>
    - `frontend/src/pages/sites/CreateNoteModal.tsx` no longer contains the note/photo toggle buttons labeled `Notiz` and `Foto`.
    - The modal contains `Dateien auswählen`, `Unterstützt Bilder und PDFs.`, and `Eintrag speichern` strings.
    - `frontend/src/pages/sites/CreateNoteModal.test.tsx` contains cases for note+attachments, attachments-only, PDF acceptance, and invalid-file rejection.
    - `frontend/src/types/sites.ts` contains an attachment metadata type used by the modal and feed.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- CreateNoteModal</automated>
  </verify>
  <done>The document modal matches the UI contract and supports all valid note/attachment combinations before wiring network mutations.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: wire generic attachment upload and create-activity submission flow</name>
  <files>frontend/src/lib/api/hooks/useSites.ts, frontend/src/lib/api/hooks/useSites.test.tsx, frontend/src/pages/sites/CreateNoteModal.tsx</files>
  <read_first>frontend/src/lib/api/hooks/useSites.ts, frontend/src/lib/api/hooks/useSites.test.tsx, frontend/src/pages/sites/CreateNoteModal.tsx, frontend/src/types/generated.ts, .planning/phases/35-document-upload-rework/35-01-PLAN.md</read_first>
  <behavior>
    - Test 1: the upload hook sends one multipart request per selected file using the backend contract from Plan 35-01.
    - Test 2: modal submit uploads selected files first, then sends one create-activity request with returned attachment IDs and optional note content.
    - Test 3: submit remains disabled only while pending or when both note and attachments are empty.
  </behavior>
  <action>In `frontend/src/lib/api/hooks/useSites.ts`, add/rename the upload mutation to target the generic attachment endpoint from Plan 35-01 using multipart field `attachment`, and keep request/response typing aligned with `frontend/src/types/generated.ts`. Update `frontend/src/pages/sites/CreateNoteModal.tsx` so submit uploads every selected file, collects returned attachment IDs/metadata, then calls `useCreateActivity()` exactly once with optional trimmed content plus the uploaded attachment IDs. Preserve the existing success/close/reset behavior and use the exact network failure copy from the UI spec: `Upload fehlgeschlagen. Prüfen Sie Dateityp oder Verbindung und versuchen Sie es erneut.` Update hook tests to assert the new multipart field name, PDF/image MIME coverage, and create-request payload shape.</action>
  <acceptance_criteria>
    - `frontend/src/lib/api/hooks/useSites.ts` contains a generic upload mutation that appends files under `attachment` rather than `photo`.
    - `frontend/src/pages/sites/CreateNoteModal.tsx` performs upload-first then a single create-activity mutation with attachment IDs.
    - `frontend/src/lib/api/hooks/useSites.test.tsx` asserts the `attachment` FormData key and the attachment-aware create payload.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- useSites CreateNoteModal</automated>
  </verify>
  <done>The frontend composer submits mixed document entries against the new backend contract with regression coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user file picker → React state | Untrusted files and filenames enter the UI here |
| React UI → upload/create APIs | Untrusted note text and attachment metadata cross network boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-06 | T | file selection UI | mitigate | Filter selected files by allowed MIME types, keep valid files, and surface one aggregated validation error for invalid selections. |
| T-35-07 | I | selected-file rendering | mitigate | Render filename/type only; never expose protected blob URLs directly without backend-authenticated fetch flows. |
| T-35-08 | D | submit action | mitigate | Disable submit during pending upload/create mutations to prevent duplicate requests and accidental resubmits. |
| T-35-09 | E | camera/document separation | mitigate | Keep `CameraUploadFlow` separate so document composer cannot bypass the established camera-specific path. |
</threat_model>

<verification>
The modal and hook test suites pass, and the UI matches the approved attachment workflow strings and validation states.
</verification>

<success_criteria>
Users can compose mixed document entries in the modal with images and PDFs, remove individual files, and submit one combined entry when note or attachments are present.
</success_criteria>

<output>
After completion, create `.planning/phases/35-document-upload-rework/35-document-upload-rework-02-SUMMARY.md`
</output>
