---
phase: 10-bug-fixes-round-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/fleet/FleetPage.tsx
autonomous: true
requirements: [BUG-004]
user_setup: []

must_haves:
  truths:
    - "User can click Neu button to see dropdown menu"
    - "User can select Fahrzeug to open AddVehicleDialog"
    - "User can select Werkzeug to open AddToolDialog"
    - "Dialogs close after successful creation"
  artifacts:
    - path: "frontend/src/pages/fleet/FleetPage.tsx"
      provides: "Dropdown menu with dialog integration"
      contains: "dialogType state"
  key_links:
    - from: "FleetPage.tsx"
      to: "AddVehicleDialog"
      via: "dialogType === 'vehicle'"
      pattern: "AddVehicleDialog.*open"
    - from: "FleetPage.tsx"
      to: "AddToolDialog"
      via: "dialogType === 'tool'"
      pattern: "AddToolDialog.*open"
---

<objective>
Fix non-functional "Neu" button in Fleet page by wiring up existing AddVehicleDialog and AddToolDialog components.

Purpose: Enable users to add vehicles and tools from the Fleet page.
Output: Working dropdown menu that opens the appropriate dialog.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/BUG-REPORT.md

# Key source files
@frontend/src/pages/fleet/FleetPage.tsx
@frontend/src/pages/fleet/AddVehicleDialog.tsx
@frontend/src/pages/fleet/AddToolDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dropdown menu to FleetPage Neu button</name>
  <files>
    - frontend/src/pages/fleet/FleetPage.tsx
  </files>
  <read_first>
    - frontend/src/pages/fleet/FleetPage.tsx (current button implementation)
    - frontend/src/pages/fleet/AddVehicleDialog.tsx (dialog interface)
    - frontend/src/pages/fleet/AddToolDialog.tsx (dialog interface)
  </read_first>
  <behavior>
    - Test: Clicking Neu button opens dropdown menu
    - Test: Selecting Fahrzeug opens AddVehicleDialog
    - Test: Selecting Werkzeug opens AddToolDialog
    - Test: Dialogs close after successful creation
  </behavior>
  <action>
Fix BUG-004: Fleet "Neu" button non-functional.

In `frontend/src/pages/fleet/FleetPage.tsx`:

1. Add imports at top (after existing imports):
   ```typescript
   import { AddVehicleDialog } from "./AddVehicleDialog"
   import { AddToolDialog } from "./AddToolDialog"
   import {
     DropdownMenu,
     DropdownMenuContent,
     DropdownMenuItem,
     DropdownMenuTrigger,
   } from "@/components/ui/dropdown-menu"
   import { Car, Wrench } from "lucide-react"
   ```

2. Add dialog state after existing state declarations:
   ```typescript
   const [dialogType, setDialogType] = useState<"vehicle" | "tool" | null>(null)
   ```

3. Replace the existing Neu button (lines 49-52) with dropdown menu:
   ```tsx
   <DropdownMenu>
     <DropdownMenuTrigger asChild>
       <Button className="gap-2">
         <Plus className="h-4 w-4" />
         <span className="hidden sm:inline">Neu</span>
       </Button>
     </DropdownMenuTrigger>
     <DropdownMenuContent align="end">
       <DropdownMenuItem onClick={() => setDialogType("vehicle")}>
         <Car className="h-4 w-4 mr-2" />
         Fahrzeug
       </DropdownMenuItem>
       <DropdownMenuItem onClick={() => setDialogType("tool")}>
         <Wrench className="h-4 w-4 mr-2" />
         Werkzeug
       </DropdownMenuItem>
     </DropdownMenuContent>
   </DropdownMenu>
   ```

4. Add dialog components after the ReservationDialog (at end of JSX, before closing div):
   ```tsx
   <AddVehicleDialog
     open={dialogType === "vehicle"}
     onOpenChange={(open) => !open && setDialogType(null)}
   />
   <AddToolDialog
     open={dialogType === "tool"}
     onOpenChange={(open) => !open && setDialogType(null)}
   />
   ```

The dialogs will automatically close after successful creation because they call onOpenChange(false), which will set dialogType to null.
</action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | grep -c "error" | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    - FleetPage.tsx imports AddVehicleDialog, AddToolDialog, and DropdownMenu components
    - dialogType state variable exists
    - DropdownMenu replaces the non-functional Button
    - Both dialogs are rendered with proper open/onOpenChange props
    - Build passes without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Vehicle/Tool creation sends data to backend, backend validates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | Form input | mitigate | Backend validates all vehicle/tool fields |
</threat_model>

<verification>
1. Run `npm run build` in frontend directory
2. Verify no TypeScript errors
3. Check that all imports resolve correctly
</verification>

<success_criteria>
1. User can click Neu button to see dropdown menu
2. User can select "Fahrzeug" to open AddVehicleDialog
3. User can select "Werkzeug" to open AddToolDialog
4. Dialogs close after successful creation
5. Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-bug-fixes-round-2/10-02-SUMMARY.md`
</output>
