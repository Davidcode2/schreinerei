---
phase: 39-range-selection-confirmation-flow
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/fleet/calendarRangeSelection.ts
  - frontend/src/pages/fleet/calendarRangeSelection.test.ts
autonomous: true
requirements:
  - FSEL-01
  - FSEL-02
  - FSEL-03
  - FSEL-04
must_haves:
  truths:
    - User can start a pending reservation selection with the first tap.
    - User can complete a same-resource range with the second tap, including the same day twice.
    - The completed range is always normalized so the earlier date is the start.
  artifacts:
    - path: frontend/src/pages/fleet/calendarRangeSelection.ts
      provides: Pure selection contract for calendar taps
      exports:
        - createPendingRangeSelection
        - advanceRangeSelection
    - path: frontend/src/pages/fleet/calendarRangeSelection.test.ts
      provides: Regression coverage for tap-to-range rules
  key_links:
    - from: frontend/src/pages/fleet/calendarRangeSelection.ts
      to: frontend/src/pages/fleet/CalendarView.tsx
      via: imported selection reducer/helper used by click handlers
      pattern: advanceRangeSelection
---

<objective>
Define and prove the range-selection contract that Phase 39 depends on.

Purpose: isolate the date-range rules before UI wiring so the executor can build the calendar flow against a deterministic contract.
Output: a tested helper module that encodes first-tap pending state, same-resource completion, same-day booking support, and start/end sorting.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-fleet-page-calendar-integration/38-01-SUMMARY.md
@.planning/phases/39-range-selection-confirmation-flow/39-RESEARCH.md

<interfaces>
From frontend/src/types/fleet.ts:
```ts
export type ResourceType = 'vehicle' | 'tool'
```

From frontend/src/pages/fleet/CalendarView.tsx:
```ts
function getWeekDates(date: Date): { start: string; end: string }
```

Selection helper contract to create in this plan:
```ts
export interface PendingRangeSelection {
  resourceId: string
  resourceType: ResourceType
  firstDate: string
}

export interface CompletedRangeSelection {
  resourceId: string
  resourceType: ResourceType
  startDate: string
  endDate: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: write failing tests for the selection contract</name>
  <files>frontend/src/pages/fleet/calendarRangeSelection.test.ts</files>
  <action>Create a focused Vitest suite that defines the required tap behavior per FSEL-01, FSEL-02, FSEL-03, and FSEL-04. Cover: first tap returns a pending selection, second tap on the same resource returns a completed range, second tap on the same date yields a one-day range, reverse-order taps normalize to earlier-first, and a tap on a different resource does not create a cross-row range. Keep the tests pure; do not render React here.</action>
  <verify>
    <automated>cd frontend && npm run test:run -- src/pages/fleet/calendarRangeSelection.test.ts</automated>
  </verify>
  <done>The test file expresses every required Phase 39 range-selection rule and initially fails because the helper does not exist yet.</done>
</task>

<task type="auto">
  <name>task 2: implement the pure range-selection helper</name>
  <files>frontend/src/pages/fleet/calendarRangeSelection.ts, frontend/src/pages/fleet/calendarRangeSelection.test.ts</files>
  <action>Create the helper module consumed later by `CalendarView`. Export typed functions that accept the current pending selection plus the tapped resource/date and return either a new pending state or a completed range. Enforce same-resource completion only, support same-day completion, and sort reverse-order dates before returning the completed range. Keep the module free of UI concerns, `Date` objects in persisted state, and React dependencies so later plans can wire it directly.</action>
  <verify>
    <automated>cd frontend && npm run test:run -- src/pages/fleet/calendarRangeSelection.test.ts</automated>
  </verify>
  <done>The helper exports a stable typed contract with passing regression tests for all Phase 39 date-range rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calendar cell click → selection helper | Untrusted UI events must not create invalid cross-resource ranges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | T | frontend/src/pages/fleet/calendarRangeSelection.ts | mitigate | Reject cross-resource completion by comparing the pending selection resource with the second tap before returning a completed range. |
| T-39-02 | D | frontend/src/pages/fleet/calendarRangeSelection.ts | mitigate | Normalize reverse-order taps inside the helper so downstream UI and API code never receive inverted ranges. |
| T-39-03 | R | frontend/src/pages/fleet/calendarRangeSelection.test.ts | mitigate | Lock required behaviors in unit tests so future UI refactors cannot silently reintroduce first-tap immediate booking. |
</threat_model>

<verification>
- `cd frontend && npm run test:run -- src/pages/fleet/calendarRangeSelection.test.ts`
</verification>

<success_criteria>
- The selection contract is fully encoded in a pure typed helper.
- Same-day and reverse-order date selections are covered by automated tests.
- Later plans can import the helper without re-deriving selection rules from UI code.
</success_criteria>

<output>
After completion, create `.planning/phases/39-range-selection-confirmation-flow/39-01-SUMMARY.md`
</output>
