---
phase: 32-enriched-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/api/hooks/useInventory.ts
  - frontend/src/lib/api/hooks/useInventory.test.tsx
autonomous: true
requirements:
  - STOCK-02
  - HIST-02
  - HIST-03
must_haves:
  truths:
    - "Inventory detail history can request enriched entries with entry_type, user_name, and Baustelle metadata."
    - "The frontend uses a dedicated enriched-history query instead of the legacy /history contract."
  artifacts:
    - path: "frontend/src/lib/api/hooks/useInventory.ts"
      provides: "Dedicated useEnrichedMaterialHistory React Query hook"
    - path: "frontend/src/lib/api/hooks/useInventory.test.tsx"
      provides: "Regression coverage for the enriched history endpoint and cache key"
  key_links:
    - from: "frontend/src/lib/api/hooks/useInventory.ts"
      to: "/api/v1/inventory/materials/{id}/history/enriched"
      via: "apiClient.get in useEnrichedMaterialHistory"
      pattern: "history/enriched"
---

<objective>
Introduce the dedicated enriched-history data hook the Phase 32 UI will render.

Purpose: consume the Phase 30 `/history/enriched` backend contract without page-local fetch logic or contract drift.
Output: a tested `useEnrichedMaterialHistory` hook ready for the detail-page history UI.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-enriched-history/32-RESEARCH.md
@.planning/phases/32-enriched-history/32-UI-SPEC.md
@.planning/phases/30-backend-api-foundation/30-02-SUMMARY.md
@frontend/src/lib/api/hooks/useInventory.ts
@frontend/src/lib/api/hooks/useInventory.test.tsx
@frontend/src/types/inventory.ts

<interfaces>
From `frontend/src/types/inventory.ts`:

```typescript
export type EntryType = 'withdrawn' | 'adjusted' | 'material_added' | 'location_changed' | 'min_quantity_changed'

export interface EnrichedStockHistoryEntry {
  id: string
  material_id: string
  user_id: string
  user_name: string
  entry_type: EntryType
  quantity_change: number
  quantity_after: number
  notes: string | null
  site_id: string | null
  site_name: string | null
  category_name: string
  created_at: string
}
```

From `frontend/src/lib/api/hooks/useInventory.ts` current pattern:

```typescript
export function useMaterialHistory(id: string) {
  return useQuery({
    queryKey: ["material-history", id],
    queryFn: () => apiClient.get<MaterialStockHistoryEntry[]>(`/api/v1/inventory/materials/${id}/history`),
    enabled: !!id,
    staleTime: 30000,
  })
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add enriched-history hook regression coverage first</name>
  <files>frontend/src/lib/api/hooks/useInventory.test.tsx</files>
  <read_first>frontend/src/lib/api/hooks/useInventory.test.tsx, frontend/src/lib/api/hooks/useInventory.ts, frontend/src/types/inventory.ts</read_first>
  <behavior>
    - Test 1: `useEnrichedMaterialHistory("mat-123")` requests `/api/v1/inventory/materials/mat-123/history/enriched`.
    - Test 2: the hook registers a dedicated cache key `['material-history-enriched', 'mat-123']` so the legacy history cache is not silently reused.
    - Test 3: the hook stays disabled when called with an empty material id.
  </behavior>
  <action>Add Vitest coverage for a new `useEnrichedMaterialHistory` export. Mirror the existing `useMaterialHistory` test style, but assert the enriched endpoint path exactly, assert the cache key exactly as `['material-history-enriched', 'mat-123']`, and assert `result.current.isEnabled === false` for the empty-id case. Keep the legacy `useMaterialHistory` tests intact so the old endpoint remains explicitly covered until Plan 32-02 finishes the UI migration.</action>
  <acceptance_criteria>
    - `frontend/src/lib/api/hooks/useInventory.test.tsx` contains `describe("useEnrichedMaterialHistory"`.
    - The new test block asserts `/api/v1/inventory/materials/mat-123/history/enriched`.
    - The new test block asserts query key `['material-history-enriched', 'mat-123']`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- src/lib/api/hooks/useInventory.test.tsx</automated>
  </verify>
  <done>The enriched-history hook contract is specified by failing-then-passing tests before implementation.</done>
</task>

<task type="auto">
  <name>task 2: implement the dedicated enriched-history query hook</name>
  <files>frontend/src/lib/api/hooks/useInventory.ts</files>
  <read_first>frontend/src/lib/api/hooks/useInventory.ts, frontend/src/types/inventory.ts, src/modules/inventory/api/routes.rs</read_first>
  <action>Export `useEnrichedMaterialHistory(id: string)` from `frontend/src/lib/api/hooks/useInventory.ts` using the existing `useQuery` pattern. Its `queryKey` must be `['material-history-enriched', id]`, its `queryFn` must call `apiClient.get<EnrichedStockHistoryEntry[]>(`/api/v1/inventory/materials/${id}/history/enriched`)`, `enabled` must remain `!!id`, and `staleTime` must stay `30000`. Add the required `EnrichedStockHistoryEntry` type import from `@/types/inventory`. Do not replace or remove `useMaterialHistory` in this plan; Plan 32-02 will switch the page over after the new hook exists.</action>
  <acceptance_criteria>
    - `frontend/src/lib/api/hooks/useInventory.ts` exports `useEnrichedMaterialHistory`.
    - The hook uses `EnrichedStockHistoryEntry[]` as the response type.
    - The hook path is exactly `/api/v1/inventory/materials/${id}/history/enriched`.
    - The hook query key is exactly `['material-history-enriched', id]`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- src/lib/api/hooks/useInventory.test.tsx</automated>
  </verify>
  <done>The frontend owns a tested enriched-history data contract that later UI work can consume without inventing new fetch logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→inventory API | Untrusted network data crosses into typed React Query responses |
| hook→page rendering | History payload values become visible UI text and links in later plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | T | `frontend/src/lib/api/hooks/useInventory.ts` | mitigate | Call only the dedicated `/history/enriched` endpoint and keep a distinct query key so legacy-response tampering cannot masquerade as enriched data. |
| T-32-02 | I | `frontend/src/lib/api/hooks/useInventory.ts` | accept | This plan fetches stock-movement metadata already authorized by the backend; no new client-side storage or logging is introduced. |
| T-32-03 | D | `frontend/src/lib/api/hooks/useInventory.ts` | mitigate | Preserve `enabled: !!id` so empty ids do not trigger useless requests or retry churn. |
</threat_model>

<verification>
- `npm run test:run -- src/lib/api/hooks/useInventory.test.tsx`
- Confirm the new hook export exists and points at `/history/enriched`.
</verification>

<success_criteria>
- The codebase contains a dedicated `useEnrichedMaterialHistory` hook.
- Regression tests prove the enriched hook uses the enriched endpoint and its own cache key.
- No UI file is forced to invent fetch logic to obtain `entry_type`, `user_name`, or Baustelle metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/32-enriched-history/32-01-SUMMARY.md`
</output>
