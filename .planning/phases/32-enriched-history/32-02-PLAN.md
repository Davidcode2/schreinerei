---
phase: 32-enriched-history
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - frontend/src/pages/inventory/InventoryDetailPage.tsx
  - frontend/src/pages/inventory/InventoryDetailPage.test.tsx
  - frontend/src/pages/inventory/MaterialHistoryFeed.tsx
autonomous: true
requirements:
  - STOCK-02
  - HIST-01
  - HIST-02
  - HIST-03
must_haves:
  truths:
    - "Users can distinguish stock-in, withdrawal, and adjustment entries by badge color and label."
    - "Each inventory history row shows who performed the action as `von {user_name}`."
    - "Withdrawal rows link to the Baustelle detail page when site data is present."
    - "Stock-in rows appear as positive `material_added` events instead of reading like withdrawals only."
  artifacts:
    - path: "frontend/src/pages/inventory/MaterialHistoryFeed.tsx"
      provides: "Dedicated enriched history presentation for inventory detail"
    - path: "frontend/src/pages/inventory/InventoryDetailPage.tsx"
      provides: "Inventory detail page wired to the enriched history hook"
    - path: "frontend/src/pages/inventory/InventoryDetailPage.test.tsx"
      provides: "Regression coverage for badges, attribution, links, and empty state"
  key_links:
    - from: "frontend/src/pages/inventory/InventoryDetailPage.tsx"
      to: "frontend/src/lib/api/hooks/useInventory.ts"
      via: "useEnrichedMaterialHistory(id)"
      pattern: "useEnrichedMaterialHistory"
    - from: "frontend/src/pages/inventory/MaterialHistoryFeed.tsx"
      to: "/sites/:site_id"
      via: "react-router-dom Link"
      pattern: "to={`/sites/"
---

<objective>
Upgrade the inventory detail page to render the enriched stock-movement feed defined by Phase 30 and the Phase 32 UI contract.

Purpose: make history understandable at a glance, attributable to a user, and navigable back to the related Baustelle.
Output: a dedicated inventory history presentation component plus detail-page regression coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-enriched-history/32-RESEARCH.md
@.planning/phases/32-enriched-history/32-UI-SPEC.md
@.planning/phases/25-deduction-history-site-name-end-to-end-wiring/25-01-SUMMARY.md
@.planning/phases/31-settings-editing-stock-in/31-03-SUMMARY.md
@frontend/src/pages/inventory/InventoryDetailPage.tsx
@frontend/src/pages/inventory/InventoryDetailPage.test.tsx
@frontend/src/pages/sites/ActivityFeed.tsx
@frontend/src/types/inventory.ts

<interfaces>
From `frontend/src/types/inventory.ts`:

```typescript
export type EntryType = 'withdrawn' | 'adjusted' | 'material_added' | 'location_changed' | 'min_quantity_changed'

export interface EnrichedStockHistoryEntry {
  id: string
  material_id: string
  user_id: string
  user_name: string
  entry_type: EntryType
  quantity_change: number
  quantity_after: number
  notes: string | null
  site_id: string | null
  site_name: string | null
  category_name: string
  created_at: string
}
```

From `frontend/src/pages/sites/ActivityFeed.tsx` link pattern:

```tsx
{entry.site_id && entry.site_name && (
  <Link to={`/sites/${entry.site_id}`} className="text-xs text-blue-600 hover:underline">
    {entry.site_name}
  </Link>
)}
```

Phase 32 UI contract fixed labels and colors:

```text
material_added -> Eingelagert -> bg-green-100 text-green-700 border-green-200
withdrawn -> Entnommen -> bg-red-100 text-red-700 border-red-200
adjusted -> Bestand korrigiert -> bg-blue-100 text-blue-700 border-blue-200
Attribution pattern: von {user_name}
Empty state heading: Noch keine Materialbewegungen
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: write detail-page regression tests for enriched history rendering</name>
  <files>frontend/src/pages/inventory/InventoryDetailPage.test.tsx</files>
  <read_first>frontend/src/pages/inventory/InventoryDetailPage.test.tsx, frontend/src/pages/inventory/InventoryDetailPage.tsx, frontend/src/pages/sites/ActivityFeed.tsx, .planning/phases/32-enriched-history/32-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: `material_added` rows render the `Eingelagert` badge, a positive `+` quantity, and `von {user_name}`.
    - Test 2: `withdrawn` rows render the `Entnommen` badge and a Baustelle link with `href="/sites/{site_id}"`.
    - Test 3: `adjusted` rows render `Bestand korrigiert` with the blue badge family.
    - Test 4: empty enriched history renders `Noch keine Materialbewegungen` and the explanatory body copy from the UI spec.
  </behavior>
  <action>Replace the legacy `/history` mocks in `InventoryDetailPage.test.tsx` with `/history/enriched` fixtures that include `entry_type`, `user_name`, `site_id`, and `site_name`. Add assertions for the fixed German event labels, `von Max Mustermann` style attribution, explicit `+3`-style positive quantity rendering for stock-in, and a real Baustelle link for withdrawals. Update the empty-state assertion to the new copy `Noch keine Materialbewegungen` plus `Einlagerungen, Entnahmen und Korrekturen erscheinen hier, sobald die erste Änderung erfasst wurde.` Keep the existing material-detail action tests intact.</action>
  <acceptance_criteria>
    - `frontend/src/pages/inventory/InventoryDetailPage.test.tsx` mocks `/api/v1/inventory/materials/mat-123/history/enriched`.
    - The test file asserts `Eingelagert`, `Entnommen`, and `Bestand korrigiert`.
    - The test file asserts `von Max Mustermann` (or equivalent fixture user) and a `/sites/site-1` link.
    - The empty-state assertion uses `Noch keine Materialbewegungen`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- src/pages/inventory/InventoryDetailPage.test.tsx</automated>
  </verify>
  <done>The enriched history UI behavior is locked down by failing-then-passing tests before component work begins.</done>
</task>

<task type="auto">
  <name>task 2: extract and wire a dedicated enriched history feed component</name>
  <files>frontend/src/pages/inventory/MaterialHistoryFeed.tsx, frontend/src/pages/inventory/InventoryDetailPage.tsx</files>
  <read_first>frontend/src/pages/inventory/InventoryDetailPage.tsx, frontend/src/pages/sites/ActivityFeed.tsx, frontend/src/types/inventory.ts, .planning/phases/32-enriched-history/32-UI-SPEC.md</read_first>
  <action>Create `frontend/src/pages/inventory/MaterialHistoryFeed.tsx` as a dedicated inventory-only presenter for `EnrichedStockHistoryEntry[]` so the page does not reuse the sites `ActivityFeed` component. In that component, define a concrete `entryTypeConfig` map with these exact labels and badge classes: `material_added -> Eingelagert -> bg-green-100 text-green-700 border-green-200`, `withdrawn -> Entnommen -> bg-red-100 text-red-700 border-red-200`, `adjusted -> Bestand korrigiert -> bg-blue-100 text-blue-700 border-blue-200`, `location_changed -> Lagerort geändert -> bg-blue-100 text-blue-700 border-blue-200`, `min_quantity_changed -> Mindestbestand geändert -> bg-blue-100 text-blue-700 border-blue-200`. Render each row in the UI-spec order: badge + timestamp + signed quantity on the top row, then `von {user_name}`, then optional notes and optional Baustelle metadata. For `withdrawn` rows with both `site_id` and `site_name`, render `<Link to={`/sites/${site_id}`}>` using accent-blue text with hover underline; otherwise render plain muted text or omit the site block. Update `InventoryDetailPage.tsx` to import `useEnrichedMaterialHistory`, replace the legacy history hook, and render the new `MaterialHistoryFeed` inside the history card. Keep the independent material page load behavior intact; if the enriched history query alone fails, show the existing `ErrorState` component inside the history section with the fixed message `Historie konnte nicht geladen werden. Bitte Seite neu laden oder später erneut versuchen.` and a retry affordance wired to the history query refetch. Preserve `toLocaleString('de-DE')` formatting and keep positive quantities explicitly prefixed with `+`.</action>
  <acceptance_criteria>
    - `frontend/src/pages/inventory/InventoryDetailPage.tsx` imports and uses `useEnrichedMaterialHistory`.
    - `frontend/src/pages/inventory/MaterialHistoryFeed.tsx` exists and contains the fixed label/class map for all five `EntryType` values.
    - Withdrawal rows render a `Link` to `/sites/${site_id}` when both site fields exist.
    - Stock-in rows render `Eingelagert` with a positive `+` quantity.
    - The history empty state uses both fixed copy lines from the UI spec.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- src/pages/inventory/InventoryDetailPage.test.tsx</automated>
  </verify>
  <done>The inventory detail page renders enriched history with visual differentiation, user attribution, and navigable Baustelle links per HIST-01/02/03 and STOCK-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inventory API→React UI | Untrusted history text and ids are rendered to users |
| React UI→router navigation | API-provided site identifiers are transformed into internal links |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | T | `frontend/src/pages/inventory/MaterialHistoryFeed.tsx` | mitigate | Render `notes`, `user_name`, and `site_name` as plain React text only; do not use `dangerouslySetInnerHTML`. |
| T-32-05 | I | `frontend/src/pages/inventory/MaterialHistoryFeed.tsx` | mitigate | Only create Baustelle links when both `site_id` and `site_name` exist, and point them strictly to internal `/sites/${site_id}` routes. |
| T-32-06 | D | `frontend/src/pages/inventory/InventoryDetailPage.tsx` | mitigate | Keep history failure isolated to the history card via `ErrorState` + retry instead of crashing the full detail page. |
| T-32-07 | R | `frontend/src/pages/inventory/MaterialHistoryFeed.tsx` | accept | User attribution comes from backend-provided `user_name`; this plan displays existing audit data but does not alter its source of truth. |
</threat_model>

<verification>
- `npm run test:run -- src/pages/inventory/InventoryDetailPage.test.tsx`
- Inspect `InventoryDetailPage.tsx` to confirm it no longer calls the legacy `useMaterialHistory` hook for the rendered history section.
</verification>

<success_criteria>
- Inventory detail history uses the enriched endpoint contract.
- Badge labels and colors distinguish stock-in, withdrawal, and adjustment rows.
- Every rendered history row shows `von {user_name}`.
- Withdrawal rows link to Baustelle detail pages when site data exists.
- Empty history and error copy match the Phase 32 UI spec exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/32-enriched-history/32-02-SUMMARY.md`
</output>
