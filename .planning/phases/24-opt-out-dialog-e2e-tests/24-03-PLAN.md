---
phase: 24-opt-out-dialog-e2e-tests
plan: 03
type: execute
wave: 3
depends_on:
  - 24-02
files_modified:
  - frontend/tests/helpers/api.ts
  - frontend/tests/active-site-workflow.spec.ts
autonomous: true
requirements:
  - TEST-16
  - TEST-17
  - TEST-18
  - DLOG-02
  - DLOG-04
must_haves:
  truths:
    - E2E verifies user can set active Baustelle preference
    - E2E verifies auto-assignment in withdrawal flow
    - E2E verifies dialog interaction paths (auto-confirm and dismiss/unassign)
  artifacts:
    - path: frontend/tests/active-site-workflow.spec.ts
      provides: End-to-end workflow coverage for active site + opt-out behavior
    - path: frontend/tests/helpers/api.ts
      provides: API helpers for preferences endpoint interactions in tests
  key_links:
    - from: active-site-workflow.spec.ts
      to: /api/v1/preferences
      via: helper methods
      pattern: "preferences"
    - from: active-site-workflow.spec.ts
      to: inventory withdraw request
      via: asserted payload/result behavior
      pattern: "site_id"
---

<objective>
Add Playwright coverage for active-site preference, auto-assignment, and opt-out dialog behavior.

Purpose: Prove Phase 24 behavior end-to-end and prevent regressions.
Output: New E2E spec and helper API methods for preferences setup/verification.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-opt-out-dialog-e2e-tests/24-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Playwright API helpers for preferences</name>
  <files>frontend/tests/helpers/api.ts</files>
  <read_first>
    - frontend/tests/helpers/api.ts
    - frontend/src/types/generated.ts
    - src/modules/iam/api/routes.rs
  </read_first>
  <behavior>
    - Tests can read current user preference (`active_site_id`).
    - Tests can update/clear preference through PATCH `/api/v1/preferences`.
  </behavior>
  <action>Add typed helper functions in `frontend/tests/helpers/api.ts` for `getPreferences(page)` and `updatePreferences(page, { active_site_id })` calling `/api/v1/preferences`. Include response typing aligned with generated DTO shape (`active_site_id: string | null`) and throw explicit errors on non-OK responses.</action>
  <acceptance_criteria>
    - `api.ts` exports `getPreferences`
    - `api.ts` exports `updatePreferences`
    - Both helpers call `/api/v1/preferences`
    - PATCH helper supports `active_site_id: null` to clear preference
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run typecheck</automated>
  </verify>
  <done>Preferences helper layer is available for deterministic E2E setup/cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement active-site workflow Playwright spec</name>
  <files>frontend/tests/active-site-workflow.spec.ts</files>
  <read_first>
    - frontend/tests/inventory.spec.ts
    - frontend/tests/sites.spec.ts
    - frontend/tests/helpers/auth.ts
    - frontend/tests/helpers/api.ts
    - frontend/tests/helpers/data.ts
  </read_first>
  <behavior>
    - TEST-16: user sets active Baustelle and value persists in preferences API.
    - TEST-17: material withdrawal flow auto-assigns active site when auto-confirm path wins.
    - TEST-18: dialog interaction supports dismiss/unassign path and sends no `site_id`.
  </behavior>
  <action>Create `active-site-workflow.spec.ts` with three tests mapped one-to-one to TEST-16/17/18. Use API helpers to seed site/material, set active preference, and clean up. For auto-confirm, assert resulting record/history includes expected site binding. For dismiss/unassign, assert resulting action completes and stored entry/reservation/time-entry has `site_id` null/omitted. Use resilient waits (`expect.poll`/`toPass`) around async UI updates and backend persistence checks.</action>
  <acceptance_criteria>
    - File contains tests labeled for TEST-16, TEST-17, TEST-18 intent
    - At least one test validates auto-confirm path after ~5s timeout
    - At least one test validates dismiss/unassign path yields null/absent `site_id`
    - Spec uses cleanup helpers and leaves no persistent test artifacts
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx playwright test tests/active-site-workflow.spec.ts</automated>
  </verify>
  <done>E2E suite covers Phase 24 goal and passes reliably in local CI profile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test actor → authenticated API endpoints | E2E user actions and API requests exercise tenant/user scoped data paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | I | E2E assertions/data setup | mitigate | Use per-test generated names and cleanup helpers to avoid leaking tenant data across tests |
| T-24-08 | T | preference update helper | mitigate | Helper enforces explicit payload shape and checks response status before proceeding |
| T-24-09 | D | time-based test flow | accept | 5-second timer dependency is product requirement; mitigate flakiness with polling-based assertions |
</threat_model>

<verification>
- `cd frontend && npm run typecheck`
- `cd frontend && npx playwright test tests/active-site-workflow.spec.ts`
</verification>

<success_criteria>
- TEST-16/17/18 all have automated E2E coverage
- Coverage validates both default auto-confirm and explicit opt-out behavior
- New spec is stable enough for CI execution
</success_criteria>

<output>
After completion, create `.planning/phases/24-opt-out-dialog-e2e-tests/24-03-SUMMARY.md`
</output>
