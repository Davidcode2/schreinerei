---
phase: 24-opt-out-dialog-e2e-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/shared/AutoAssignmentConfirmDialog.tsx
  - frontend/src/hooks/useAutoAssignmentConfirmation.ts
  - frontend/src/types/auto-assignment.ts
autonomous: true
requirements:
  - DLOG-01
  - DLOG-02
  - DLOG-03
  - DLOG-04
  - DLOG-05
must_haves:
  truths:
    - Auto-assignment confirmation appears without blocking the user flow
    - No user action for 5 seconds applies the default confirmation path
    - User can switch assignment or explicitly choose unassigned from the dialog
  artifacts:
    - path: frontend/src/components/shared/AutoAssignmentConfirmDialog.tsx
      provides: Reusable non-blocking confirmation dialog UI
      exports: ["AutoAssignmentConfirmDialog"]
    - path: frontend/src/hooks/useAutoAssignmentConfirmation.ts
      provides: Timer + decision orchestration for auto-assignment state
      exports: ["useAutoAssignmentConfirmation"]
    - path: frontend/src/types/auto-assignment.ts
      provides: Shared contracts for confirmation decision payloads
      exports: ["AutoAssignmentDecision", "AutoAssignmentDraft"]
  key_links:
    - from: useAutoAssignmentConfirmation
      to: AutoAssignmentConfirmDialog
      via: controlled open/state props
      pattern: "open.*onDecision"
    - from: useAutoAssignmentConfirmation
      to: setTimeout
      via: 5-second auto-confirm timer
      pattern: "5000"
---

<objective>
Create the reusable auto-assignment confirmation contract (types + hook + dialog) for all form flows.

Purpose: Centralize DLOG behavior once so all forms stay consistent.
Output: Shared component/hook/types ready for integration in form dialogs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-opt-out-dialog-e2e-tests/24-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define auto-assignment contracts and decision model</name>
  <files>frontend/src/types/auto-assignment.ts</files>
  <read_first>
    - frontend/src/types/generated.ts
    - frontend/src/types/sites.ts
    - frontend/src/types/fleet.ts
  </read_first>
  <behavior>
    - Decision model distinguishes: confirm current suggestion, change to another site, and unassign.
    - Draft model supports optional suggested site and optional selected site.
  </behavior>
  <action>Create `AutoAssignmentDraft` and `AutoAssignmentDecision` exported types with explicit union branches (`confirm`, `change`, `unassign`) and explicit `siteId` fields where required. Ensure this file is import-only (no React runtime code).</action>
  <acceptance_criteria>
    - `frontend/src/types/auto-assignment.ts` exists
    - File exports `AutoAssignmentDraft`
    - File exports `AutoAssignmentDecision`
    - `AutoAssignmentDecision` includes `confirm | change | unassign` cases
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run typecheck</automated>
  </verify>
  <done>Shared type contracts compile and are importable by hooks/components.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build timer-driven non-blocking confirmation hook + dialog component</name>
  <files>frontend/src/hooks/useAutoAssignmentConfirmation.ts, frontend/src/components/shared/AutoAssignmentConfirmDialog.tsx</files>
  <read_first>
    - frontend/src/components/ui/dialog.tsx
    - frontend/src/components/shared/DeleteConfirmDialog.tsx
    - frontend/src/pages/fleet/ReservationDialog.tsx
    - frontend/src/pages/sites/TimeEntryDialog.tsx
    - frontend/src/types/auto-assignment.ts
  </read_first>
  <behavior>
    - Opening the dialog starts a 5-second countdown and auto-confirms if untouched.
    - User can select a different site and confirm that change.
    - User can dismiss/choose unassigned without blocking surrounding form actions.
  </behavior>
  <action>Implement `useAutoAssignmentConfirmation` with controlled state (`isOpen`, `secondsRemaining`, `draft`, `decision`) and idempotent finalize handlers; enforce `5000ms` timeout and cleanup on close/unmount. Implement `AutoAssignmentConfirmDialog` as a controlled component that renders suggestion text, site selector callback, countdown indicator, and actions for confirm/change/unassign. Keep it non-blocking by exposing `onOpenChange` + `onDecision` without trapping parent submit flow.</action>
  <acceptance_criteria>
    - Hook file exports `useAutoAssignmentConfirmation`
    - Hook contains explicit 5-second timer (`5000` or equivalent countdown ticks)
    - Dialog file exports `AutoAssignmentConfirmDialog`
    - Dialog exposes callbacks for confirm, change, and unassign outcomes
    - Dialog component does not require modal-blocking continuation from parent
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- --run</automated>
  </verify>
  <done>Reusable dialog/hook satisfy DLOG behavior and compile with existing frontend code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → auto-assignment decision | User-controlled site choice crosses into mutation payload building |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | T | useAutoAssignmentConfirmation | mitigate | Restrict decision union to explicit branches and validate required siteId for `change` before emit |
| T-24-02 | R | AutoAssignmentConfirmDialog | accept | Low-risk UI-only repudiation; server-side audit remains source of truth |
| T-24-03 | D | timer logic | mitigate | Clear timeout on unmount/close to prevent stale auto-submit bursts |
</threat_model>

<verification>
- `cd frontend && npm run typecheck`
- `cd frontend && npm run test -- --run`
</verification>

<success_criteria>
- Shared auto-assignment confirmation hook/component are available and compile
- 5-second auto-confirm path is encoded in shared logic
- Decision contract supports confirm, change, and unassign
</success_criteria>

<output>
After completion, create `.planning/phases/24-opt-out-dialog-e2e-tests/24-01-SUMMARY.md`
</output>
