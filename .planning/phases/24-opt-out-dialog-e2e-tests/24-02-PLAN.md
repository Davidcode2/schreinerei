---
phase: 24-opt-out-dialog-e2e-tests
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - frontend/src/pages/inventory/WithdrawDialog.tsx
  - frontend/src/pages/fleet/ReservationDialog.tsx
  - frontend/src/pages/sites/TimeEntryDialog.tsx
autonomous: true
requirements:
  - DLOG-01
  - DLOG-02
  - DLOG-03
  - DLOG-04
  - DLOG-05
must_haves:
  truths:
    - Auto-assignment dialog appears when suggested site is about to be applied
    - User can change site or dismiss to submit unassigned directly from each flow
    - Dialog does not block other form interactions
  artifacts:
    - path: frontend/src/pages/inventory/WithdrawDialog.tsx
      provides: Withdrawal flow integrated with confirmation decisions
    - path: frontend/src/pages/fleet/ReservationDialog.tsx
      provides: Reservation flow integrated with confirmation decisions
    - path: frontend/src/pages/sites/TimeEntryDialog.tsx
      provides: Time-entry flow integrated with confirmation decisions
  key_links:
    - from: WithdrawDialog
      to: useWithdrawMaterial mutation payload
      via: decision-mapped site_id
      pattern: "site_id"
    - from: ReservationDialog
      to: useCreateReservation mutation payload
      via: decision-mapped site_id
      pattern: "site_id"
    - from: TimeEntryDialog
      to: useCreateTimeEntry mutation payload
      via: decision-mapped site_id
      pattern: "site_id"
---

<objective>
Integrate the shared auto-assignment confirmation behavior into material withdrawal, reservation, and time-entry flows.

Purpose: Deliver the end-user DLOG functionality in all auto-assignment touchpoints.
Output: Three dialogs use the same decision flow and map result to submitted `site_id`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-opt-out-dialog-e2e-tests/24-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire confirmation flow into WithdrawDialog</name>
  <files>frontend/src/pages/inventory/WithdrawDialog.tsx</files>
  <read_first>
    - frontend/src/pages/inventory/WithdrawDialog.tsx
    - frontend/src/lib/api/hooks/useInventory.ts
    - frontend/src/hooks/useAutoAssignmentConfirmation.ts
    - frontend/src/components/shared/AutoAssignmentConfirmDialog.tsx
  </read_first>
  <behavior>
    - Suggested site opens confirmation dialog before request payload finalization.
    - Auto-confirm after 5 seconds submits with suggested `site_id`.
    - Unassign path submits without `site_id`.
  </behavior>
  <action>Extend WithdrawDialog props/state to accept an optional suggested active site context and route submission through `useAutoAssignmentConfirmation`. Resolve final payload `site_id` based on decision (`confirm` = suggested, `change` = selected, `unassign` = omitted/null). Render `AutoAssignmentConfirmDialog` alongside existing dialog and keep main dialog interactive while confirmation is visible.</action>
  <acceptance_criteria>
    - `WithdrawDialog.tsx` imports `useAutoAssignmentConfirmation`
    - `WithdrawDialog.tsx` renders `AutoAssignmentConfirmDialog`
    - Withdraw payload mapping contains explicit branches for confirm/change/unassign
    - Submission path can omit `site_id` for unassign
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run typecheck</automated>
  </verify>
  <done>Withdraw flow supports dialog confirmation without blocking and maps decisions to payload.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire confirmation flow into ReservationDialog and TimeEntryDialog</name>
  <files>frontend/src/pages/fleet/ReservationDialog.tsx, frontend/src/pages/sites/TimeEntryDialog.tsx</files>
  <read_first>
    - frontend/src/pages/fleet/ReservationDialog.tsx
    - frontend/src/pages/sites/TimeEntryDialog.tsx
    - frontend/src/lib/api/hooks/useFleet.ts
    - frontend/src/lib/api/hooks/useSites.ts
    - frontend/src/hooks/useAutoAssignmentConfirmation.ts
    - frontend/src/components/shared/AutoAssignmentConfirmDialog.tsx
  </read_first>
  <behavior>
    - Reservation creation flow applies decision-mapped site assignment.
    - Time-entry create flow applies decision-mapped site assignment for `work_type=site`.
    - Dismiss/unassign keeps create flows functional and non-blocking.
  </behavior>
  <action>In ReservationDialog and TimeEntryDialog create-mode submission paths, route default/suggested assignment through shared confirmation hook and component. For reservation/time entry payload builders, replace ad-hoc `site_id` inclusion with decision-driven mapping. Preserve edit-mode semantics (no new confirmation for edit updates unless assignment suggestion logic applies explicitly to create mode).</action>
  <acceptance_criteria>
    - `ReservationDialog.tsx` imports and renders shared confirmation dialog
    - `TimeEntryDialog.tsx` imports and renders shared confirmation dialog
    - Create payload builders in both files include explicit decision mapping branches
    - Edit-mode update behavior remains unchanged
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- --run</automated>
  </verify>
  <done>Reservation and time-entry flows match DLOG behavior with shared implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dialog decision → API mutation payload | UI decision controls whether `site_id` is sent to backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | S | form submission handlers | mitigate | Use authenticated existing mutations only; no direct unauthenticated requests |
| T-24-05 | T | payload mapping in dialogs | mitigate | Centralize decision->payload mapping with explicit branches; avoid implicit fallbacks |
| T-24-06 | D | dual-dialog interaction | mitigate | Keep confirmation non-blocking and prevent duplicate mutation calls with pending guards |
</threat_model>

<verification>
- `cd frontend && npm run typecheck`
- `cd frontend && npm run test -- --run`
</verification>

<success_criteria>
- All three create flows can confirm/change/unassign assignment
- 5-second auto-confirm applies suggested assignment in each flow
- Flows remain usable while dialog is present (non-blocking)
</success_criteria>

<output>
After completion, create `.planning/phases/24-opt-out-dialog-e2e-tests/24-02-SUMMARY.md`
</output>
