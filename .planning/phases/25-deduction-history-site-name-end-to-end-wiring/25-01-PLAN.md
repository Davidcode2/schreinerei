---
phase: 25-deduction-history-site-name-end-to-end-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/inventory.ts
  - frontend/src/lib/api/hooks/useInventory.ts
  - frontend/src/pages/inventory/InventoryDetailPage.tsx
  - frontend/src/pages/inventory/InventoryDetailPage.test.tsx
autonomous: true
requirements:
  - DEDU-03
must_haves:
  truths:
    - "User can view stock deduction history on inventory detail page"
    - "Deduction history shows Baustelle name when backend provides site_name"
    - "Entries without linked Baustelle render without site label and do not crash"
  artifacts:
    - path: "frontend/src/lib/api/hooks/useInventory.ts"
      provides: "React Query hook to fetch material stock history"
    - path: "frontend/src/pages/inventory/InventoryDetailPage.tsx"
      provides: "Rendered history UI consuming site_name"
    - path: "frontend/src/pages/inventory/InventoryDetailPage.test.tsx"
      provides: "Automated regression coverage for site_name render path"
  key_links:
    - from: "frontend/src/lib/api/hooks/useInventory.ts"
      to: "/api/v1/inventory/materials/{id}/history"
      via: "apiClient.get"
      pattern: "materials/.*/history"
    - from: "frontend/src/pages/inventory/InventoryDetailPage.tsx"
      to: "useMaterialHistory"
      via: "hook consumption + rendered history list"
      pattern: "useMaterialHistory"
---

<objective>
Close DEDU-03 by wiring frontend consumption and rendering of material history `site_name` end-to-end.

Purpose: Eliminate the milestone audit integration gap where backend produces `site_name` but frontend does not consume it.
Output: Inventory history hook, detail-page history UI, and regression tests proving `site_name` render behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-deduction-history-site-name-end-to-end-wiring/25-RESEARCH.md
@.planning/phases/22-backend-foundation-user-preferences/22-04-SUMMARY.md
@frontend/src/lib/api/hooks/useInventory.ts
@frontend/src/pages/inventory/InventoryDetailPage.tsx
@frontend/src/pages/fleet/ReservationsList.tsx

<interfaces>
From `frontend/src/types/generated.ts`:
```typescript
export type StockEntryResponse = {
  id: string
  quantity_change: number
  quantity_after: number
  notes: string | null
  site_id: string | null
  site_name: string | null
  created_at: string
}
```

From `frontend/src/lib/api/client.ts` usage pattern:
```typescript
apiClient.get<T>(path: string): Promise<T>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed material history consumer hook for DEDU-03</name>
  <read_first>
    - frontend/src/lib/api/hooks/useInventory.ts
    - frontend/src/types/inventory.ts
    - frontend/src/types/generated.ts
  </read_first>
  <files>frontend/src/types/inventory.ts, frontend/src/lib/api/hooks/useInventory.ts</files>
  <behavior>
    - Test 1: `useMaterialHistory(id)` issues GET to `/api/v1/inventory/materials/{id}/history` with query key `['material-history', id]`.
    - Test 2: Hook is disabled when `id` is empty/falsy.
  </behavior>
  <action>Add `MaterialStockHistoryEntry` interface in `frontend/src/types/inventory.ts` with fields `id`, `quantity_change`, `quantity_after`, `notes`, `site_id`, `site_name`, `created_at` (all matching backend response types), then add `useMaterialHistory(id: string)` in `frontend/src/lib/api/hooks/useInventory.ts` using `useQuery`, `queryKey: ['material-history', id]`, `queryFn: () => apiClient.get<MaterialStockHistoryEntry[]>(`/api/v1/inventory/materials/${id}/history`)`, `enabled: !!id`, `staleTime: 30000`.</action>
  <acceptance_criteria>
    - `frontend/src/types/inventory.ts` contains `export interface MaterialStockHistoryEntry` with `site_name: string | null`.
    - `frontend/src/lib/api/hooks/useInventory.ts` contains `export function useMaterialHistory(`.
    - Hook fetch path is exactly `/api/v1/inventory/materials/${id}/history`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- --run</automated>
  </verify>
  <done>Frontend has a reusable, typed API hook consuming backend history rows including `site_name`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render deduction history with conditional site name in inventory detail</name>
  <read_first>
    - frontend/src/pages/inventory/InventoryDetailPage.tsx
    - frontend/src/pages/fleet/ReservationsList.tsx
    - frontend/src/lib/api/hooks/useInventory.ts
  </read_first>
  <files>frontend/src/pages/inventory/InventoryDetailPage.tsx</files>
  <behavior>
    - Test 1: History section shows entries from `useMaterialHistory`.
    - Test 2: Entry with non-null `site_name` renders site label text.
    - Test 3: Entry with `site_name: null` renders without site label and page stays stable.
  </behavior>
  <action>Update `InventoryDetailPage` to call `useMaterialHistory(id!)`, render a new "Historie" card below existing sections, and map over history entries. For each row show timestamp (`created_at` formatted to `de-DE`), quantity delta (`quantity_change`), optional notes, and conditional site row using `MapPin` icon only when `site_name` exists. Render explicit empty state text (e.g. "Noch keine Entnahmen erfasst") when history array is empty.</action>
  <acceptance_criteria>
    - `InventoryDetailPage.tsx` imports and uses `useMaterialHistory`.
    - File contains a "Historie" section with mapped history entries.
    - Conditional check exists for `entry.site_name` before rendering site label.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- --run</automated>
  </verify>
  <done>Inventory detail page visibly consumes backend history data and renders `site_name` when linked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add regression test for site_name end-to-end render path</name>
  <read_first>
    - frontend/src/pages/inventory/AddMaterialDialog.test.tsx
    - frontend/src/test/mocks/handlers.ts
    - frontend/src/pages/inventory/InventoryDetailPage.tsx
  </read_first>
  <files>frontend/src/pages/inventory/InventoryDetailPage.test.tsx</files>
  <behavior>
    - Test 1: History row with `site_name: 'Baustelle Müller'` renders that site text.
    - Test 2: History row with `site_name: null` does not render map-pin/site text placeholder.
    - Test 3: Empty history response shows empty-state copy.
  </behavior>
  <action>Create `InventoryDetailPage.test.tsx` with MSW-backed tests that mock `GET /api/v1/inventory/materials/:id`, `GET /api/v1/preferences`, `GET /api/v1/sites`, and `GET /api/v1/inventory/materials/:id/history`. Verify the page renders history rows and `site_name` conditionally per DEDU-03. Keep tests deterministic by using fixed ISO timestamps and explicit expected strings.</action>
  <acceptance_criteria>
    - New test file exists and contains at least one assertion for visible `site_name` text.
    - New test file contains assertion for non-render when `site_name` is `null`.
    - Test command exits with code 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- src/pages/inventory/InventoryDetailPage.test.tsx --run</automated>
  </verify>
  <done>Automated regression coverage prevents reintroduction of the DEDU-03 frontend consumer gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → inventory history API | Untrusted response data is rendered in frontend UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | T (Tampering) | `InventoryDetailPage.tsx` rendered notes/site text | mitigate | Render backend strings as plain React text nodes only (no HTML injection, no `dangerouslySetInnerHTML`) |
| T-25-02 | I (Information Disclosure) | history site labels in UI | accept | Data is already tenant-scoped by backend; this phase adds only frontend consumption of existing authorized response |
| T-25-03 | D (Denial of Service) | history list rendering | mitigate | Keep backend list bounded (existing last-50 behavior) and render simple mapped rows without expensive client transforms |
</threat_model>

<verification>
- `cd frontend && npm run test -- src/pages/inventory/InventoryDetailPage.test.tsx --run`
- `cd frontend && npm run test -- --run`
- Manual smoke: open `/inventory/:id` and confirm "Historie" shows site name for linked withdrawals.
</verification>

<success_criteria>
- DEDU-03 requirement is fulfilled end-to-end on frontend.
- Inventory detail page consumes `/api/v1/inventory/materials/{id}/history`.
- `site_name` is visible for linked entries and omitted for null entries.
- Regression test exists and passes for both site-linked and site-null scenarios.
</success_criteria>

<output>
After completion, create `.planning/phases/25-deduction-history-site-name-end-to-end-wiring/25-01-SUMMARY.md`
</output>
