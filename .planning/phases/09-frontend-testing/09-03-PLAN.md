---
phase: 09-frontend-testing
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - frontend/tests/BUGS.md
  - frontend/tests/test-report/
autonomous: false
requirements: [TEST-06]
must_haves:
  truths:
    - All tests have been executed
    - Bugs discovered are documented
    - Test report is generated
    - Screenshots/videos captured for failures
  artifacts:
    - path: frontend/tests/BUGS.md
      provides: Bug documentation
      min_lines: 10
    - path: frontend/playwright-report/index.html
      provides: Test report
  key_links:
    - from: playwright-report
      to: Test failures
      via: Screenshots and traces
---

<objective>
Run comprehensive E2E test suite and document all discovered bugs.

Purpose: Execute all tests to discover remaining issues and create actionable bug report.
Output: Test results, bug documentation, and test report.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-frontend-testing/09-CONTEXT.md

# Known pending issues from STATE.md:
- Fix fleet page "Neu" button
- Fix baustelle time booking 400 error
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Full Test Suite</name>
  <files>frontend/playwright-report/</files>
  <read_first>
    - frontend/tests/auth.spec.ts
    - frontend/tests/inventory.spec.ts
    - frontend/tests/fleet.spec.ts
    - frontend/tests/sites.spec.ts
    - frontend/tests/dashboard.spec.ts
  </read_first>
  <behavior>
    - Test suite runs to completion
    - Report is generated
    - Failures are captured with screenshots
  </behavior>
  <action>
1. Ensure backend is running:
   ```bash
   # Check if backend is accessible
   curl -s http://localhost:3000/health || echo "Backend not running"
   ```

2. Ensure Keycloak is accessible:
   ```bash
   curl -s https://auth.jakob-lingel.dev || curl -s http://localhost:8080 || echo "Keycloak not accessible"
   ```

3. Run Playwright tests:
   ```bash
   cd frontend
   npx playwright test --reporter=html
   ```

4. If tests fail, capture output:
   ```bash
   npx playwright test --reporter=html --trace on 2>&1 | tee test-output.log
   ```

5. Open report for review:
   ```bash
   npx playwright show-report
   ```
</action>
  <verify>
    <automated>cd frontend && test -d playwright-report && test -f playwright-report/index.html</automated>
  </verify>
  <done>
    - playwright-report/index.html exists
    - Test results captured (pass/fail counts)
    - Screenshots/traces saved for failures
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Playwright test suite has been executed. Test report generated at `frontend/playwright-report/index.html`.

The report contains:
- Test pass/fail status for each test
- Screenshots and traces for failed tests
- Duration and timing information
  </what-built>
  <how-to-verify>
1. Open the test report:
   ```bash
   cd frontend && npx playwright show-report
   ```
   Or open `frontend/playwright-report/index.html` in browser.

2. Review each failed test:
   - Click on failed test to see screenshot
   - View trace to understand what happened
   - Note the error message

3. Check console output for API errors:
   - Network tab in trace viewer
   - Console messages in test details

4. List of tests that should pass (if implementation is correct):
   - auth.spec.ts: login, logout
   - inventory.spec.ts: navigation, material creation
   - fleet.spec.ts: navigation, vehicle creation
   - sites.spec.ts: navigation, site creation
   - dashboard.spec.ts: load, sites overview
  </how-to-verify>
  <resume-signal>Type "approved" if tests pass, or describe the bugs found</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Document Discovered Bugs</name>
  <files>frontend/tests/BUGS.md</files>
  <read_first>
    - frontend/playwright-report/index.html (test results)
    - frontend/test-output.log (if exists)
  </read_first>
  <behavior>
    - Each bug has clear reproduction steps
    - Each bug has expected vs actual behavior
    - Each bug has severity assessment
  </behavior>
  <action>
Create `frontend/tests/BUGS.md` documenting all discovered issues:

```markdown
# Bug Report - Phase 9 Testing

**Date:** [current date]
**Tester:** Playwright E2E Tests
**Environment:** localhost:5173 → localhost:3000 API

## Summary

| Bug ID | Severity | Test | Status |
|--------|----------|------|--------|
| BUG-01 | High/Medium/Low | test-name | Open/Fixed |

---

## BUG-01: [Bug Title]

**Test:** `tests/xyz.spec.ts - test name`

**Severity:** High | Medium | Low

**Steps to Reproduce:**
1. Navigate to /page
2. Click button
3. Observe error

**Expected:** Should X
**Actual:** Does Y

**Error Message:**
\`\`\`
[error from test output]
\`\`\`

**Screenshot:** `playwright-report/...png`

**Console Errors:**
- [list any console errors]

---

## Additional Notes

[Any patterns observed, potential causes, or recommendations]
```

For each failed test:
1. Create a BUG entry
2. Copy the error message
3. Note the test file and test name
4. Assess severity:
   - High: Blocks core functionality
   - Medium: Feature broken but workaround exists
   - Low: Minor issue or edge case
</action>
  <verify>
    <automated>cd frontend && test -f tests/BUGS.md && grep -c "BUG-" tests/BUGS.md | xargs -I {} test {} -ge 0</automated>
  </verify>
  <done>
    - tests/BUGS.md exists
    - Each failed test has a BUG entry
    - Severity assigned to each bug
    - Reproduction steps documented
  </done>
</task>

<task type="auto">
  <name>Task 4: Create Test Summary</name>
  <files>frontend/tests/TEST-SUMMARY.md</files>
  <read_first>
    - frontend/playwright-report/index.html
    - frontend/tests/BUGS.md
  </read_first>
  <behavior>
    - Summary shows total tests run
    - Summary shows pass/fail counts
    - Summary lists bugs discovered
  </behavior>
  <action>
Create `frontend/tests/TEST-SUMMARY.md`:

```markdown
# E2E Test Summary - Phase 9

**Date:** [current date]
**Duration:** [from test run]

## Results Overview

| Suite | Tests | Passed | Failed | Skipped |
|-------|-------|--------|--------|---------|
| auth | X | X | X | X |
| inventory | X | X | X | X |
| fleet | X | X | X | X |
| sites | X | X | X | X |
| dashboard | X | X | X | X |
| **Total** | **X** | **X** | **X** | **X** |

## Pass Rate

X% of tests passing

## Discovered Bugs

See [BUGS.md](./BUGS.md) for details.

| Bug ID | Description | Severity |
|--------|-------------|----------|
| BUG-01 | ... | High/Medium/Low |

## Navigation Patterns Recorded

The following user flows were tested:
1. Login → Dashboard
2. Dashboard → Inventory → Add Material
3. Dashboard → Fleet → Add Vehicle
4. Dashboard → Sites → Add Site

## Recommendations

1. [Fix BUG-01 before next release]
2. [Add more edge case tests]
3. [Consider CI integration]

## Next Steps

- [ ] Fix discovered bugs
- [ ] Add more test coverage
- [ ] Set up CI/CD for tests
```

Extract pass/fail counts from test output or report.
</action>
  <verify>
    <automated>cd frontend && test -f tests/TEST-SUMMARY.md && grep -q "Results Overview" tests/TEST-SUMMARY.md</automated>
  </verify>
  <done>
    - tests/TEST-SUMMARY.md exists
    - Results table populated
    - Bugs summarized
    - Recommendations provided
  </done>
</task>

</tasks>

<verification>
1. All tests executed
2. Test report generated
3. Bugs documented
4. Test summary created
</verification>

<success_criteria>
- playwright-report/index.html exists
- tests/BUGS.md exists with bug entries
- tests/TEST-SUMMARY.md exists with results
- User has reviewed test results
</success_criteria>

<output>
After completion, create `.planning/phases/09-frontend-testing/09-03-SUMMARY.md`
</output>
