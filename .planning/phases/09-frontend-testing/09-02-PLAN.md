---
phase: 09-frontend-testing
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - frontend/tests/fleet.spec.ts
  - frontend/tests/sites.spec.ts
  - frontend/tests/dashboard.spec.ts
autonomous: true
requirements: [TEST-03, TEST-04, TEST-05]
must_haves:
  truths:
    - Fleet vehicles can be created via test
    - Fleet tools can be created via test
    - Sites can be created via test
    - Dashboard loads without errors
  artifacts:
    - path: frontend/tests/fleet.spec.ts
      provides: Fleet flow tests
      exports: ["test"]
    - path: frontend/tests/sites.spec.ts
      provides: Sites flow tests
      exports: ["test"]
    - path: frontend/tests/dashboard.spec.ts
      provides: Dashboard tests
      exports: ["test"]
  key_links:
    - from: tests/fleet.spec.ts
      to: /api/v1/fleet
      via: API calls after auth
    - from: tests/sites.spec.ts
      to: /api/v1/sites
      via: API calls after auth
---

<objective>
Write E2E tests for fleet, sites, and dashboard flows.

Purpose: Validate fleet management, site management, and dashboard functionality work end-to-end.
Output: Working tests for fleet, sites, and dashboard.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-frontend-testing/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Fleet Management Tests</name>
  <files>frontend/tests/fleet.spec.ts</files>
  <read_first>
    - frontend/src/pages/fleet/FleetPage.tsx (fleet page)
    - frontend/src/pages/fleet/AddVehicleDialog.tsx (add vehicle form)
    - frontend/src/lib/api/hooks/useFleet.ts (fleet API hooks)
    - frontend/src/types/fleet.ts (fleet types including VehicleType enum)
  </read_first>
  <behavior>
    - Test 1: Can navigate to fleet page
    - Test 2: Can create a new vehicle
    - Test 3: Can create a new tool
    - Test 4: Can view fleet list
    - Test 5: Reservation dialog opens (date format was fixed in Phase 8)
  </behavior>
  <action>
Create `frontend/tests/fleet.spec.ts`:

```typescript
import { test, expect } from '@playwright/test';
import { login } from './helpers/auth';

test.describe('Fleet Management', () => {
  test.beforeEach(async ({ page }) => {
    await login(page);
  });

  test('should navigate to fleet page', async ({ page }) => {
    await page.click('a[href*="fuhrpark"], a:has-text("Fuhrpark")');
    await expect(page).toHaveURL(/fuhrpark|fleet/);
  });

  test('should create a new vehicle', async ({ page }) => {
    await page.goto('/fuhrpark');
    
    // Click add vehicle button
    await page.click('button:has-text("Neu"), button:has-text("Hinzufügen")');
    await page.waitForSelector('[role="dialog"]');
    
    const dialog = page.locator('[role="dialog"]');
    
    // Fill vehicle name
    const nameInput = dialog.locator('input[name="name"], input[placeholder*="Name"]').first();
    await nameInput.fill(`Test Vehicle ${Date.now()}`);
    
    // Fill license plate
    const plateInput = dialog.locator('input[name="license_plate"], input[placeholder*="Kennzeichen"]').first();
    await plateInput.fill('B-TEST 123');
    
    // Select vehicle type (van, car, truck, trailer, other)
    const typeSelect = dialog.locator('select[name="vehicle_type"], button:has-text("Typ")').first();
    if (await typeSelect.isVisible()) {
      await typeSelect.click();
      await page.keyboard.press('ArrowDown');
      await page.keyboard.press('Enter');
    }
    
    // Submit
    await dialog.locator('button[type="submit"], button:has-text("Speichern")').click();
    
    // Wait for dialog to close
    await expect(dialog).not.toBeVisible({ timeout: 5000 });
  });

  test('should create a new tool', async ({ page }) => {
    await page.goto('/fuhrpark');
    
    // Look for tabs or section for tools
    const toolsTab = page.locator('button:has-text("Werkzeuge"), [role="tab"]:has-text("Tools")');
    if (await toolsTab.isVisible()) {
      await toolsTab.click();
    }
    
    // Click add tool button
    await page.click('button:has-text("Neu"), button:has-text("Hinzufügen")');
    await page.waitForSelector('[role="dialog"]');
    
    const dialog = page.locator('[role="dialog"]');
    
    // Fill tool name
    const nameInput = dialog.locator('input[name="name"], input[placeholder*="Name"]').first();
    await nameInput.fill(`Test Tool ${Date.now()}`);
    
    // Submit
    await dialog.locator('button[type="submit"], button:has-text("Speichern")').click();
    
    await expect(dialog).not.toBeVisible({ timeout: 5000 });
  });

  test('should display fleet items', async ({ page }) => {
    await page.goto('/fuhrpark');
    
    // Wait for content
    await page.waitForSelector('table, [data-testid="fleet-list"], .vehicle-card', { timeout: 5000 });
    
    const content = await page.locator('main').textContent();
    expect(content?.length).toBeGreaterThan(0);
  });

  test('should open reservation dialog', async ({ page }) => {
    await page.goto('/fuhrpark');
    await page.waitForSelector('table, [data-testid="fleet-list"]', { timeout: 5000 });
    
    // Look for calendar or reserve button
    const reserveButton = page.locator('button:has-text("Reservieren"), button:has-text("Kalender")').first();
    if (await reserveButton.isVisible()) {
      await reserveButton.click();
      await page.waitForSelector('[role="dialog"]', { timeout: 3000 });
      const dialog = page.locator('[role="dialog"]');
      await expect(dialog).toBeVisible();
    }
  });
});
```
</action>
  <verify>
    <automated>cd frontend && test -f tests/fleet.spec.ts && grep -q "should create a new vehicle" tests/fleet.spec.ts && grep -q "should create a new tool" tests/fleet.spec.ts</automated>
  </verify>
  <done>
    - tests/fleet.spec.ts exists with 5 test cases
    - Tests cover: navigation, vehicle creation, tool creation, list display, reservation dialog
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Sites Management Tests</name>
  <files>frontend/tests/sites.spec.ts</files>
  <read_first>
    - frontend/src/pages/sites/SitesPage.tsx (sites page)
    - frontend/src/pages/sites/AddSiteDialog.tsx (add site form)
    - frontend/src/lib/api/hooks/useSites.ts (sites API hooks)
    - frontend/src/types/sites.ts (site types)
  </read_first>
  <behavior>
    - Test 1: Can navigate to sites page
    - Test 2: Can create a new site
    - Test 3: Can view sites list
    - Test 4: Time booking works (check for 400 errors)
  </behavior>
  <action>
Create `frontend/tests/sites.spec.ts`:

```typescript
import { test, expect } from '@playwright/test';
import { login } from './helpers/auth';

test.describe('Sites Management', () => {
  test.beforeEach(async ({ page }) => {
    await login(page);
  });

  test('should navigate to sites page', async ({ page }) => {
    await page.click('a[href*="baustellen"], a:has-text("Baustellen")');
    await expect(page).toHaveURL(/baustellen|sites/);
  });

  test('should create a new site', async ({ page }) => {
    await page.goto('/baustellen');
    
    // Click add site button
    await page.click('button:has-text("Neu"), button:has-text("Hinzufügen")');
    await page.waitForSelector('[role="dialog"]');
    
    const dialog = page.locator('[role="dialog"]');
    
    // Fill site name
    const nameInput = dialog.locator('input[name="name"], input[placeholder*="Name"]').first();
    await nameInput.fill(`Test Site ${Date.now()}`);
    
    // Fill customer
    const customerInput = dialog.locator('input[name="customer"], input[placeholder*="Kunde"]').first();
    if (await customerInput.isVisible()) {
      await customerInput.fill('Test Customer');
    }
    
    // Fill location
    const locationInput = dialog.locator('input[name="location"], input[placeholder*="Ort"]').first();
    if (await locationInput.isVisible()) {
      await locationInput.fill('Berlin');
    }
    
    // Submit
    await dialog.locator('button[type="submit"], button:has-text("Speichern")').click();
    
    await expect(dialog).not.toBeVisible({ timeout: 5000 });
  });

  test('should display sites list', async ({ page }) => {
    await page.goto('/baustellen');
    
    await page.waitForSelector('table, [data-testid="sites-list"], .site-card', { timeout: 5000 });
    
    const content = await page.locator('main').textContent();
    expect(content?.length).toBeGreaterThan(0);
  });

  test('should handle time booking without errors', async ({ page }) => {
    await page.goto('/baustellen');
    await page.waitForSelector('table, [data-testid="sites-list"]', { timeout: 5000 });
    
    // Look for time booking button
    const timeButton = page.locator('button:has-text("Zeit"), button:has-text("Buchen")').first();
    if (await timeButton.isVisible()) {
      await timeButton.click();
      await page.waitForSelector('[role="dialog"]', { timeout: 3000 });
      
      const dialog = page.locator('[role="dialog"]');
      await expect(dialog).toBeVisible();
      
      // Check for error messages
      const errorAlert = dialog.locator('[role="alert"], .error');
      await expect(errorAlert).not.toBeVisible();
    }
  });
});
```
</action>
  <verify>
    <automated>cd frontend && test -f tests/sites.spec.ts && grep -q "should create a new site" tests/sites.spec.ts && grep -q "should display sites" tests/sites.spec.ts</automated>
  </verify>
  <done>
    - tests/sites.spec.ts exists with 4 test cases
    - Tests cover: navigation, site creation, list display, time booking
  </done>
</task>

<task type="auto">
  <name>Task 3: Write Dashboard Tests</name>
  <files>frontend/tests/dashboard.spec.ts</files>
  <read_first>
    - frontend/src/pages/DashboardPage.tsx (dashboard page)
    - frontend/src/lib/api/hooks/useDashboard.ts (dashboard API hooks)
  </read_first>
  <behavior>
    - Test 1: Dashboard loads after login
    - Test 2: Dashboard shows sites overview (API was fixed in Phase 8)
    - Test 3: No console errors on dashboard load
  </behavior>
  <action>
Create `frontend/tests/dashboard.spec.ts`:

```typescript
import { test, expect } from '@playwright/test';
import { login } from './helpers/auth';

test.describe('Dashboard', () => {
  test.beforeEach(async ({ page }) => {
    await login(page);
  });

  test('should load dashboard after login', async ({ page }) => {
    // Dashboard should be default after login
    await page.waitForSelector('main', { timeout: 5000 });
    
    // Check for dashboard content
    const content = await page.locator('main').textContent();
    expect(content?.length).toBeGreaterThan(0);
  });

  test('should display sites overview', async ({ page }) => {
    await page.goto('/');
    await page.waitForSelector('main', { timeout: 5000 });
    
    // Look for sites section or stats
    const sitesSection = page.locator('[data-testid="sites-overview"], h2:has-text("Baustellen"), h2:has-text("Sites")');
    
    // Sites overview should exist (API fixed in Phase 8)
    await expect(sitesSection.first()).toBeVisible({ timeout: 5000 });
  });

  test('should not have console errors', async ({ page }) => {
    const errors: string[] = [];
    
    page.on('console', msg => {
      if (msg.type() === 'error') {
        errors.push(msg.text());
      }
    });
    
    page.on('pageerror', err => {
      errors.push(err.message);
    });
    
    await page.goto('/');
    await page.waitForSelector('main', { timeout: 5000 });
    
    // Wait for any async operations
    await page.waitForTimeout(2000);
    
    // Filter out known non-critical errors (e.g., browser extension errors)
    const criticalErrors = errors.filter(e => 
      !e.includes('extension') && 
      !e.includes('favicon') &&
      !e.includes('manifest')
    );
    
    expect(criticalErrors).toHaveLength(0);
  });
});
```
</action>
  <verify>
    <automated>cd frontend && test -f tests/dashboard.spec.ts && grep -q "should load dashboard" tests/dashboard.spec.ts && grep -q "console errors" tests/dashboard.spec.ts</automated>
  </verify>
  <done>
    - tests/dashboard.spec.ts exists with 3 test cases
    - Tests cover: dashboard load, sites overview, console error detection
  </done>
</task>

</tasks>

<verification>
1. Fleet tests written with vehicle/tool creation
2. Sites tests written with site creation
3. Dashboard tests written with error detection
4. All test files can be listed
</verification>

<success_criteria>
- tests/fleet.spec.ts exists with 4+ test cases
- tests/sites.spec.ts exists with 3+ test cases
- tests/dashboard.spec.ts exists with 3 test cases
- npx playwright test --list shows all tests
</success_criteria>

<output>
After completion, create `.planning/phases/09-frontend-testing/09-02-SUMMARY.md`
</output>
