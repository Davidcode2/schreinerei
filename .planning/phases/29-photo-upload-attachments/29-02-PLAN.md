---
phase: 29-photo-upload-attachments
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - Cargo.toml
  - src/modules/sites/api/routes.rs
  - src/modules/sites/application/site_service.rs
  - src/modules/sites/infrastructure/site_repository.rs
autonomous: true
requirements:
  - FILE-01
  - FILE-03
  - FILE-04
  - FILE-07
must_haves:
  truths:
    - "User can upload a photo for a site activity"
    - "Stored filenames are UUID-based"
    - "Thumbnail is generated on upload"
  artifacts:
    - path: "src/modules/sites/api/routes.rs"
      provides: "Multipart upload endpoint and response contract"
    - path: "src/modules/sites/application/site_service.rs"
      provides: "Upload flow orchestration with validation"
    - path: "src/modules/sites/infrastructure/site_repository.rs"
      provides: "Attachment row creation and activity linkage"
  key_links:
    - from: "src/modules/sites/api/routes.rs"
      to: "src/modules/sites/application/site_service.rs"
      via: "multipart handler invokes upload service"
      pattern: "multipart"
    - from: "site_service.rs"
      to: "site_repository.rs"
      via: "persist attachment metadata + activity linkage"
      pattern: "create_attachment"
---

<objective>
Implement upload pipeline with UUID storage keys and thumbnail generation.

Purpose: Deliver secure photo ingestion that powers previewable activity attachments.
Output: Multipart upload endpoint, server-side thumbnail generation, and persisted attachment/activity linkage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-photo-upload-attachments/29-RESEARCH.md
@.planning/phases/29-photo-upload-attachments/29-01-PLAN.md
@src/modules/sites/api/routes.rs
@src/modules/sites/application/site_service.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add multipart upload route and DTO contracts</name>
  <files>src/modules/sites/api/routes.rs</files>
  <behavior>
    - Test 1: Upload endpoint accepts multipart image and returns attachment_id + photo_url + thumbnail_url.
    - Test 2: Invalid MIME or oversized payload returns validation error.
  </behavior>
  <action>Add `POST /api/v1/sites/{id}/attachments/photo` handler using Axum multipart extraction. Validate MIME allowlist (jpeg/png/webp) and enforce file size cap. Return a typed response DTO exported via `ts-rs` so frontend can consume canonical upload result.</action>
  <verify>
    <automated>cargo test sites --lib upload -- --nocapture</automated>
  </verify>
  <done>Upload endpoint contract exists with strict validation and TS-exported response.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement UUID storage + thumbnail generation flow</name>
  <files>Cargo.toml, src/modules/sites/application/site_service.rs, src/modules/sites/infrastructure/site_repository.rs</files>
  <behavior>
    - Test 1: Upload writes original and thumbnail with UUID keys.
    - Test 2: Thumbnail dimensions are bounded and valid image bytes are returned.
  </behavior>
  <action>Add image-processing dependency (e.g., `image`) and implement service flow: generate UUID keys, persist original bytes, generate thumbnail bytes, store thumbnail under separate UUID key, then persist attachment metadata row linked to site/activity and tenant. Ensure no user-provided filename is used in storage key generation (FILE-03).</action>
  <verify>
    <automated>cargo test sites --lib thumbnail -- --nocapture</automated>
  </verify>
  <done>Upload pipeline produces original + thumbnail assets with UUID-based keys and persisted metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Link uploaded attachment to photo activity feed records</name>
  <files>src/modules/sites/application/site_service.rs, src/modules/sites/infrastructure/site_repository.rs, src/modules/sites/api/routes.rs</files>
  <action>After successful upload, create or enrich associated photo activity record so feed consumes stable `photo_url`/thumbnail URLs immediately. Maintain tenant and site consistency checks before linking rows (per FILE-07).</action>
  <verify>
    <automated>cargo test sites --lib activity -- --nocapture</automated>
  </verify>
  <done>Uploaded photos are visible through activity API with attachment-backed URLs.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→upload API | Untrusted binary payload and metadata |
| upload service→storage | Raw bytes must be sanitized/validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | D | upload endpoint | mitigate | Enforce file size cap and MIME allowlist before processing. |
| T-29-05 | T | storage key generation | mitigate | Generate UUID keys server-side; ignore client filename. |
| T-29-06 | I | attachment-to-activity linkage | mitigate | Validate tenant/site ownership before linking rows. |
</threat_model>

<verification>
- `cargo test sites --lib`
</verification>

<success_criteria>
- Upload endpoint accepts valid camera/gallery images.
- Original + thumbnail are generated with UUID keys.
- Activity records reference upload outputs for feed preview.
</success_criteria>

<output>
After completion, create `.planning/phases/29-photo-upload-attachments/29-photo-upload-attachments-02-SUMMARY.md`
</output>
