---
phase: 29-photo-upload-attachments
plan: 03
type: execute
wave: 3
depends_on: [29-02]
files_modified:
  - frontend/src/types/sites.ts
  - frontend/src/lib/api/hooks/useSites.ts
  - frontend/src/pages/sites/ActivityFeed.tsx
  - frontend/src/pages/sites/CreateNoteModal.tsx
  - frontend/src/pages/sites/ActivityFeed.test.tsx
autonomous: true
requirements:
  - FILE-01
  - FILE-02
must_haves:
  truths:
    - "User can select or capture a photo when creating a photo activity"
    - "Activity feed shows uploaded image preview"
  artifacts:
    - path: "frontend/src/lib/api/hooks/useSites.ts"
      provides: "Upload mutation hook"
    - path: "frontend/src/pages/sites/ActivityFeed.tsx"
      provides: "Photo preview rendering with attachment URLs"
  key_links:
    - from: "CreateNoteModal.tsx"
      to: "useSites.ts"
      via: "photo upload mutation then create activity"
      pattern: "mutateAsync"
---

<objective>
Wire frontend photo capture/gallery selection and feed previews to the backend upload contract.

Purpose: Make photo attachments usable in day-to-day site activity entry.
Output: Upload-capable activity UI + API hooks + tests for preview behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-photo-upload-attachments/29-02-PLAN.md
@frontend/src/pages/sites/ActivityFeed.tsx
@frontend/src/lib/api/hooks/useSites.ts

<interfaces>
From `frontend/src/types/sites.ts`:
```ts
export interface CreateActivityRequest {
  activity_type: ActivityType
  content?: string
  photo_url?: string
}
```

From `frontend/src/lib/api/hooks/useSites.ts`:
```ts
export function useCreateActivity()
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add typed upload hook and attachment response contracts</name>
  <files>frontend/src/types/sites.ts, frontend/src/lib/api/hooks/useSites.ts</files>
  <behavior>
    - Test 1: Upload hook sends multipart payload and returns attachment metadata.
    - Test 2: Activity creation reuses returned photo URL without manual string assembly.
  </behavior>
  <action>Extend TypeScript contracts for upload response (`attachment_id`, `photo_url`, `thumbnail_url`) and implement React Query mutation for `POST /api/v1/sites/{id}/attachments/photo`. Keep existing `useCreateActivity` invalidation behavior.
  </action>
  <verify>
    <automated>npm run test:run -- useSites</automated>
  </verify>
  <done>Frontend has a typed upload mutation contract aligned with backend response.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement camera/gallery picker flow in activity creation UI</name>
  <files>frontend/src/pages/sites/CreateNoteModal.tsx, frontend/src/pages/sites/ActivityFeed.tsx</files>
  <behavior>
    - Test 1: User can choose image from camera or gallery input.
    - Test 2: Successful upload creates photo activity and preview appears in feed.
  </behavior>
  <action>Update activity creation UI to support image input (`accept="image/*"`, mobile capture hint), upload selected file first, then call create activity with returned URL. Preserve existing note creation path unchanged. Render thumbnail/photo preview in feed cards using attachment-backed URL.</action>
  <verify>
    <automated>npm run test:run -- ActivityFeed</automated>
  </verify>
  <done>Photo workflow works end-to-end in UI and previews are rendered in activity feed.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for upload and preview states</name>
  <files>frontend/src/pages/sites/ActivityFeed.test.tsx, frontend/src/lib/api/hooks/useSites.ts</files>
  <action>Add test coverage for image present/absent states, upload success wiring, and loading/error surface so FILE-02 stays protected in future changes.</action>
  <verify>
    <automated>npm run test:run -- ActivityFeed useSites</automated>
  </verify>
  <done>Frontend tests lock expected upload-to-preview behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→upload API | Untrusted selected file and metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-07 | T | upload request builder | mitigate | Submit as FormData file object only; no user-controlled path/key fields. |
| T-29-08 | R | UI upload flow | accept | No PII in UI logs; rely on backend audit for authoritative records. |
</threat_model>

<verification>
- `npm run test:run -- ActivityFeed useSites`
</verification>

<success_criteria>
- Users can choose/capture photos and upload from site activity UI.
- Activity feed renders uploaded image previews reliably.
</success_criteria>

<output>
After completion, create `.planning/phases/29-photo-upload-attachments/29-photo-upload-attachments-03-SUMMARY.md`
</output>
