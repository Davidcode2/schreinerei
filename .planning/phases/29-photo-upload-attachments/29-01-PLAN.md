---
phase: 29-photo-upload-attachments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/012_site_activity_attachments.sql
  - src/modules/sites/domain/activity.rs
  - src/modules/sites/infrastructure/site_repository.rs
  - src/modules/sites/api/routes.rs
autonomous: true
requirements:
  - FILE-03
  - FILE-07
must_haves:
  truths:
    - "Attachment records are tenant-scoped and linked to activities"
    - "Attachment URLs do not expose user-provided filenames"
    - "Attachment fetch is denied across tenants"
  artifacts:
    - path: "migrations/012_site_activity_attachments.sql"
      provides: "Tenant-scoped attachments table and indexes"
    - path: "src/modules/sites/infrastructure/site_repository.rs"
      provides: "Tenant-scoped attachment persistence + lookup"
    - path: "src/modules/sites/api/routes.rs"
      provides: "Authorized attachment read endpoints"
  key_links:
    - from: "src/modules/sites/api/routes.rs"
      to: "src/modules/sites/infrastructure/site_repository.rs"
      via: "attachment_id + tenant_id lookup"
      pattern: "tenant_id"
    - from: "migrations/012_site_activity_attachments.sql"
      to: "site_activities"
      via: "activity_id foreign key"
      pattern: "REFERENCES site_activities"
---

<objective>
Create the tenant-safe storage contract and read path for activity photo attachments.

Purpose: Establish secure data model and authorization boundary before implementing binary upload.
Output: Migration + domain/repository contracts + tenant-authorized attachment read endpoints.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-photo-upload-attachments/29-RESEARCH.md
@src/modules/sites/api/routes.rs
@src/modules/sites/infrastructure/site_repository.rs

<interfaces>
From `src/modules/sites/api/routes.rs`:
```rust
pub struct ActivityResponse {
    pub id: String,
    pub site_id: String,
    pub user_id: String,
    pub activity_type: String,
    pub content: Option<String>,
    pub photo_url: Option<String>,
    pub created_at: String,
}
```

From `src/modules/sites/domain/activity.rs`:
```rust
pub struct Activity {
    pub id: ActivityId,
    pub tenant_id: TenantId,
    pub site_id: SiteId,
    pub user_id: UserId,
    pub activity_type: ActivityType,
    pub content: Option<String>,
    pub photo_url: Option<String>,
    pub created_at: DateTime<Utc>,
}
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define attachment persistence contracts and migration</name>
  <files>migrations/012_site_activity_attachments.sql, src/modules/sites/domain/activity.rs, src/modules/sites/infrastructure/site_repository.rs</files>
  <behavior>
    - Test 1: Migration creates table with tenant_id, activity_id, site_id, storage_key, thumbnail_key and indexes.
    - Test 2: Repository insert/select signatures require tenant_id for all reads/writes.
  </behavior>
  <action>Create migration `012_site_activity_attachments.sql` adding tenant-scoped attachment table and indexes; enforce UUID PK and FK to `site_activities`. Add domain/repository structs for attachment metadata (including storage and thumbnail keys). Keep `photo_url` field for compatibility, but move canonical storage tracking to attachment rows.</action>
  <verify>
    <automated>cargo test sites --lib</automated>
  </verify>
  <done>Attachment schema and repository contracts exist with tenant-scoped ownership guarantees.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tenant-authorized attachment retrieval endpoints</name>
  <files>src/modules/sites/api/routes.rs, src/modules/sites/infrastructure/site_repository.rs</files>
  <behavior>
    - Test 1: Same-tenant request for attachment ID returns 200.
    - Test 2: Cross-tenant request for same attachment ID returns not found/forbidden.
  </behavior>
  <action>Add read endpoints for original and thumbnail attachment bytes keyed by attachment UUID. Resolve tenant from `TenantContext::from_auth` and perform repository lookup with `(attachment_id, tenant_id)` filter (per FILE-07). Do not expose storage keys in public route paths.</action>
  <verify>
    <automated>cargo test sites --lib -- --nocapture</automated>
  </verify>
  <done>Attachment fetch path enforces tenant authorization and hides raw storage keys.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→attachment API | Untrusted attachment IDs and auth token input |
| attachment API→database | Authorization must be enforced via tenant-scoped query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | I | attachment read endpoint | mitigate | Always filter by `attachment_id` + `tenant_id` in repository query. |
| T-29-02 | E | attachment read endpoint | mitigate | Derive tenant from JWT context only; never accept tenant from request params. |
| T-29-03 | T | attachment lookup query | mitigate | Use parameterized SQL binds for all identifiers. |
</threat_model>

<verification>
- `cargo test sites --lib`
</verification>

<success_criteria>
- Attachments table exists and is tenant-scoped.
- Attachment reads are authorized by tenant.
- No user filename is persisted as storage key.
</success_criteria>

<output>
After completion, create `.planning/phases/29-photo-upload-attachments/29-photo-upload-attachments-01-SUMMARY.md`
</output>
