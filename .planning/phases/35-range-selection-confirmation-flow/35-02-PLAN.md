---
phase: 35-range-selection-confirmation-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - frontend/src/pages/fleet/CalendarView.tsx
  - frontend/src/pages/fleet/ReservationConfirmationSheet.tsx
  - frontend/src/pages/fleet/CalendarView.test.tsx
  - frontend/src/pages/fleet/FleetPage.test.tsx
autonomous: true
requirements:
  - FSEL-01
  - FSEL-02
  - FSEL-03
  - FSEL-04
  - FCONF-01
  - FCONF-02
  - FCONF-03
  - FCONF-04
must_haves:
  truths:
    - First tap on an empty calendar day starts a pending selection instead of opening the old reservation dialog.
    - Second tap on the same resource opens a bottom-positioned confirmation surface with the selected range.
    - Cancel clears the pending range and closes the confirmation surface.
    - Confirm creates the reservation with sorted dates, and optional time entry is off until explicitly enabled.
  artifacts:
    - path: frontend/src/pages/fleet/CalendarView.tsx
      provides: two-tap selection flow wired into the fleet calendar grid
    - path: frontend/src/pages/fleet/ReservationConfirmationSheet.tsx
      provides: bottom-sheet confirmation UI for calendar-created reservations
    - path: frontend/src/pages/fleet/CalendarView.test.tsx
      provides: interaction coverage for pending selection, cancel, confirm, and optional times
  key_links:
    - from: frontend/src/pages/fleet/CalendarView.tsx
      to: frontend/src/pages/fleet/calendarRangeSelection.ts
      via: click handler drives pending/completed selection state
      pattern: advanceRangeSelection
    - from: frontend/src/pages/fleet/ReservationConfirmationSheet.tsx
      to: frontend/src/lib/api/hooks/useFleet.ts
      via: useCreateReservation mutation
      pattern: useCreateReservation
    - from: frontend/src/pages/fleet/ReservationConfirmationSheet.tsx
      to: frontend/src/components/ui/sheet.tsx
      via: bottom positioned confirmation surface
      pattern: side="bottom"
---

<objective>
Replace immediate click-to-book calendar behavior with a two-tap range flow and bottom confirmation sheet.

Purpose: deliver the user-visible Phase 35 reservation UX without breaking the existing fleet-page calendar embedding from Phase 34.
Output: a wired calendar interaction, bottom confirmation sheet, and targeted frontend regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-fleet-page-calendar-integration/34-01-SUMMARY.md
@.planning/phases/35-range-selection-confirmation-flow/35-RESEARCH.md
@.planning/phases/35-range-selection-confirmation-flow/35-01-PLAN.md

<interfaces>
From frontend/src/pages/fleet/CalendarView.tsx:
```ts
interface CalendarViewProps {
  embedded?: boolean
}
```

From frontend/src/pages/fleet/ReservationDialog.tsx:
```ts
interface ReservationDialogProps {
  open: boolean
  onOpenChange: (open: boolean) => void
  resourceId?: string
  resourceType?: ResourceType
  initialStartTime?: string
  initialEndTime?: string
}
```

From frontend/src/lib/api/hooks/useFleet.ts:
```ts
export function useCreateReservation(): { mutateAsync: (data: CreateReservationRequest) => Promise<Reservation> }
export function useCalendar(query: CalendarQuery): unknown
```

From frontend/src/components/ui/sheet.tsx:
```ts
export { Sheet, SheetContent, SheetHeader, SheetFooter, SheetTitle, SheetDescription }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: wire two-tap pending selection into CalendarView</name>
  <files>frontend/src/pages/fleet/CalendarView.tsx, frontend/src/pages/fleet/CalendarView.test.tsx, frontend/src/pages/fleet/FleetPage.test.tsx</files>
  <behavior>
    - Test 1: first tap on an empty day marks a pending selection and does not open the confirmation sheet yet.
    - Test 2: second tap on the same resource opens confirmation with a sorted range.
    - Test 3: same-day double tap produces a one-day range.
  </behavior>
  <action>Replace the current `selectedSlot` + immediate `ReservationDialog` calendar path with state driven by the helper from Plan 35-01. Per FSEL-01 through FSEL-04, first tap stores a pending range anchor, second tap on the same resource completes the range, and reverse-order taps normalize before display. Preserve existing reservation chips in the grid while selection is pending, and keep the existing list-based `ReservationDialog` path in `FleetPage` untouched. Add/extend targeted tests to prove the embedded calendar still renders on the fleet page and now uses two-tap selection instead of first-tap booking.</action>
  <verify>
    <automated>cd frontend && npm run test:run -- src/pages/fleet/CalendarView.test.tsx src/pages/fleet/FleetPage.test.tsx</automated>
  </verify>
  <done>CalendarView uses pending/completed range state for empty-day clicks, preserves existing reservations visually, and targeted tests prove the new interaction contract.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: add the bottom confirmation sheet with optional time entry</name>
  <files>frontend/src/pages/fleet/ReservationConfirmationSheet.tsx, frontend/src/pages/fleet/CalendarView.tsx, frontend/src/pages/fleet/CalendarView.test.tsx</files>
  <behavior>
    - Test 1: after a completed range, a bottom sheet opens and shows the selected date range.
    - Test 2: cancel closes the sheet and clears the pending selection.
    - Test 3: optional time entry is hidden by default and appears only after the user enables it.
    - Test 4: confirm submits a reservation mutation with sorted start/end datetimes.
  </behavior>
  <action>Create a dedicated `ReservationConfirmationSheet` using the existing `Sheet` primitive with `side="bottom"` to satisfy FCONF-01. Show the selected range summary, cancel action, confirm action, optional site prefill from preferences, and a checkbox-controlled time section for FCONF-04; when unchecked, submit sensible all-day defaults for the selected dates, and when checked, submit the user-edited datetimes. On cancel, clear completed/pending selection per FCONF-02. On confirm, call `useCreateReservation`, close the sheet, and clear selection per FCONF-03. Do not repurpose the generic `ReservationDialog` for this path.</action>
  <verify>
    <automated>cd frontend && npm run test:run -- src/pages/fleet/CalendarView.test.tsx</automated>
  </verify>
  <done>The embedded calendar opens a bottom confirmation sheet after the second tap, cancel resets state, optional times are opt-in, and confirm creates the reservation through the existing fleet API mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calendar UI → confirmation sheet | Untrusted selection state becomes reservation payload candidates |
| confirmation sheet → fleet reservation API | Client-provided date/time inputs cross into server-side booking validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | T | frontend/src/pages/fleet/CalendarView.tsx | mitigate | Only open the confirmation sheet from a completed helper result so the UI cannot submit a mixed-resource or unsorted range. |
| T-35-05 | I | frontend/src/pages/fleet/ReservationConfirmationSheet.tsx | accept | The sheet exposes only data the user already selected or already sees in the calendar; no new sensitive fields are introduced in this phase. |
| T-35-06 | D | frontend/src/pages/fleet/ReservationConfirmationSheet.tsx | mitigate | Keep confirm submission on the existing `useCreateReservation` path so backend availability and tenant-scoped booking rules still enforce final acceptance. |
| T-35-07 | R | frontend/src/pages/fleet/CalendarView.test.tsx | mitigate | Add regression tests for cancel/reset and opt-in time entry so future refactors cannot silently restore first-tap immediate booking. |
</threat_model>

<verification>
- `cd frontend && npm run test:run -- src/pages/fleet/CalendarView.test.tsx src/pages/fleet/FleetPage.test.tsx`
- `cd frontend && npx eslint src/pages/fleet/CalendarView.tsx src/pages/fleet/ReservationConfirmationSheet.tsx src/pages/fleet/CalendarView.test.tsx src/pages/fleet/FleetPage.test.tsx`
</verification>

<success_criteria>
- The embedded calendar uses a two-tap range selection flow instead of first-tap modal booking.
- The confirmation UI is bottom-positioned and shows the selected date range before submission.
- Cancel and confirm both clear selection state correctly.
- Optional time entry is disabled by default and only affects the reservation payload when enabled.
</success_criteria>

<output>
After completion, create `.planning/phases/35-range-selection-confirmation-flow/35-02-SUMMARY.md`
</output>
