---
phase: 05-pwa-mobile
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - frontend/src/lib/api/client.ts
  - frontend/src/lib/api/auth.ts
  - frontend/src/lib/auth/authStore.ts
  - frontend/src/lib/auth/keycloak.ts
  - frontend/src/lib/auth/pkce.ts
  - frontend/src/components/auth/LoginPage.tsx
  - frontend/src/components/auth/AuthGuard.tsx
  - frontend/src/components/auth/ProfileDropdown.tsx
  - frontend/src/types/api.ts
  - frontend/src/hooks/useAuth.ts
  - frontend/src/hooks/useApi.ts
  - frontend/src/App.tsx
autonomous: true
requirements:
  - PWA-02
must_haves:
  truths:
    - "User kann sich via Keycloak einloggen"
    - "Auth state wird persistent gespeichert (offline verfügbar)"
    - "API Requests sind authentifiziert"
    - "Token Refresh funktioniert automatisch"
    - "Protected Routes leiten Unauthenticated auf Login"
  artifacts:
    - path: "frontend/src/lib/auth/keycloak.ts"
      provides: "Keycloak OAuth2 PKCE flow"
      contains: "authorize, handleCallback"
    - path: "frontend/src/lib/auth/authStore.ts"
      provides: "Auth state with Zustand"
      contains: "useAuthStore"
    - path: "frontend/src/lib/api/client.ts"
      provides: "Authenticated API client"
      contains: "apiClient"
    - path: "frontend/src/components/auth/AuthGuard.tsx"
      provides: "Route protection"
      contains: "AuthGuard"
  key_links:
    - from: "frontend/src/lib/auth/keycloak.ts"
      to: "Keycloak"
      via: "OAuth2 PKCE redirect flow"
    - from: "frontend/src/lib/api/client.ts"
      to: "Backend API"
      via: "Bearer token in Authorization header"
    - from: "frontend/src/lib/auth/authStore.ts"
      to: "localStorage"
      via: "Persist middleware"
---

<objective>
Implement Keycloak authentication with OAuth2 PKCE flow, create authenticated API client, and protect frontend routes with auth guards.

Purpose: Enable secure authentication that works with the existing Keycloak instance, persists across sessions, and provides offline auth state.
Output: Working login flow, authenticated API requests, protected routes, and persistent auth state.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pwa-mobile/05-01-SUMMARY.md

## Keycloak Configuration

From backend implementation:
- JWT validation via JWKS endpoint
- Token contains: sub (user ID), email, tenant_id, realm_access.roles
- Roles: admin, mitarbeiter

Backend auth middleware reference: @src/auth/middleware.rs @src/auth/jwt.rs

## API Endpoints

From backend routes:
- GET /api/v1/auth/me - Get current user profile
- All other endpoints require Bearer token in Authorization header

## Environment Variables

Expected in frontend/.env:
- VITE_KEYCLOAK_URL - Keycloak base URL
- VITE_KEYCLOAK_REALM - Realm name
- VITE_KEYCLOAK_CLIENT_ID - Client ID for PWA
- VITE_API_URL - Backend API URL
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API types</name>
  <files>
    frontend/src/types/api.ts
    frontend/src/types/user.ts
    frontend/src/types/inventory.ts
    frontend/src/types/sites.ts
    frontend/src/types/fleet.ts
  </files>
  <action>
    Create TypeScript types matching backend DTOs:

    1. Create src/types/api.ts with common types:
       ```typescript
       export interface ApiError {
         message: string
         code?: string
       }
       
       export interface PaginatedResponse<T> {
         items: T[]
         total: number
         page: number
         pageSize: number
       }
       ```

    2. Create src/types/user.ts:
       ```typescript
       export interface User {
         id: string
         email: string
         name: string | null
         role: 'admin' | 'mitarbeiter'
         created_at: string
       }
       
       export interface AuthTokens {
         access_token: string
         refresh_token: string
         expires_at: number
       }
       
       export interface KeycloakTokenPayload {
         sub: string
         email: string
         preferred_username: string
         tenant_id: string
         realm_access: { roles: string[] }
       }
       ```

    3. Create src/types/inventory.ts matching backend DTOs:
       - Category, Material, OrderRequest interfaces
       - Create/Update request types

    4. Create src/types/sites.ts matching backend DTOs:
       - Site, SiteAssignment, TimeEntry, Activity interfaces

    5. Create src/types/fleet.ts matching backend DTOs:
       - Vehicle, Tool, Reservation interfaces
       - ResourceType, ResourceStatus enums
  </action>
  <verify>
    <automated>test -f frontend/src/types/user.ts && test -f frontend/src/types/inventory.ts</automated>
  </verify>
  <done>
    - All API types defined
    - Types match backend DTOs exactly
    - TypeScript compilation passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement PKCE helper functions</name>
  <files>
    frontend/src/lib/auth/pkce.ts
  </files>
  <action>
    Create PKCE (Proof Key for Code Exchange) utilities for OAuth2:

    ```typescript
    // src/lib/auth/pkce.ts
    
    // Generate cryptographically secure random string
    export function generateRandomString(length: number): string {
      const array = new Uint8Array(length)
      crypto.getRandomValues(array)
      return Array.from(array, byte => byte.toString(16).padStart(2, '0')).join('')
    }
    
    // Generate code_verifier (43-128 chars)
    export function generateCodeVerifier(): string {
      const array = new Uint8Array(32)
      crypto.getRandomValues(array)
      return base64UrlEncode(array)
    }
    
    // Generate code_challenge from verifier (S256 method)
    export async function generateCodeChallenge(verifier: string): Promise<string> {
      const encoder = new TextEncoder()
      const data = encoder.encode(verifier)
      const digest = await crypto.subtle.digest('SHA-256', data)
      return base64UrlEncode(new Uint8Array(digest))
    }
    
    // Base64 URL encode without padding
    function base64UrlEncode(array: Uint8Array): string {
      let binary = ''
      for (let i = 0; i < array.length; i++) {
        binary += String.fromCharCode(array[i])
      }
      return btoa(binary)
        .replace(/\+/g, '-')
        .replace(/\//g, '_')
        .replace(/=+$/, '')
    }
    ```
  </action>
  <verify>
    <automated>grep -c "generateCodeChallenge" frontend/src/lib/auth/pkce.ts | xargs -I{} test {} -ge 1</automated>
  </verify>
  <done>
    - PKCE utilities implemented
    - Uses Web Crypto API
    - Base64 URL encoding correct
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Keycloak OAuth2 client</name>
  <files>
    frontend/src/lib/auth/keycloak.ts
    frontend/.env.example
  </files>
  <action>
    Create Keycloak OAuth2 client with PKCE flow:

    1. Create .env.example file:
       ```
       VITE_KEYCLOAK_URL=http://keycloak.example.com
       VITE_KEYCLOAK_REALM=schreinerei
       VITE_KEYCLOAK_CLIENT_ID=schreinerei_pwa
       VITE_API_URL=http://localhost:3000
       ```

    2. Create src/lib/auth/keycloak.ts:
       ```typescript
       import { generateCodeVerifier, generateCodeChallenge } from './pkce'
       
       const KEYCLOAK_URL = import.meta.env.VITE_KEYCLOAK_URL
       const REALM = import.meta.env.VITE_KEYCLOAK_REALM
       const CLIENT_ID = import.meta.env.VITE_KEYCLOAK_CLIENT_ID
       
       const AUTH_URL = `${KEYCLOAK_URL}/realms/${REALM}/protocol/openid-connect/auth`
       const TOKEN_URL = `${KEYCLOAK_URL}/realms/${REALM}/protocol/openid-connect/token`
       const LOGOUT_URL = `${KEYCLOAK_URL}/realms/${REALM}/protocol/openid-connect/logout`
       
       const REDIRECT_URI = `${window.location.origin}/auth/callback`
       
       interface TokenResponse {
         access_token: string
         refresh_token: string
         expires_in: number
         token_type: string
       }
       
       // Start login flow - redirect to Keycloak
       export async function startLogin(): Promise<void> {
         const state = generateCodeVerifier()
         const verifier = generateCodeVerifier()
         const challenge = await generateCodeChallenge(verifier)
         
         // Store for callback
         sessionStorage.setItem('pkce_verifier', verifier)
         sessionStorage.setItem('auth_state', state)
         
         const params = new URLSearchParams({
           client_id: CLIENT_ID,
           redirect_uri: REDIRECT_URI,
           response_type: 'code',
           scope: 'openid profile email',
           code_challenge: challenge,
           code_challenge_method: 'S256',
           state: state,
         })
         
         window.location.href = `${AUTH_URL}?${params}`
       }
       
       // Handle callback from Keycloak
       export async function handleCallback(code: string, state: string): Promise<TokenResponse> {
         const storedState = sessionStorage.getItem('auth_state')
         const verifier = sessionStorage.getItem('pkce_verifier')
         
         if (state !== storedState || !verifier) {
           throw new Error('Invalid auth state')
         }
         
         const response = await fetch(TOKEN_URL, {
           method: 'POST',
           headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
           body: new URLSearchParams({
             client_id: CLIENT_ID,
             grant_type: 'authorization_code',
             code,
             redirect_uri: REDIRECT_URI,
             code_verifier: verifier,
           }),
         })
         
         if (!response.ok) {
           throw new Error('Token exchange failed')
         }
         
         // Clear PKCE state
         sessionStorage.removeItem('pkce_verifier')
         sessionStorage.removeItem('auth_state')
         
         return response.json()
       }
       
       // Refresh access token
       export async function refreshToken(refreshToken: string): Promise<TokenResponse> {
         const response = await fetch(TOKEN_URL, {
           method: 'POST',
           headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
           body: new URLSearchParams({
             client_id: CLIENT_ID,
             grant_type: 'refresh_token',
             refresh_token: refreshToken,
           }),
         })
         
         if (!response.ok) {
           throw new Error('Token refresh failed')
         }
         
         return response.json()
       }
       
       // Logout
       export function getLogoutUrl(): string {
         return `${LOGOUT_URL}?client_id=${CLIENT_ID}&post_logout_redirect_uri=${window.location.origin}`
       }
       ```
  </action>
  <verify>
    <automated>grep -c "startLogin" frontend/src/lib/auth/keycloak.ts | xargs -I{} test {} -ge 1</automated>
  </verify>
  <done>
    - Keycloak OAuth2 client implemented
    - PKCE flow works
    - Token refresh implemented
    - Logout URL generated
  </done>
</task>

<task type="auto">
  <name>Task 4: Create auth store with Zustand</name>
  <files>
    frontend/src/lib/auth/authStore.ts
  </files>
  <action>
    Create persistent auth store with Zustand:

    ```typescript
    // src/lib/auth/authStore.ts
    import { create } from 'zustand'
    import { persist, createJSONStorage } from 'zustand/middleware'
    import type { User, AuthTokens } from '@/types/user'
    import { refreshToken as refreshKeycloakToken, getLogoutUrl } from './keycloak'
    
    interface AuthState {
      user: User | null
      tokens: AuthTokens | null
      isAuthenticated: boolean
      isLoading: boolean
      
      // Actions
      setAuth: (user: User, tokens: AuthTokens) => void
      updateUser: (user: Partial<User>) => void
      clearAuth: () => void
      setLoading: (loading: boolean) => void
      
      // Token management
      getAccessToken: () => Promise<string | null>
      checkTokenExpiry: () => void
      logout: () => void
    }
    
    export const useAuthStore = create<AuthState>()(
      persist(
        (set, get) => ({
          user: null,
          tokens: null,
          isAuthenticated: false,
          isLoading: true,
          
          setAuth: (user, tokens) => set({
            user,
            tokens,
            isAuthenticated: true,
            isLoading: false
          }),
          
          updateUser: (userData) => set(state => ({
            user: state.user ? { ...state.user, ...userData } : null
          })),
          
          clearAuth: () => set({
            user: null,
            tokens: null,
            isAuthenticated: false,
            isLoading: false
          }),
          
          setLoading: (loading) => set({ isLoading: loading }),
          
          getAccessToken: async () => {
            const { tokens } = get()
            if (!tokens) return null
            
            // Check if token needs refresh (5 min buffer)
            const now = Date.now() / 1000
            if (tokens.expires_at - now < 300) {
              try {
                const newTokens = await refreshKeycloakToken(tokens.refresh_token)
                const updatedTokens = {
                  ...newTokens,
                  expires_at: now + newTokens.expires_in
                }
                set({ tokens: updatedTokens })
                return updatedTokens.access_token
              } catch {
                get().clearAuth()
                return null
              }
            }
            
            return tokens.access_token
          },
          
          checkTokenExpiry: () => {
            const { tokens, clearAuth } = get()
            if (!tokens) return
            
            const now = Date.now() / 1000
            if (tokens.expires_at < now) {
              clearAuth()
            }
          },
          
          logout: () => {
            get().clearAuth()
            window.location.href = getLogoutUrl()
          }
        }),
        {
          name: 'auth-storage',
          storage: createJSONStorage(() => localStorage),
          partialize: (state) => ({
            user: state.user,
            tokens: state.tokens,
            isAuthenticated: state.isAuthenticated
          })
        }
      )
    )
    ```
  </action>
  <verify>
    <automated>grep -c "useAuthStore" frontend/src/lib/auth/authStore.ts | xargs -I{} test {} -ge 1</automated>
  </verify>
  <done>
    - Auth store with Zustand created
    - Persists to localStorage
    - Auto token refresh
    - Logout function works
  </done>
</task>

<task type="auto">
  <name>Task 5: Create authenticated API client</name>
  <files>
    frontend/src/lib/api/client.ts
    frontend/src/hooks/useApi.ts
  </files>
  <action>
    Create API client with automatic auth token injection:

    1. Create src/lib/api/client.ts:
       ```typescript
       import { useAuthStore } from '@/lib/auth/authStore'
       import type { ApiError } from '@/types/api'
       
       const API_URL = import.meta.env.VITE_API_URL || ''
       
       export class ApiClient {
         private baseUrl: string
         
         constructor(baseUrl: string = API_URL) {
           this.baseUrl = baseUrl
         }
         
         private async getHeaders(): Promise<HeadersInit> {
           const token = await useAuthStore.getState().getAccessToken()
           return {
             'Content-Type': 'application/json',
             ...(token && { Authorization: `Bearer ${token}` })
           }
         }
         
         async request<T>(
           endpoint: string,
           options: RequestInit = {}
         ): Promise<T> {
           const headers = await this.getHeaders()
           
           const response = await fetch(`${this.baseUrl}${endpoint}`, {
             ...options,
             headers: {
               ...headers,
               ...options.headers
             }
           })
           
           if (!response.ok) {
             const error: ApiError = await response.json().catch(() => ({
               message: `HTTP ${response.status}`
             }))
             
             if (response.status === 401) {
               useAuthStore.getState().clearAuth()
             }
             
             throw new Error(error.message)
           }
           
           if (response.status === 204) {
             return {} as T
           }
           
           return response.json()
         }
         
         get<T>(endpoint: string): Promise<T> {
           return this.request<T>(endpoint, { method: 'GET' })
         }
         
         post<T>(endpoint: string, data?: unknown): Promise<T> {
           return this.request<T>(endpoint, {
             method: 'POST',
             body: data ? JSON.stringify(data) : undefined
           })
         }
         
         patch<T>(endpoint: string, data: unknown): Promise<T> {
           return this.request<T>(endpoint, {
             method: 'PATCH',
             body: JSON.stringify(data)
           })
         }
         
         delete<T>(endpoint: string): Promise<T> {
           return this.request<T>(endpoint, { method: 'DELETE' })
         }
       }
       
       export const apiClient = new ApiClient()
       ```

    2. Create src/hooks/useApi.ts for React Query integration:
       ```typescript
       import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
       import { apiClient } from '@/lib/api/client'
       
       // Generic hooks for API operations
       export function useApiQuery<T>(key: string[], endpoint: string) {
         return useQuery({
           queryKey: key,
           queryFn: () => apiClient.get<T>(endpoint)
         })
       }
       
       export function useApiMutation<T, U>(
         endpoint: string,
         method: 'POST' | 'PATCH' | 'DELETE',
         invalidateKeys?: string[][]
       ) {
         const queryClient = useQueryClient()
         
         return useMutation({
           mutationFn: (data: U) => {
             if (method === 'POST') return apiClient.post<T>(endpoint, data)
             if (method === 'PATCH') return apiClient.patch<T>(endpoint, data)
             return apiClient.delete<T>(endpoint)
           },
           onSuccess: () => {
             if (invalidateKeys) {
               invalidateKeys.forEach(key => queryClient.invalidateQueries({ queryKey: key }))
             }
           }
         })
       }
       ```
  </action>
  <verify>
    <automated>grep -c "apiClient" frontend/src/lib/api/client.ts | xargs -I{} test {} -ge 3</automated>
  </verify>
  <done>
    - API client with auth injection
    - Automatic token refresh on 401
    - React Query hooks for data fetching
    - Query invalidation support
  </done>
</task>

<task type="auto">
  <name>Task 6: Create auth components (LoginPage, AuthGuard)</name>
  <files>
    frontend/src/components/auth/LoginPage.tsx
    frontend/src/components/auth/AuthGuard.tsx
    frontend/src/components/auth/AuthCallbackPage.tsx
    frontend/src/hooks/useAuth.ts
    frontend/src/App.tsx
  </files>
  <action>
    Create auth UI components:

    1. Create src/hooks/useAuth.ts:
       ```typescript
       export { useAuthStore as useAuth } from '@/lib/auth/authStore'
       ```

    2. Create src/components/auth/LoginPage.tsx:
       ```typescript
       import { Button } from '@/components/ui/button'
       import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
       import { startLogin } from '@/lib/auth/keycloak'
       
       export default function LoginPage() {
         return (
           <div className="min-h-screen flex items-center justify-center bg-gray-50 p-4">
             <Card className="w-full max-w-md">
               <CardHeader className="text-center">
                 <CardTitle className="text-2xl">Schreinerei SaaS</CardTitle>
                 <CardDescription>
                   Inventar und Baustellenverwaltung
                 </CardDescription>
               </CardHeader>
               <CardContent>
                 <Button 
                   onClick={() => startLogin()}
                   className="w-full"
                   size="lg"
                 >
                   Mit Keycloak anmelden
                 </Button>
               </CardContent>
             </Card>
           </div>
         )
       }
       ```

    3. Create src/components/auth/AuthCallbackPage.tsx:
       ```typescript
       import { useEffect } from 'react'
       import { useNavigate, useSearchParams } from 'react-router-dom'
       import { handleCallback } from '@/lib/auth/keycloak'
       import { useAuth } from '@/hooks/useAuth'
       import { apiClient } from '@/lib/api/client'
       import type { User } from '@/types/user'
       
       export default function AuthCallbackPage() {
         const [params] = useSearchParams()
         const navigate = useNavigate()
         const { setAuth } = useAuth()
         
         useEffect(() => {
           const code = params.get('code')
           const state = params.get('state')
           const error = params.get('error')
           
           if (error) {
             console.error('Auth error:', error)
             navigate('/login')
             return
           }
           
           if (!code || !state) {
             navigate('/login')
             return
           }
           
           handleCallback(code, state)
             .then(async (tokens) => {
               // Get user profile from backend
               const user = await apiClient.get<User>('/api/v1/auth/me')
               
               setAuth(user, {
                 access_token: tokens.access_token,
                 refresh_token: tokens.refresh_token,
                 expires_at: Date.now() / 1000 + tokens.expires_in
               })
               
               navigate('/')
             })
             .catch((err) => {
               console.error('Auth callback error:', err)
               navigate('/login')
             })
         }, [params, navigate, setAuth])
         
         return (
           <div className="min-h-screen flex items-center justify-center">
             <p>Anmeldung wird verarbeitet...</p>
           </div>
         )
       }
       ```

    4. Create src/components/auth/AuthGuard.tsx:
       ```typescript
       import { useEffect } from 'react'
       import { Navigate, useLocation } from 'react-router-dom'
       import { useAuth } from '@/hooks/useAuth'
       import { Skeleton } from '@/components/ui/skeleton'
       
       interface AuthGuardProps {
         children: React.ReactNode
       }
       
       export default function AuthGuard({ children }: AuthGuardProps) {
         const { isAuthenticated, isLoading, checkTokenExpiry } = useAuth()
         const location = useLocation()
         
         useEffect(() => {
           checkTokenExpiry()
         }, [checkTokenExpiry])
         
         if (isLoading) {
           return (
             <div className="min-h-screen flex items-center justify-center">
               <div className="space-y-4 w-64">
                 <Skeleton className="h-8 w-full" />
                 <Skeleton className="h-4 w-3/4" />
               </div>
             </div>
           )
         }
         
         if (!isAuthenticated) {
           return <Navigate to="/login" state={{ from: location }} replace />
         }
         
         return <>{children}</>
       }
       ```

    5. Update App.tsx to use AuthGuard and add auth routes:
       - Add /login route (unprotected)
       - Add /auth/callback route (unprotected)
       - Wrap protected routes with AuthGuard
  </action>
  <verify>
    <automated>test -f frontend/src/components/auth/LoginPage.tsx && test -f frontend/src/components/auth/AuthGuard.tsx</automated>
  </verify>
  <done>
    - Login page renders
    - Auth callback handles OAuth response
    - AuthGuard protects routes
    - Redirects to login when unauthenticated
  </done>
</task>

<task type="auto">
  <name>Task 7: Create ProfileDropdown component</name>
  <files>
    frontend/src/components/auth/ProfileDropdown.tsx
  </files>
  <action>
    Create profile dropdown for user menu in navigation:

    ```typescript
    // src/components/auth/ProfileDropdown.tsx
    import { LogOut, User, Settings } from 'lucide-react'
    import { useAuth } from '@/hooks/useAuth'
    import { Button } from '@/components/ui/button'
    import {
      DropdownMenu,
      DropdownMenuContent,
      DropdownMenuItem,
      DropdownMenuLabel,
      DropdownMenuSeparator,
      DropdownMenuTrigger,
    } from '@/components/ui/dropdown-menu'
    import { Avatar, AvatarFallback } from '@/components/ui/avatar'
    
    export default function ProfileDropdown() {
      const { user, logout } = useAuth()
      
      if (!user) return null
      
      const initials = user.name
        ? user.name.split(' ').map(n => n[0]).join('').toUpperCase()
        : user.email[0].toUpperCase()
      
      return (
        <DropdownMenu>
          <DropdownMenuTrigger asChild>
            <Button variant="ghost" className="relative h-10 w-10 rounded-full">
              <Avatar className="h-10 w-10">
                <AvatarFallback>{initials}</AvatarFallback>
              </Avatar>
            </Button>
          </DropdownMenuTrigger>
          <DropdownMenuContent className="w-56" align="end" forceMount>
            <DropdownMenuLabel className="font-normal">
              <div className="flex flex-col space-y-1">
                <p className="text-sm font-medium leading-none">
                  {user.name || 'Benutzer'}
                </p>
                <p className="text-xs leading-none text-muted-foreground">
                  {user.email}
                </p>
                <p className="text-xs leading-none text-muted-foreground capitalize">
                  {user.role}
                </p>
              </div>
            </DropdownMenuLabel>
            <DropdownMenuSeparator />
            <DropdownMenuItem>
              <User className="mr-2 h-4 w-4" />
              <span>Profil</span>
            </DropdownMenuItem>
            <DropdownMenuItem>
              <Settings className="mr-2 h-4 w-4" />
              <span>Einstellungen</span>
            </DropdownMenuItem>
            <DropdownMenuSeparator />
            <DropdownMenuItem onClick={logout} className="text-red-600">
              <LogOut className="mr-2 h-4 w-4" />
              <span>Abmelden</span>
            </DropdownMenuItem>
          </DropdownMenuContent>
        </DropdownMenu>
      )
    }
    ```
    
    Integrate ProfileDropdown into AppLayout (both MobileNav and DesktopSidebar).
  </action>
  <verify>
    <automated>test -f frontend/src/components/auth/ProfileDropdown.tsx</automated>
  </verify>
  <done>
    - Profile dropdown shows user info
    - Logout works
    - Integrated into navigation
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Keycloak | OAuth2 redirect flow |
| Keycloak → Browser | Authorization code return |
| PWA → Backend | Bearer token authentication |
| localStorage → PWA | Token storage (client-side) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | S (Spoofing) | Token storage | accept | localStorage is origin-scoped, XSS is primary risk |
| T-05-07 | T (Tampering) | JWT tokens | mitigate | Validate tokens server-side, short expiry |
| T-05-08 | I (Info Disclosure) | Refresh token | mitigate | Clear on logout, short expiry, HttpOnly not possible for PWA |
| T-05-09 | E (Elevation) | Role check | mitigate | Role enforced server-side, not just client |
| T-05-10 | S (Spoofing) | PKCE flow | mitigate | State parameter validation, code_verifier prevents code interception |
</threat_model>

<verification>
1. Login page renders at /login
2. Clicking login redirects to Keycloak
3. Callback handles authorization code
4. User profile fetched from /api/v1/auth/me
5. Auth state persisted to localStorage
6. Protected routes redirect to /login when unauthenticated
7. Token refresh triggers before expiry
8. Logout clears state and redirects to Keycloak logout
</verification>

<success_criteria>
1. User kann sich via Keycloak einloggen (PKCE Flow)
2. Auth State wird in localStorage persistiert
3. API Requests enthalten Bearer Token
4. Token Refresh funktioniert automatisch
5. Protected Routes leiten auf Login wenn nicht authentifiziert
6. Logout funktioniert
7. ProfileDropdown zeigt User-Informationen
</success_criteria>

<output>
After completion, create `.planning/phases/05-pwa-mobile/05-02-SUMMARY.md`
</output>
