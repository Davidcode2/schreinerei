---
phase: 05-pwa-mobile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.node.json
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/App.css
  - frontend/src/index.css
  - frontend/src/vite-env.d.ts
  - frontend/tailwind.config.js
  - frontend/postcss.config.js
  - frontend/components.json
  - frontend/src/lib/utils.ts
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/ui/card.tsx
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/label.tsx
  - frontend/src/components/ui/skeleton.tsx
  - frontend/src/components/ui/toast.tsx
  - frontend/src/components/ui/toaster.tsx
  - frontend/src/hooks/use-toast.ts
  - frontend/src/components/layout/AppLayout.tsx
  - frontend/src/components/layout/MobileNav.tsx
  - frontend/src/components/layout/DesktopSidebar.tsx
  - frontend/src/lib/utils/cn.ts
  - frontend/public/manifest.json
  - frontend/public/icons/icon-192.png
  - frontend/public/icons/icon-512.png
autonomous: true
requirements:
  - PWA-01
  - PWA-04
must_haves:
  truths:
    - "App kann auf Mobile-Gerät installiert werden"
    - "PWA Manifest ist konfiguriert"
    - "Responsive Layout funktioniert auf Tablet und Smartphone"
    - "Navigation funktioniert auf Mobile und Desktop"
    - "Tailwind CSS und shadcn/ui sind eingerichtet"
  artifacts:
    - path: "frontend/package.json"
      provides: "Dependencies and scripts"
      contains: '"vite", "react", "tailwindcss"'
    - path: "frontend/vite.config.ts"
      provides: "Vite configuration with PWA plugin"
      contains: "VitePWA"
    - path: "frontend/public/manifest.json"
      provides: "PWA manifest"
      contains: '"name", "start_url"'
    - path: "frontend/src/App.tsx"
      provides: "Root component with routing"
      contains: "BrowserRouter"
    - path: "frontend/src/components/layout/AppLayout.tsx"
      provides: "Responsive layout"
      contains: "MobileNav"
  key_links:
    - from: "frontend/vite.config.ts"
      to: "vite-plugin-pwa"
      via: "VitePWA plugin configuration"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/layout/AppLayout.tsx"
      via: "Layout wrapper"
    - from: "frontend/src/components/layout/AppLayout.tsx"
      to: "MobileNav, DesktopSidebar"
      via: "Responsive layout switching"
---

<objective>
Create the frontend foundation with Vite + React, configure PWA capabilities, set up Tailwind CSS with shadcn/ui components, and implement responsive layout structure.

Purpose: Establish the complete frontend infrastructure for a mobile-first PWA that can be installed on devices.
Output: Working React app with PWA manifest, responsive layout, and UI component library ready for feature development.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Tech Stack Decisions

From user context:
- Vite + React 18
- React Query + Zustand (state management)
- Tailwind CSS + shadcn/ui (styling)
- vite-plugin-pwa + Workbox (offline)
- German UI language

## Backend APIs Available

All modules complete and available:
- /api/v1/auth/* - Authentication
- /api/v1/users/* - User management
- /api/v1/inventory/* - Materials, categories, stock
- /api/v1/sites/* - Sites, time entries, activities
- /api/v1/fleet/* - Vehicles, tools, reservations

## PWA Requirements

From PROJECT.md:
- Mobile-first PWA for Tablet and Smartphone
- Offline-Unterstützung für Baustellen ohne Empfang
- QR-Code Scanner via Kamera
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite + React project</name>
  <files>
    frontend/package.json
    frontend/vite.config.ts
    frontend/tsconfig.json
    frontend/tsconfig.node.json
    frontend/index.html
    frontend/src/main.tsx
    frontend/src/App.tsx
    frontend/src/App.css
    frontend/src/index.css
    frontend/src/vite-env.d.ts
  </files>
  <action>
    Create the frontend directory and initialize a Vite + React + TypeScript project:

    1. Create frontend/ directory at project root
    2. Initialize with npm create vite@latest (React + TypeScript template)
    3. Configure vite.config.ts with:
       - React plugin
       - Server proxy to backend (localhost:3000)
       - Build output directory
    4. Update tsconfig.json for strict mode
    5. Create basic App.tsx with "Schreinerei SaaS" heading
    6. Update index.html with German lang attribute and meta tags for PWA:
       - viewport with mobile-optimized settings
       - theme-color
       - apple-mobile-web-app-capable
    7. Install core dependencies:
       - react, react-dom, react-router-dom
       - @tanstack/react-query
       - zustand
       - tailwindcss, postcss, autoprefixer
       - vite-plugin-pwa
       - workbox-window
       - clsx, tailwind-merge
       - lucide-react (icons)
       - class-variance-authority

    Run: cd frontend && npm create vite@latest . -- --template react-ts --force
    Then: npm install react-router-dom @tanstack/react-query zustand
    Then: npm install -D tailwindcss postcss autoprefixer vite-plugin-pwa
    Then: npm install clsx tailwind-merge lucide-react class-variance-authority
  </action>
  <verify>
    <automated>test -f frontend/package.json && grep -c '"react"' frontend/package.json | xargs -I{} test {} -ge 1</automated>
  </verify>
  <done>
    - frontend/ directory exists with Vite + React + TypeScript
    - package.json has all required dependencies
    - vite.config.ts configured
    - Basic App.tsx renders
    - npm run dev starts the dev server
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS</name>
  <files>
    frontend/tailwind.config.js
    frontend/postcss.config.js
    frontend/src/index.css
  </files>
  <action>
    Set up Tailwind CSS with mobile-first configuration:

    1. Initialize Tailwind: npx tailwindcss init -p
    2. Configure tailwind.config.js:
       - content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"]
       - theme with mobile-first breakpoints:
         - sm: 640px (mobile landscape)
         - md: 768px (tablets)
         - lg: 1024px (desktop)
         - xl: 1280px (large desktop)
       - extend with custom colors for app branding
       - Add container configuration for responsive layouts
    3. Update src/index.css:
       ```css
       @tailwind base;
       @tailwind components;
       @tailwind utilities;
       
       @layer base {
         :root {
           --background: 0 0% 100%;
           --foreground: 222.2 84% 4.9%;
           /* ... shadcn/ui CSS variables */
         }
       }
       ```
    4. Test with sample classes in App.tsx
  </action>
  <verify>
    <automated>test -f frontend/tailwind.config.js && grep -c "content" frontend/tailwind.config.js | xargs -I{} test {} -ge 1</automated>
  </verify>
  <done>
    - Tailwind CSS configured
    - Mobile-first breakpoints defined
    - CSS variables for theming
    - Tailwind classes work in components
  </done>
</task>

<task type="auto">
  <name>Task 3: Install and configure shadcn/ui</name>
  <files>
    frontend/components.json
    frontend/src/lib/utils.ts
    frontend/src/components/ui/button.tsx
    frontend/src/components/ui/card.tsx
    frontend/src/components/ui/input.tsx
    frontend/src/components/ui/label.tsx
    frontend/src/components/ui/skeleton.tsx
    frontend/src/components/ui/toast.tsx
    frontend/src/components/ui/toaster.tsx
    frontend/src/hooks/use-toast.ts
  </files>
  <action>
    Initialize shadcn/ui and install essential components:

    1. Create components.json for shadcn/ui configuration:
       ```json
       {
         "$schema": "https://ui.shadcn.com/schema.json",
         "style": "default",
         "rsc": false,
         "tsx": true,
         "tailwind": {
           "config": "tailwind.config.js",
           "css": "src/index.css",
           "baseColor": "slate"
         },
         "aliases": {
           "components": "@/components",
           "utils": "@/lib/utils"
         }
       }
       ```

    2. Create src/lib/utils.ts with cn() helper:
       ```typescript
       import { clsx, type ClassValue } from "clsx"
       import { twMerge } from "tailwind-merge"
       export function cn(...inputs: ClassValue[]) {
         return twMerge(clsx(inputs))
       }
       ```

    3. Install essential shadcn/ui components via npx:
       - button
       - card
       - input
       - label
       - skeleton
       - toast (and toaster)
       - dropdown-menu
       - dialog
       - sheet
       - avatar
       - badge
       - separator

    Run: npx shadcn@latest add button card input label skeleton toast dropdown-menu dialog sheet avatar badge separator

    4. Update vite.config.ts to add path alias for @/
  </action>
  <verify>
    <automated>test -f frontend/src/components/ui/button.tsx && test -f frontend/src/components/ui/card.tsx</automated>
  </verify>
  <done>
    - shadcn/ui initialized
    - Core components installed
    - cn() utility available
    - Path alias @/ works
  </done>
</task>

<task type="auto">
  <name>Task 4: Configure PWA with vite-plugin-pwa</name>
  <files>
    frontend/vite.config.ts
    frontend/public/manifest.json
  </files>
  <action>
    Configure PWA capabilities with Workbox:

    1. Update vite.config.ts to add VitePWA plugin:
       ```typescript
       import { VitePWA } from 'vite-plugin-pwa'
       
       export default defineConfig({
         plugins: [
           react(),
           VitePWA({
             registerType: 'autoUpdate',
             includeAssets: ['favicon.ico', 'icons/*.png'],
             manifest: {
               name: 'Schreinerei SaaS',
               short_name: 'Schreinerei',
               description: 'Inventar und Baustellenverwaltung für Schreinereien',
               theme_color: '#1e40af',
               background_color: '#ffffff',
               display: 'standalone',
               orientation: 'portrait',
               scope: '/',
               start_url: '/',
               icons: [
                 { src: '/icons/icon-192.png', sizes: '192x192', type: 'image/png' },
                 { src: '/icons/icon-512.png', sizes: '512x512', type: 'image/png' },
                 { src: '/icons/icon-512.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' }
               ]
             },
             workbox: {
               globPatterns: ['**/*.{js,css,html,ico,png,svg}'],
               runtimeCaching: [
                 {
                   urlPattern: /^https:\/\/.*\/api\/v1\/.*/i,
                   handler: 'NetworkFirst',
                   options: {
                     cacheName: 'api-cache',
                     expiration: { maxEntries: 100, maxAgeSeconds: 60 * 60 }
                   }
                 }
               ]
             }
           })
         ]
       })
       ```

    2. Create public/manifest.json with same content as plugin config (for static serving)

    3. Create placeholder icons (192x192 and 512x512) - simple blue square with "S" text for now

    4. Update index.html to link manifest:
       ```html
       <link rel="manifest" href="/manifest.json" />
       <meta name="theme-color" content="#1e40af" />
       <link rel="apple-touch-icon" href="/icons/icon-192.png" />
       ```

    Note: We'll use Workbox runtime caching for offline (Wave 4).
  </action>
  <verify>
    <automated>grep -c "VitePWA" frontend/vite.config.ts | xargs -I{} test {} -ge 1 && test -f frontend/public/manifest.json</automated>
  </verify>
  <done>
    - vite-plugin-pwa configured
    - PWA manifest created
    - Icons present
    - Service worker registration enabled
  </done>
</task>

<task type="auto">
  <name>Task 5: Create responsive layout structure</name>
  <files>
    frontend/src/components/layout/AppLayout.tsx
    frontend/src/components/layout/MobileNav.tsx
    frontend/src/components/layout/DesktopSidebar.tsx
    frontend/src/components/layout/SidebarContent.tsx
    frontend/src/lib/utils/navigation.ts
  </files>
  <action>
    Create the responsive layout with mobile navigation and desktop sidebar:

    1. Create src/lib/utils/navigation.ts with navigation items:
       ```typescript
       export interface NavItem {
         label: string
         href: string
         icon: string // lucide icon name
       }
       
       export const mainNavItems: NavItem[] = [
         { label: 'Dashboard', href: '/', icon: 'LayoutDashboard' },
         { label: 'Inventar', href: '/inventory', icon: 'Package' },
         { label: 'Baustellen', href: '/sites', icon: 'Building2' },
         { label: 'Fuhrpark', href: '/fleet', icon: 'Truck' },
         { label: 'Einstellungen', href: '/settings', icon: 'Settings' },
       ]
       ```

    2. Create DesktopSidebar.tsx:
       - Fixed sidebar on left (hidden on mobile)
       - Logo at top
       - Navigation items with icons
       - User profile at bottom
       - Uses shadcn/ui Button and Separator
       - Width: 240px on desktop

    3. Create MobileNav.tsx:
       - Fixed top bar (hidden on desktop)
       - Hamburger menu icon
       - App title/Logo in center
       - Sheet component for navigation drawer
       - Height: 64px

    4. Create SidebarContent.tsx:
       - Shared navigation content for both layouts
       - Maps over navItems
       - Active state highlighting
       - German labels

    5. Create AppLayout.tsx:
       - Wraps entire application
       - Detects screen size (useMediaQuery or CSS classes)
       - Shows MobileNav on mobile, DesktopSidebar on desktop
       - Main content area with proper padding/margins
       - Breakpoint: md (768px)

    Use Tailwind responsive classes:
       - Hidden on mobile: `hidden md:flex`
       - Hidden on desktop: `flex md:hidden`
  </action>
  <verify>
    <automated>test -f frontend/src/components/layout/AppLayout.tsx && grep -c "MobileNav" frontend/src/components/layout/AppLayout.tsx | xargs -I{} test {} -ge 1</automated>
  </verify>
  <done>
    - Responsive layout switches between mobile and desktop
    - Navigation items defined
    - Mobile: top bar with drawer
    - Desktop: fixed sidebar
    - Main content area responsive
  </done>
</task>

<task type="auto">
  <name>Task 6: Set up React Router with basic routes</name>
  <files>
    frontend/src/App.tsx
    frontend/src/pages/DashboardPage.tsx
    frontend/src/pages/InventoryPage.tsx
    frontend/src/pages/SitesPage.tsx
    frontend/src/pages/FleetPage.tsx
    frontend/src/pages/SettingsPage.tsx
    frontend/src/pages/NotFoundPage.tsx
  </files>
  <action>
    Configure React Router with placeholder pages:

    1. Update App.tsx with React Router setup:
       ```typescript
       import { BrowserRouter, Routes, Route } from 'react-router-dom'
       import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
       import { Toaster } from '@/components/ui/toaster'
       import AppLayout from '@/components/layout/AppLayout'
       
       const queryClient = new QueryClient({
         defaultOptions: {
           queries: {
             staleTime: 5 * 60 * 1000,
             retry: 1
           }
         }
       })
       
       export default function App() {
         return (
           <QueryClientProvider client={queryClient}>
             <BrowserRouter>
               <AppLayout>
                 <Routes>
                   <Route path="/" element={<DashboardPage />} />
                   <Route path="/inventory/*" element={<InventoryPage />} />
                   <Route path="/sites/*" element={<SitesPage />} />
                   <Route path="/fleet/*" element={<FleetPage />} />
                   <Route path="/settings" element={<SettingsPage />} />
                   <Route path="*" element={<NotFoundPage />} />
                 </Routes>
               </AppLayout>
               <Toaster />
             </BrowserRouter>
           </QueryClientProvider>
         )
       }
       ```

    2. Create placeholder pages with German content:
       - DashboardPage: "Dashboard - Übersicht" with placeholder cards
       - InventoryPage: "Inventar" with placeholder list
       - SitesPage: "Baustellen" with placeholder list
       - FleetPage: "Fuhrpark & Werkzeuge" with placeholder tabs
       - SettingsPage: "Einstellungen" with placeholder form
       - NotFoundPage: "404 - Seite nicht gefunden"

    Each page should have:
       - h1 title in German
       - Card component with Skeleton placeholders
       - Responsive layout (full width on mobile, constrained on desktop)
  </action>
  <verify>
    <automated>grep -c "BrowserRouter" frontend/src/App.tsx | xargs -I{} test {} -ge 1 && test -f frontend/src/pages/DashboardPage.tsx</automated>
  </verify>
  <done>
    - React Router configured
    - QueryClient set up
    - All route pages created with placeholders
    - Navigation between pages works
    - Toaster for notifications ready
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → PWA | Untrusted client environment |
| PWA → Backend API | Authenticated requests required |
| Service Worker → Cache | Sensitive data should not be cached without encryption |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | S (Spoofing) | PWA installation | accept | PWA identity verified by manifest origin |
| T-05-02 | T (Tampering) | Service Worker | mitigate | Use integrity checks, HTTPS only |
| T-05-03 | I (Info Disclosure) | Cached data | mitigate | Don't cache sensitive data (auth tokens), clear on logout |
| T-05-04 | D (Denial of Service) | Service Worker | accept | Browser manages SW lifecycle |
| T-05-05 | E (Elevation) | Client-side code | mitigate | All authorization enforced server-side |
</threat_model>

<verification>
1. npm run dev starts the development server
2. PWA manifest accessible at /manifest.json
3. App renders on both mobile and desktop viewports
4. Navigation works between all pages
5. Tailwind classes apply correctly
6. shadcn/ui components render
7. Service worker registration succeeds (check DevTools)
</verification>

<success_criteria>
1. Frontend läuft mit npm run dev
2. PWA Manifest ist konfiguriert und erreichbar
3. Responsive Layout funktioniert (Mobile < 768px, Desktop >= 768px)
4. Navigation zwischen Seiten funktioniert
5. Alle shadcn/ui Komponenten sind installiert
6. Tailwind CSS funktioniert
7. React Query ist konfiguriert
</success_criteria>

<output>
After completion, create `.planning/phases/05-pwa-mobile/05-01-SUMMARY.md`
</output>
