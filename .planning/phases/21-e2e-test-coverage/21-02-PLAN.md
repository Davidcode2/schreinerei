---
phase: 21-e2e-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [frontend/tests/edit-operations.spec.ts]
autonomous: true
requirements: [TEST-13]
user_setup: []

must_haves:
  truths:
    - "E2E test edits a time entry and verifies changes persist"
    - "E2E test deletes a time entry and verifies removal"
    - "E2E test edits a reservation and verifies changes persist"
  artifacts:
    - path: "frontend/tests/edit-operations.spec.ts"
      provides: "Edit operations E2E test suite"
      min_lines: 80
  key_links:
    - from: "frontend/tests/edit-operations.spec.ts"
      to: "TimeEntryDialog component"
      via: "edit button click"
      pattern: "edit|bearbeiten"
    - from: "frontend/tests/edit-operations.spec.ts"
      to: "ReservationDialog component"
      via: "calendar reservation click"
      pattern: "reservation.*edit"
---

<objective>
Create E2E tests that verify edit operations on time entries and reservations, ensuring changes persist correctly.

Purpose: Ensure users can edit and delete time entries and reservations through the UI.
Output: E2E test suite for edit operations.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Time Entry API endpoints from routes.rs -->

```
POST /api/v1/time-entries - Create time entry
GET /api/v1/time-entries/{id} - Get time entry
PATCH /api/v1/time-entries/{id} - Update time entry
DELETE /api/v1/time-entries/{id} - Delete time entry
```

<!-- TimeEntry type from frontend/src/types/sites.ts -->

```typescript
export interface TimeEntry {
  id: string
  site_id: string | null
  user_id: string
  work_type: WorkType
  hours: number
  work_date: string
  notes: string | null
  created_at: string
}

export interface UpdateTimeEntryRequest {
  site_id?: string
  work_type?: WorkType
  hours?: number
  work_date?: string
  notes?: string
}
```

<!-- Reservation API endpoints from routes.rs -->

```
POST /api/v1/fleet/reservations - Create reservation
GET /api/v1/fleet/reservations/{id} - Get reservation
PATCH /api/v1/fleet/reservations/{id} - Update reservation
DELETE /api/v1/fleet/reservations/{id} - Cancel/delete reservation
```

<!-- Reservation type from frontend/src/types/fleet.ts -->

```typescript
export interface UpdateReservationRequest {
  start_time?: string
  end_time?: string
  site_id?: string
  notes?: string
  status?: ReservationStatus
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create time entry edit/delete E2E tests</name>
  <files>frontend/tests/edit-operations.spec.ts</files>
  <behavior>
    - Test 1: Edit time entry hours from 4 to 8, verify change persists
    - Test 2: Edit time entry work_type from 'workshop' to 'site', verify change
    - Test 3: Delete time entry, verify 404 on get
  </behavior>
  <action>
Create frontend/tests/edit-operations.spec.ts with E2E tests for time entry operations:

1. Create test describe block "Time Entry Edit Operations"

2. Test: "should edit time entry hours"
   - Create time entry via API (hours: 4)
   - Navigate to sites page or time entries view
   - Find time entry and click edit button
   - Change hours to 8
   - Save changes
   - Verify hours updated via API (getTimeEntry)

3. Test: "should edit time entry work type"
   - Create time entry via API (work_type: 'workshop')
   - Navigate to time entry edit
   - Change work_type to 'site'
   - Save changes
   - Verify work_type updated via API

4. Test: "should delete time entry"
   - Create time entry via API
   - Navigate to time entry
   - Click delete button
   - Confirm deletion in dialog
   - Verify time entry returns 404 via API

UI elements to target:
- Time entry list: sites page or dedicated time entries view
- Edit button: icon or "Bearbeiten" text
- Delete button: trash icon or "Löschen" text
- Hours input: input[type="number"] or labeled "Stunden"
- Work type select: select or radio buttons

Use useCleanup helper and uniqueName pattern from existing tests.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test edit-operations.spec.ts --grep "time entry" --reporter=list 2>&1 | grep -c "passed" | xargs -I{} bash -c 'test {} -ge 3'</automated>
  </verify>
  <done>All 3 time entry edit/delete tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create reservation edit E2E test</name>
  <files>frontend/tests/edit-operations.spec.ts</files>
  <behavior>
    - Test 1: Edit reservation notes, verify change persists
    - Test 2: Edit reservation start/end time, verify changes
  </behavior>
  <action>
Add to frontend/tests/edit-operations.spec.ts:

1. Create test describe block "Reservation Edit Operations"

2. Test: "should edit reservation notes"
   - Create vehicle and reservation via API
   - Navigate to fleet calendar or reservations view
   - Click on existing reservation
   - Edit notes field
   - Save changes
   - Verify notes updated via API (getReservation)

3. Test: "should edit reservation time range"
   - Create vehicle and reservation via API
   - Navigate to reservation edit
   - Change start_time and end_time
   - Save changes
   - Verify times updated via API

4. Use cleanup to remove test vehicles and reservations

UI elements to target:
- Calendar view: /fleet with calendar tab
- Reservation in calendar: clickable reservation block
- Notes textarea: textarea or input with notes label
- Time inputs: inputs for start/end time

Add to useCleanup tracking for reservations if not already present.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test edit-operations.spec.ts --grep "reservation" --reporter=list 2>&1 | grep -c "passed" | xargs -I{} bash -c 'test {} -ge 2'</automated>
  </verify>
  <done>All 2 reservation edit tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → API | Tests verify only owner can edit own time entries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02 | E | Test isolation | mitigate | uniqueName + useCleanup pattern |
</threat_model>

<verification>
1. TypeScript compiles without errors
2. All 3 time entry edit/delete tests pass
3. All 2 reservation edit tests pass
</verification>

<success_criteria>
1. E2E test edits time entry hours and verifies persistence
2. E2E test edits time entry work type and verifies persistence
3. E2E test deletes time entry and verifies removal
4. E2E test edits reservation and verifies changes persist
</success_criteria>

<output>
After completion, create `.planning/phases/21-e2e-test-coverage/21-02-SUMMARY.md`
</output>
