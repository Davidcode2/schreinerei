---
phase: 21-e2e-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/tests/helpers/api.ts, frontend/tests/helpers/data.ts, frontend/tests/delete-operations.spec.ts]
autonomous: true
requirements: [TEST-12]
user_setup: []

must_haves:
  truths:
    - "E2E test deletes a site via UI and verifies soft delete via API"
    - "E2E test deletes a material via UI and verifies soft delete via API"
    - "E2E test deletes a vehicle via UI and verifies soft delete via API"
    - "E2E test deletes a tool via UI and verifies soft delete via API"
    - "E2E test shows confirmation dialog before delete"
  artifacts:
    - path: "frontend/tests/delete-operations.spec.ts"
      provides: "Delete operations E2E test suite"
      min_lines: 100
    - path: "frontend/tests/helpers/api.ts"
      provides: "API helpers with soft delete verification"
      exports: ["getMaterial", "getSite", "getVehicle", "getTool"]
  key_links:
    - from: "frontend/tests/delete-operations.spec.ts"
      to: "AlertDialog component"
      via: "data-testid selectors"
      pattern: "data-testid.*delete"
---

<objective>
Create E2E tests that verify delete operations on all entity types (sites, materials, vehicles, tools) with confirmation dialogs and soft delete verification.

Purpose: Ensure delete functionality works correctly across all entity types and data is properly soft-deleted.
Output: E2E test suite for delete operations.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing API helper patterns from frontend/tests/helpers/api.ts -->

```typescript
// Pattern for create entity
export async function createSite(page: Page, data: SiteData): Promise<SiteResponse>

// Pattern for delete entity
export async function deleteSite(page: Page, id: string): Promise<void>

// Pattern for get entity
export async function getSite(page: Page, id: string): Promise<SiteResponse>
```

<!-- Existing test structure pattern from frontend/tests/inventory.spec.ts -->

```typescript
test.describe('Data Persistence', () => {
  let cleanup: () => Promise<void>;
  let track: ReturnType<typeof useCleanup>['track'];

  test.beforeEach(async ({ page }) => {
    await login(page);
    const helper = useCleanup(page);
    cleanup = helper.cleanup;
    track = helper.track;
  });

  test.afterEach(async () => {
    await cleanup();
  });
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add time entry and reservation API helpers</name>
  <files>frontend/tests/helpers/api.ts, frontend/tests/helpers/data.ts</files>
  <behavior>
    - Test 1: createTimeEntry returns created entry with ID
    - Test 2: createReservation returns created reservation with ID
    - Test 3: updateTimeEntry updates hours field
    - Test 4: updateReservation updates status field
  </behavior>
  <action>
Add API helpers for time entries and reservations to frontend/tests/helpers/api.ts:

1. Add TimeEntry interfaces and functions:
   - TimeEntryData, TimeEntryResponse interfaces
   - createTimeEntry(page, data): Promise&lt;TimeEntryResponse&gt;
   - getTimeEntry(page, id): Promise&lt;TimeEntryResponse&gt;
   - updateTimeEntry(page, id, data): Promise&lt;TimeEntryResponse&gt;
   - deleteTimeEntry(page, id): Promise&lt;void&gt;

2. Add Reservation interfaces and functions:
   - ReservationData, ReservationResponse interfaces
   - createReservation(page, data): Promise&lt;ReservationResponse&gt;
   - getReservation(page, id): Promise&lt;ReservationResponse&gt;
   - updateReservation(page, id, data): Promise&lt;ReservationResponse&gt;
   - deleteReservation(page, id): Promise&lt;void&gt;

3. Update frontend/tests/helpers/data.ts:
   - Add timeEntries and reservations to TestResources interface
   - Add track.timeEntry and track.reservation methods
   - Add cleanup for these resource types

Use the existing patterns from createMaterial, createSite, etc. as templates.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | grep -c "error" | xargs -I{} bash -c 'test {} -eq 0'</automated>
  </verify>
  <done>TypeScript compiles without errors, new helpers export correctly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create delete operations E2E test suite</name>
  <files>frontend/tests/delete-operations.spec.ts</files>
  <behavior>
    - Test 1: Delete site shows confirmation dialog, confirms, verifies soft delete via 404
    - Test 2: Delete material shows confirmation dialog, confirms, verifies soft delete
    - Test 3: Delete vehicle shows confirmation dialog, confirms, verifies soft delete
    - Test 4: Delete tool shows confirmation dialog, confirms, verifies soft delete
  </behavior>
  <action>
Create frontend/tests/delete-operations.spec.ts with E2E tests for delete operations:

1. Import test utilities and API helpers
2. Create test describe block "Delete Operations"

3. For each entity type (site, material, vehicle, tool):
   a. Test: "should delete {entity} with confirmation dialog"
      - Create entity via API
      - Navigate to entity list page
      - Find and click delete button (look for trash icon or "Löschen" text)
      - Verify AlertDialog appears with confirmation message
      - Click confirm button ("Löschen" or "Delete")
      - Verify entity removed from list
      - Verify API returns 404 for deleted entity (soft delete check)

4. Use useCleanup helper pattern for test isolation
5. Use uniqueName helper for test data names

Follow existing test patterns from inventory.spec.ts and sites.spec.ts.
UI elements to target:
- Delete button: button with trash icon or "Löschen" text
- AlertDialog: role="alertdialog" or data-testid="alert-dialog"
- Confirm button: "Löschen", "Delete", or "Bestätigen"
  </action>
  <verify>
    <automated>cd frontend && npx playwright test delete-operations.spec.ts --reporter=list 2>&1 | grep -c "passed" | xargs -I{} bash -c 'test {} -ge 4'</automated>
  </verify>
  <done>All 4 delete tests pass (site, material, vehicle, tool)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → API | Tests authenticate as test user, verify soft delete isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | I | Test data cleanup | mitigate | useCleanup helper ensures test isolation |
</threat_model>

<verification>
1. TypeScript compiles without errors
2. All 4 delete operation tests pass
3. Tests verify both UI interaction and API soft delete
</verification>

<success_criteria>
1. E2E test deletes site with confirmation and verifies soft delete
2. E2E test deletes material with confirmation and verifies soft delete
3. E2E test deletes vehicle with confirmation and verifies soft delete
4. E2E test deletes tool with confirmation and verifies soft delete
</success_criteria>

<output>
After completion, create `.planning/phases/21-e2e-test-coverage/21-01-SUMMARY.md`
</output>
