---
phase: 21-e2e-test-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [frontend/tests/reservation-status.spec.ts]
autonomous: true
requirements: [TEST-14]
user_setup: []

must_haves:
  truths:
    - "E2E test confirms pending reservation, status becomes 'confirmed'"
    - "E2E test starts confirmed reservation, status becomes 'in_use'"
    - "E2E test completes in_use reservation, status becomes 'completed'"
    - "E2E test cancels reservation, status becomes 'cancelled'"
  artifacts:
    - path: "frontend/tests/reservation-status.spec.ts"
      provides: "Reservation status transition E2E test suite"
      min_lines: 100
  key_links:
    - from: "frontend/tests/reservation-status.spec.ts"
      to: "ReservationDialog status buttons"
      via: "status transition button clicks"
      pattern: "button.*bestätigen|button.*starten|button.*abschließen|button.*stornieren"
---

<objective>
Create E2E tests that verify reservation status transitions through all valid states (pending → confirmed → in_use → completed, and cancelled from any state).

Purpose: Ensure reservation workflow status transitions work correctly via UI.
Output: E2E test suite for reservation status transitions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- ReservationStatus type from frontend/src/types/fleet.ts -->

```typescript
export type ReservationStatus = 'pending' | 'confirmed' | 'in_use' | 'completed' | 'cancelled'
```

<!-- Valid status transitions (from backend logic):
- pending → confirmed, cancelled
- confirmed → in_use, cancelled
- in_use → completed, cancelled
- completed → (terminal state)
- cancelled → (terminal state)
-->

<!-- UpdateReservationRequest supports status change -->

```typescript
export interface UpdateReservationRequest {
  start_time?: string
  end_time?: string
  site_id?: string
  notes?: string
  status?: ReservationStatus
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create reservation status transition E2E tests</name>
  <files>frontend/tests/reservation-status.spec.ts</files>
  <behavior>
    - Test 1: pending → confirmed transition works
    - Test 2: confirmed → in_use transition works
    - Test 3: in_use → completed transition works
    - Test 4: pending → cancelled transition works
    - Test 5: Full workflow: pending → confirmed → in_use → completed
  </behavior>
  <action>
Create frontend/tests/reservation-status.spec.ts with E2E tests for status transitions:

1. Import test utilities and API helpers
2. Create test describe block "Reservation Status Transitions"

3. Test: "should confirm a pending reservation"
   - Create vehicle and reservation with status 'pending' via API
   - Navigate to fleet page
   - Find reservation in calendar or list
   - Click on reservation to open dialog
   - Click "Bestätigen" (Confirm) button
   - Verify status is 'confirmed' via API

4. Test: "should start a confirmed reservation"
   - Create reservation with status 'confirmed'
   - Navigate to reservation
   - Click "Starten" (Start) button
   - Verify status is 'in_use' via API

5. Test: "should complete an in-use reservation"
   - Create reservation with status 'in_use'
   - Navigate to reservation
   - Click "Abschließen" (Complete) button
   - Verify status is 'completed' via API

6. Test: "should cancel a pending reservation"
   - Create reservation with status 'pending'
   - Navigate to reservation
   - Click "Stornieren" (Cancel) button
   - Confirm cancellation if dialog appears
   - Verify status is 'cancelled' via API

7. Test: "should transition through full workflow"
   - Create reservation (pending)
   - Confirm → in_use → completed
   - Verify each transition via API

UI elements to target:
- Reservation in calendar: clickable reservation block
- Status buttons: Look for German labels
  - Bestätigen / Confirm
  - Starten / Start
  - Abschließen / Complete
  - Stornieren / Cancel
- Status badge: shows current status

Use useCleanup and uniqueName patterns.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test reservation-status.spec.ts --reporter=list 2>&1 | grep -c "passed" | xargs -I{} bash -c 'test {} -ge 5'</automated>
  </verify>
  <done>All 5 status transition tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → API | Tests verify state machine transitions are enforced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | T | Status transition order | mitigate | Tests verify invalid transitions blocked |
</threat_model>

<verification>
1. TypeScript compiles without errors
2. All 5 status transition tests pass
3. Tests verify state via API after each transition
</verification>

<success_criteria>
1. E2E test confirms pending reservation successfully
2. E2E test starts confirmed reservation successfully
3. E2E test completes in-use reservation successfully
4. E2E test cancels reservation successfully
</success_criteria>

<output>
After completion, create `.planning/phases/21-e2e-test-coverage/21-03-SUMMARY.md`
</output>
