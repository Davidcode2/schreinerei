---
phase: 21-e2e-test-coverage
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [frontend/tests/calendar-click-create.spec.ts]
autonomous: true
requirements: [TEST-15]
user_setup: []

must_haves:
  truths:
    - "E2E test clicks empty calendar slot and sees reservation dialog"
    - "E2E test creates reservation from calendar click"
    - "E2E test verifies pre-filled dates match clicked slot"
  artifacts:
    - path: "frontend/tests/calendar-click-create.spec.ts"
      provides: "Calendar click-to-create E2E test suite"
      min_lines: 60
  key_links:
    - from: "frontend/tests/calendar-click-create.spec.ts"
      to: "CalendarView empty slot"
      via: "click on empty time slot"
      pattern: "calendar.*slot.*click|data-testid.*slot"
---

<objective>
Create E2E tests that verify calendar click-to-create functionality for reservations, ensuring empty slots are clickable and dialogs pre-fill with correct dates.

Purpose: Ensure users can create reservations directly from calendar by clicking empty time slots.
Output: E2E test suite for calendar click-to-create.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- From Phase 20 Plan 04 Summary - Click-to-Create Implementation -->

Key decisions:
- "Empty calendar slots are clickable with hover state"
- "Default reservation time is 8am-5pm for click-to-create"
- "initialStartTime/initialEndTime props for pre-filling dialog times"

<!-- CalendarView.tsx click handler pattern -->

Click on empty slot:
- Opens ReservationDialog
- Passes initialStartTime and initialEndTime props
- Defaults to 8am-5pm if specific time not clicked

<!-- CreateReservationRequest from frontend/src/types/fleet.ts -->

```typescript
export interface CreateReservationRequest {
  resource_type: ResourceType
  resource_id: string
  site_id?: string
  start_time: string
  end_time: string
  notes?: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create calendar click-to-create E2E tests</name>
  <files>frontend/tests/calendar-click-create.spec.ts</files>
  <behavior>
    - Test 1: Click empty slot opens reservation dialog
    - Test 2: Create reservation from clicked slot, verify persistence
    - Test 3: Verify pre-filled dates match clicked time
  </behavior>
  <action>
Create frontend/tests/calendar-click-create.spec.ts with E2E tests for calendar click-to-create:

1. Import test utilities and API helpers
2. Create test describe block "Calendar Click-to-Create"

3. Test: "should open reservation dialog when clicking empty slot"
   - Create vehicle via API
   - Navigate to fleet page
   - Switch to calendar view (if tabs exist)
   - Find empty time slot (no reservation block)
   - Click on empty slot area
   - Verify ReservationDialog appears
   - Verify dialog is visible (check for dialog title or form elements)

4. Test: "should create reservation from calendar click"
   - Create vehicle via API
   - Navigate to fleet calendar
   - Click empty slot
   - Fill in required fields (if not pre-filled):
     - Resource (vehicle/tool) should be selected
     - Start/end time should be pre-filled
   - Add notes (optional)
   - Click save/create button
   - Verify reservation appears in calendar
   - Verify reservation exists via API (getReservation)

5. Test: "should pre-fill dates from clicked slot"
   - Create vehicle via API
   - Navigate to fleet calendar
   - Click on specific empty slot
   - Verify start_time and end_time inputs are pre-filled
   - Default should be 8am-5pm for clicked day
   - Cancel dialog (don't create)

UI elements to target:
- Calendar view: tab or button to switch to calendar
- Empty slot: clickable area without reservation block
- Reservation dialog: role="dialog" or data-testid="reservation-dialog"
- Time inputs: inputs for start_time/end_time
- Save button: "Speichern" or "Erstellen"

Use useCleanup and uniqueName patterns.
Track created reservations for cleanup.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test calendar-click-create.spec.ts --reporter=list 2>&1 | grep -c "passed" | xargs -I{} bash -c 'test {} -ge 3'</automated>
  </verify>
  <done>All 3 calendar click-to-create tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Calendar UI | Tests verify calendar interaction creates valid data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | I | Test data cleanup | mitigate | useCleanup tracks all created reservations |
</threat_model>

<verification>
1. TypeScript compiles without errors
2. All 3 calendar click-to-create tests pass
3. Tests verify dialog opens, reservation created, dates pre-filled
</verification>

<success_criteria>
1. E2E test verifies clicking empty slot opens reservation dialog
2. E2E test creates reservation from calendar click
3. E2E test verifies pre-filled dates match clicked slot
</success_criteria>

<output>
After completion, create `.planning/phases/21-e2e-test-coverage/21-04-SUMMARY.md`
</output>
