---
phase: 40-calendar-visibility-colors-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/fleet/resourceCalendarColor.ts
  - frontend/src/pages/fleet/resourceCalendarColor.test.ts
  - frontend/src/pages/fleet/CalendarView.tsx
  - frontend/src/pages/fleet/CalendarView.test.tsx
autonomous: true
requirements:
  - FCONF-05
  - FCONF-06
must_haves:
  truths:
    - Existing reservation ranges stay visible while the user starts or completes a new selection.
    - Each fleet resource gets the same color every time from its identity, independent of row position.
    - Selection feedback and reservation visibility coexist in the same calendar week view.
  artifacts:
    - path: frontend/src/pages/fleet/resourceCalendarColor.ts
      provides: deterministic resource-color contract derived from resource identity
      exports:
        - getResourceCalendarColor
    - path: frontend/src/pages/fleet/resourceCalendarColor.test.ts
      provides: regression coverage for stable resource-color mapping
    - path: frontend/src/pages/fleet/CalendarView.tsx
      provides: embedded calendar rendering that preserves visible reservations while selection is active
    - path: frontend/src/pages/fleet/CalendarView.test.tsx
      provides: interaction coverage for reservation visibility and resource-color stability
  key_links:
    - from: frontend/src/pages/fleet/resourceCalendarColor.ts
      to: frontend/src/pages/fleet/CalendarView.tsx
      via: resource identity hashed into UI color metadata
      pattern: getResourceCalendarColor
    - from: frontend/src/pages/fleet/CalendarView.tsx
      to: frontend/src/pages/fleet/calendarRangeSelection.ts
      via: selection overlay is additive to reservation rendering
      pattern: advanceRangeSelection
---

<objective>
Preserve reservation visibility in the embedded fleet calendar while introducing deterministic per-resource colors.

Purpose: satisfy FCONF-05 and FCONF-06 without changing the Phase 39 booking interaction or adding backend color state.
Output: a tested resource-color helper, updated calendar rendering, and focused regression coverage for visibility during selection.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-range-selection-confirmation-flow/39-RESEARCH.md
@.planning/phases/39-range-selection-confirmation-flow/39-02-SUMMARY.md
@.planning/phases/40-calendar-visibility-colors-cleanup/40-RESEARCH.md

<interfaces>
From frontend/src/types/fleet.ts:
```ts
export type ResourceType = 'vehicle' | 'tool'

export interface ReservationSummary {
  id: string
  start_time: string
  end_time: string
  user_name: string
  site_name: string | null
  status: ReservationStatus
}

export interface CalendarEntry {
  resource_type: ResourceType
  resource_id: string
  resource_name: string
  reservations: ReservationSummary[]
}
```

From frontend/src/lib/active-site/siteColor.ts:
```ts
export function getSiteColorClass(siteId: string | null | undefined): string
```

From frontend/src/pages/fleet/calendarRangeSelection.ts:
```ts
export interface PendingRangeSelection {
  resourceId: string
  resourceType: ResourceType
  firstDate: string
}

export interface CompletedRangeSelection {
  resourceId: string
  resourceType: ResourceType
  startDate: string
  endDate: string
}

export function advanceRangeSelection(...):
  | { pendingSelection: PendingRangeSelection }
  | { completedSelection: CompletedRangeSelection }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: define the deterministic fleet resource-color contract</name>
  <files>frontend/src/pages/fleet/resourceCalendarColor.ts, frontend/src/pages/fleet/resourceCalendarColor.test.ts</files>
  <behavior>
    - Test 1: the same `resource_type` + `resource_id` pair always maps to the same color token.
    - Test 2: row order does not matter because the helper never receives or uses an index.
    - Test 3: different resource identities can produce different tokens while staying within the approved palette.
  </behavior>
  <action>Create a pure helper for FCONF-06 that hashes stable resource identity (`${resourceType}:${resourceId}` or equivalent) into a small approved palette, following the client-side deterministic pattern already used in `siteColor.ts`. Export a typed contract consumed by `CalendarView`; do not add backend fields, persisted color state, or row-index-based mapping. Keep the return shape explicit enough for the UI to apply consistent row/chip accents without recomputing palette logic inline.</action>
  <verify>
    <automated>cd frontend && npm run test:run -- src/pages/fleet/resourceCalendarColor.test.ts</automated>
  </verify>
  <done>A pure tested helper exists that deterministically maps each fleet resource identity to the same UI color output on every call.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: wire stable resource colors into CalendarView without hiding reservations</name>
  <files>frontend/src/pages/fleet/CalendarView.tsx, frontend/src/pages/fleet/CalendarView.test.tsx, frontend/src/pages/fleet/resourceCalendarColor.ts</files>
  <behavior>
    - Test 1: an existing reservation label remains visible after the user starts a new selection on another empty day in the same week.
    - Test 2: reservation cells for the same resource expose the same derived color marker before and after rerender within the test.
    - Test 3: pending/completed selection states remain additive and do not replace occupied-cell reservation content.
  </behavior>
  <action>Update `CalendarView` so FCONF-05 and FCONF-06 hold at the same time: keep occupied-cell reservation chips rendered while selection is pending/completed elsewhere, and apply the new resource color contract to resource headers and occupied-cell reservation accents in a way that preserves readable reservation status text. Reuse the existing Phase 39 selection flow exactly; the new styling must layer on top of `advanceRangeSelection` results rather than reopen the old immediate-booking behavior or introduce a second rendering path.</action>
  <verify>
    <automated>cd frontend && npm run test:run -- src/pages/fleet/resourceCalendarColor.test.ts src/pages/fleet/CalendarView.test.tsx</automated>
  </verify>
  <done>The embedded calendar keeps reservation chips visible during range selection, uses deterministic per-resource colors, and has passing regression coverage for both behaviors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calendar API data → resource color helper | Untrusted resource ordering/data must not change identity-derived color mapping |
| selection state → occupied calendar cells | New selection visuals must not obscure existing reservation content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | T | frontend/src/pages/fleet/resourceCalendarColor.ts | mitigate | Hash only stable resource identity fields so color output cannot drift with row order or week-local sorting. |
| T-40-02 | D | frontend/src/pages/fleet/CalendarView.tsx | mitigate | Keep reservation chips rendered for occupied cells and make selection styling additive so booked ranges remain visible during new selection. |
| T-40-03 | R | frontend/src/pages/fleet/CalendarView.test.tsx | mitigate | Add regression tests that prove reserved labels remain visible after selection begins and that repeated renders keep the same resource color marker. |
</threat_model>

<verification>
- `cd frontend && npm run test:run -- src/pages/fleet/resourceCalendarColor.test.ts src/pages/fleet/CalendarView.test.tsx`
- `cd frontend && npx eslint src/pages/fleet/CalendarView.tsx src/pages/fleet/CalendarView.test.tsx src/pages/fleet/resourceCalendarColor.ts src/pages/fleet/resourceCalendarColor.test.ts`
</verification>

<success_criteria>
- The embedded calendar preserves visible reservation chips while a new range is being selected.
- Vehicle and tool colors come from stable resource identity instead of render position.
- Automated tests lock both visibility and color-determinism regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/40-calendar-visibility-colors-cleanup/40-01-SUMMARY.md`
</output>
