---
phase: 40-calendar-visibility-colors-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/fleet/FleetPage.tsx
  - frontend/src/pages/fleet/FleetPage.test.tsx
  - frontend/src/App.tsx
  - frontend/src/pages/fleet/index.ts
autonomous: true
requirements:
  - FCAL-03
must_haves:
  truths:
    - Users reach the main fleet booking experience from `/fleet` rather than a separate calendar CTA or route.
    - The embedded calendar remains above the fleet lists after the cleanup.
    - The list-based reservation dialog path still works after the standalone calendar entry is removed.
  artifacts:
    - path: frontend/src/pages/fleet/FleetPage.tsx
      provides: fleet page with embedded calendar as the primary booking entry experience
    - path: frontend/src/App.tsx
      provides: route table with the standalone fleet calendar entry removed
    - path: frontend/src/pages/fleet/FleetPage.test.tsx
      provides: regression coverage for embedded-first fleet navigation and preserved list-dialog flow
  key_links:
    - from: frontend/src/App.tsx
      to: frontend/src/pages/fleet/FleetPage.tsx
      via: `/fleet` route remains the primary fleet booking path
      pattern: path="/fleet"
    - from: frontend/src/pages/fleet/FleetPage.tsx
      to: frontend/src/pages/fleet/CalendarView.tsx
      via: embedded calendar section at top of page
      pattern: embedded
---

<objective>
Remove the old standalone fleet-calendar entry point so `/fleet` is the single primary booking path.

Purpose: satisfy FCAL-03 and finish the transition started in Phase 38 without breaking the embedded calendar or the list-based reservation flow.
Output: cleaned-up fleet navigation, route definitions, and regression coverage proving the embedded calendar remains the main entry experience.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-fleet-page-calendar-integration/38-01-SUMMARY.md
@.planning/phases/39-range-selection-confirmation-flow/39-02-SUMMARY.md
@.planning/phases/40-calendar-visibility-colors-cleanup/40-RESEARCH.md

<interfaces>
From frontend/src/App.tsx:
```tsx
import { FleetPage, CalendarView } from "@/pages/fleet"

<Route path="/fleet" element={<FleetPage />} />
<Route path="/fleet/calendar" element={<CalendarView />} />
```

From frontend/src/pages/fleet/FleetPage.tsx:
```tsx
<CalendarView embedded />

<ReservationDialog
  open={showReservationDialog}
  onOpenChange={handleCloseDialog}
  resourceId={reserveResource.id}
  resourceType={reserveResource.type}
/>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: remove the fleet-page calendar CTA while preserving embedded booking and list reservations</name>
  <files>frontend/src/pages/fleet/FleetPage.tsx, frontend/src/pages/fleet/FleetPage.test.tsx</files>
  <behavior>
    - Test 1: the fleet page still renders the embedded calendar above the tabs and content.
    - Test 2: the old "Kalenderansicht öffnen" control is no longer rendered.
    - Test 3: the list-based reserve button still opens `ReservationDialog` with the expected resource props.
  </behavior>
  <action>Update `FleetPage` so `/fleet` itself is the only primary booking entry surface for FCAL-03: remove the standalone calendar CTA from the page header, keep the embedded `CalendarView` section in place above the tabs, and preserve the existing list-based `ReservationDialog` path for resource-card reserve actions. Adjust the fleet-page regression tests to prove the embedded calendar remains first-class while the old standalone CTA is gone.</action>
  <verify>
    <automated>cd frontend && npm run test:run -- src/pages/fleet/FleetPage.test.tsx</automated>
  </verify>
  <done>The fleet page no longer offers a separate calendar-entry button, yet still shows the embedded calendar first and keeps list-based reservation actions working.</done>
</task>

<task type="auto">
  <name>task 2: remove the standalone calendar route and stale export wiring</name>
  <files>frontend/src/App.tsx, frontend/src/pages/fleet/index.ts</files>
  <action>Delete the standalone `/fleet/calendar` route from `App.tsx` and remove any now-unused `CalendarView` barrel export wiring so the application no longer exposes a second fleet-calendar entry path. Keep the canonical `/fleet` route intact and make sure the cleanup does not leave stale imports or dead route references behind anywhere in the fleet frontend.</action>
  <verify>
    <automated>cd frontend && npm run test:run -- src/pages/fleet/FleetPage.test.tsx && npx eslint src/App.tsx src/pages/fleet/FleetPage.tsx src/pages/fleet/FleetPage.test.tsx src/pages/fleet/index.ts && ! rg -n "/fleet/calendar" src/App.tsx src/pages/fleet</automated>
  </verify>
  <done>The app route table and fleet barrel exports no longer expose `/fleet/calendar`, and the remaining fleet navigation points users to the embedded `/fleet` experience only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fleet navigation UI → route table | Navigation cleanup must not strand users away from the main booking experience |
| list reservation buttons → dialog flow | Removing the standalone calendar path must not break the separate list-based reservation entry behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-04 | D | frontend/src/App.tsx | mitigate | Keep `/fleet` intact while removing only the standalone calendar route so the main booking surface remains reachable. |
| T-40-05 | T | frontend/src/pages/fleet/FleetPage.tsx | mitigate | Remove only the old calendar CTA; preserve the embedded calendar section and existing list-dialog handlers unchanged. |
| T-40-06 | R | frontend/src/pages/fleet/FleetPage.test.tsx | mitigate | Add regression assertions that the old CTA is absent and the list-based `ReservationDialog` path still opens with the expected props. |
</threat_model>

<verification>
- `cd frontend && npm run test:run -- src/pages/fleet/FleetPage.test.tsx`
- `cd frontend && npx eslint src/App.tsx src/pages/fleet/FleetPage.tsx src/pages/fleet/FleetPage.test.tsx src/pages/fleet/index.ts`
- `cd frontend && ! rg -n "/fleet/calendar" src/App.tsx src/pages/fleet`
</verification>

<success_criteria>
- `/fleet` remains the single main fleet booking route in the app shell.
- The fleet page no longer renders the old standalone calendar-entry CTA.
- Embedded-calendar and list-dialog regressions stay covered by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/40-calendar-visibility-colors-cleanup/40-02-SUMMARY.md`
</output>
