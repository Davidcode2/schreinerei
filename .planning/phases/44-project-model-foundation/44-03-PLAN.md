---
phase: 44-project-model-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 44-02
files_modified:
  - frontend/src/pages/sites/TimeEntryDialog.tsx
  - frontend/src/components/layout/ActiveSiteIndicator.tsx
  - frontend/src/pages/fleet/ReservationConfirmationSheet.tsx
  - frontend/src/pages/fleet/ReservationDialog.tsx
  - frontend/src/pages/DashboardPage.tsx
autonomous: true
requirements:
  - PROJ-10
  - PROJ-11
must_haves:
  truths:
    - Active-project context uses project-aware wording instead of Baustelle-only wording.
    - Time booking and reservation flows can distinguish internal workshop projects from external ones.
    - Dashboard copy reflects the broadened project model without changing hidden filter logic reserved for Phase 47.
  artifacts:
    - path: frontend/src/pages/sites/TimeEntryDialog.tsx
      provides: Project-aware time booking selector
    - path: frontend/src/pages/fleet/ReservationConfirmationSheet.tsx
      provides: Project-aware reservation linkage selector
    - path: frontend/src/components/layout/ActiveSiteIndicator.tsx
      provides: Project-aware active-context UI
    - path: frontend/src/pages/DashboardPage.tsx
      provides: Manager-facing wording aligned with the broadened model
  key_links:
    - from: frontend/src/pages/sites/TimeEntryDialog.tsx
      to: frontend/src/lib/api/hooks/useSites.ts
      via: selector options using active project defaults
      pattern: active_site_id|project_type
    - from: frontend/src/pages/fleet/ReservationConfirmationSheet.tsx
      to: frontend/src/components/layout/ActiveSiteIndicator.tsx
      via: shared active-project terminology and preference plumbing
      pattern: active_site_id
---

<objective>
Propagate the broadened project model into the downstream surfaces that currently assume every site is an external Baustelle.

Purpose: Remove the most visible terminology mismatch and make internal workshop projects usable in productive time/reservation defaults before Phase 46 links those flows more tightly.
Output: Project-aware selectors, active-context copy, and dashboard wording that consume the new Phase 44 backend/frontend contract.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MOBILE-FIRST-CHECKLIST.md
@.planning/phases/44-project-model-foundation/44-CONTEXT.md
@frontend/src/pages/sites/TimeEntryDialog.tsx
@frontend/src/components/layout/ActiveSiteIndicator.tsx
@frontend/src/pages/fleet/ReservationConfirmationSheet.tsx
@frontend/src/pages/fleet/ReservationDialog.tsx
@frontend/src/pages/DashboardPage.tsx

<interfaces>
From frontend/src/pages/sites/TimeEntryDialog.tsx:
```ts
interface TimeEntryDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  siteId?: string;
  siteName?: string;
}
```

From frontend/src/components/layout/ActiveSiteIndicator.tsx:
```ts
const activeSiteId = preferences?.active_site_id ?? null
const activeSite = sites?.find((site) => site.id === activeSiteId)
```

From frontend/src/pages/fleet/ReservationConfirmationSheet.tsx:
```ts
const { data: preferences } = usePreferences()
const { data: sites } = useSites()
const [siteId, setSiteId] = useState(preferences?.active_site_id ?? "")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: update time and reservation selectors to understand the new project model</name>
  <files>frontend/src/pages/sites/TimeEntryDialog.tsx, frontend/src/pages/fleet/ReservationConfirmationSheet.tsx, frontend/src/pages/fleet/ReservationDialog.tsx</files>
  <behavior>
    - Test 1: selectors label projects with project-aware copy and distinguish internal workshop projects from external ones.
    - Test 2: the current `active_site_id` default still preselects the active project where appropriate.
    - Test 3: optional linkage remains optional; this phase does not enforce project selection yet.
  </behavior>
  <action>Replace Baustelle-only labels in time booking and reservation dialogs with project-aware language, surface `project_type` in option labels/badges so workshop projects are recognizable, and preserve the existing `active_site_id` preference/default behavior per D-02. Do not introduce Phase-46-style required linkage; keep selectors optional where they are optional today.</action>
  <verify>
    <automated>npm --prefix frontend run test -- TimeEntryDialog.test.tsx ReservationConfirmationSheet.test.tsx ReservationDialog.test.tsx</automated>
  </verify>
  <done>Time booking and fleet reservation flows can operate against the broadened project model without null-project confusion or Baustelle-only copy.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: align active-context and dashboard wording without changing phase-47 filtering behavior</name>
  <files>frontend/src/components/layout/ActiveSiteIndicator.tsx, frontend/src/pages/DashboardPage.tsx</files>
  <behavior>
    - Test 1: active-context UI reads as project-aware in both compact and full variants.
    - Test 2: dashboard cards/headings no longer imply only external Baustellen while keeping the current dataset/filter behavior unchanged.
  </behavior>
  <action>Update the active-context indicator and dashboard copy to `Projekt`-aware wording, optionally showing internal/external type cues where they reduce ambiguity. Preserve the current dashboard query/filter behavior exactly per D-02 and the Phase-47 deferment; this task may change wording and display affordances only, not the hidden status filter logic.</action>
  <verify>
    <automated>npm --prefix frontend run test -- DashboardPage.test.tsx ActiveSiteIndicator.test.tsx</automated>
  </verify>
  <done>The shared project context UI and dashboard copy reflect the broadened model while leaving the Phase-47 visibility behavior untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| active-context preferences → UI selectors | Cached preference data drives default project selection across workflows |
| manager/worker UI → backend-linked workflows | Project wording changes must not accidentally imply stronger enforcement than the backend currently performs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-09 | Spoofing | active project labels in selectors | mitigate | Display the actual project name plus project-type cue from API data so internal workshop work is not mislabeled as an external Baustelle. |
| T-44-10 | Tampering | time/reservation selector submission | mitigate | Keep optional `site_id`/project linkage semantics unchanged in the mutation payloads; only labels and option rendering change in this phase. |
| T-44-11 | Repudiation | dashboard wording vs actual filter behavior | mitigate | Limit dashboard changes to wording/display hints only and leave query behavior unchanged so operators are not misled about what data is being filtered. |
| T-44-12 | Denial of service | mobile selectors with extra metadata | mitigate | Add lightweight labels/badges only; do not replace current simple selectors with complex desktop-only widgets, per the mobile-first checklist. |
</threat_model>

<verification>
- `npm --prefix frontend run test -- TimeEntryDialog.test.tsx ReservationConfirmationSheet.test.tsx ReservationDialog.test.tsx`
- `npm --prefix frontend run test -- DashboardPage.test.tsx ActiveSiteIndicator.test.tsx`
- `npm --prefix frontend run build`
</verification>

<success_criteria>
- Active-project, time booking, and reservation flows all use project-aware language and can represent workshop projects clearly.
- No current defaulting behavior based on `active_site_id` regresses.
- Dashboard behavior remains functionally unchanged while wording reflects the broadened model.
</success_criteria>

<output>
After completion, create `.planning/phases/44-project-model-foundation/44-03-SUMMARY.md`
</output>
