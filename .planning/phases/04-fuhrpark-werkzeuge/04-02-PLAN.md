---
phase: 04-fuhrpark-werkzeuge
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/modules/fleet/domain/reservation.rs
  - src/modules/fleet/domain.rs
  - src/modules/fleet/domain/events.rs
  - src/modules/fleet/infrastructure/fleet_repository.rs
  - src/modules/fleet/application/fleet_service.rs
  - src/modules/fleet/api/routes.rs
autonomous: true
requirements:
  - FLEET-03
  - FLEET-04
  - FLEET-05
  - FLEET-06
  - FLEET-07
must_haves:
  truths:
    - "Mitarbeiter kann Werkzeug für Zeitraum reservieren"
    - "Mitarbeiter kann Fahrzeug reservieren"
    - "Reservierung wird mit Baustelle verknüpft"
    - "Kalender zeigt Belegung von Fahrzeugen/Werkzeugen"
    - "QR-Code zeigt aktuellen Status/Reservierung"
    - "Verfügbarkeitsprüfung verhindert Überlappungen"
  artifacts:
    - path: "src/modules/fleet/domain/reservation.rs"
      provides: "Reservation aggregate"
      contains: "pub struct Reservation"
    - path: "src/modules/fleet/infrastructure/fleet_repository.rs"
      provides: "Reservation methods with overlap detection"
      contains: "check_availability"
    - path: "src/modules/fleet/api/routes.rs"
      provides: "Calendar and reservation endpoints"
      contains: "calendar"
  key_links:
    - from: "src/modules/fleet/api/routes.rs"
      to: "src/modules/fleet/application/fleet_service.rs"
      via: "service method calls"
    - from: "src/modules/fleet/application/fleet_service.rs"
      to: "src/modules/fleet/infrastructure/fleet_repository.rs"
      via: "availability check"
    - from: "reservation"
      to: "site"
      via: "site_id foreign key"
---

<objective>
Add reservation functionality with overlap detection, calendar view, and QR code status lookup for the Fleet module.

Purpose: Enable employees to reserve vehicles and tools, view calendar availability, and check status via QR code.
Output: Working reservation system with FLEET-03, FLEET-04, FLEET-05, FLEET-06, FLEET-07 requirements implemented.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fuhrpark-werkzeuge/04-RESEARCH.md

## Prior Phase Patterns

From Phase 3 (Sites):
- Site aggregate with status transitions
- Activity feed with timeline ordering
- Dashboard aggregation with JOINs

From Plan 04-01:
- Vehicle and Tool aggregates
- FleetRepository with tenant isolation
- FleetService with role-based authorization
- REST API structure

## Reference Implementation

@src/modules/fleet/domain/vehicle.rs
@src/modules/fleet/domain/tool.rs
@src/modules/fleet/infrastructure/fleet_repository.rs
@src/modules/fleet/application/fleet_service.rs
@src/modules/fleet/api/routes.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Reservation domain model</name>
  <files>
    src/modules/fleet/domain/reservation.rs
    src/modules/fleet/domain.rs
  </files>
  <read_first>
    - src/modules/fleet/domain/vehicle.rs (pattern reference)
    - src/modules/sites/domain/time_entry.rs (time validation pattern)
    - src/common/types.rs (for ReservationId, ReservationStatus, ResourceType)
  </read_first>
  <action>
    Create the Reservation domain model:
    
    1. **domain.rs**: Add `pub mod reservation;` and export Reservation, CreateReservation, UpdateReservation
    
    2. **reservation.rs**: Reservation aggregate with:
       - Reservation struct with all fields from migration:
         - id: ReservationId
         - tenant_id: TenantId
         - resource_type: ResourceType
         - resource_id: uuid::Uuid (polymorphic - references vehicle or tool)
         - user_id: UserId
         - site_id: Option<SiteId>
         - start_time: DateTime<Utc>
         - end_time: DateTime<Utc>
         - status: ReservationStatus
         - notes: Option<String>
         - created_at, updated_at: DateTime<Utc>
       
       - CreateReservation command with validation:
         - resource_type, resource_id, start_time, end_time required
         - site_id optional
         - notes optional
         - Validation: end_time must be after start_time
         - Validation: start_time must not be in the past (for new reservations)
       
       - UpdateReservation command with optional fields:
         - start_time, end_time, site_id, notes, status
       
       - overlaps(&self, other: &Reservation) -> bool method for overlap detection
       - can_transition_to(&self, new_status: ReservationStatus) -> bool method
       - status transitions: Pending → Confirmed → InUse → Completed, any → Cancelled
       
       - ReservationWithDetails struct for API responses:
         - reservation: Reservation
         - resource_name: String
         - user_name: String
         - site_name: Option<String>
  </action>
  <verify>
    <automated>cargo build 2>&1 | grep -c "error" | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>
    - Reservation aggregate with CreateReservation, UpdateReservation commands
    - Time validation (end after start)
    - Overlap detection method
    - Status transition validation
    - cargo build passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Add reservation event types</name>
  <files>src/modules/fleet/domain/events.rs</files>
  <read_first>
    - src/modules/fleet/domain/events.rs (current state)
    - src/modules/sites/domain/events.rs (pattern reference)
  </read_first>
  <action>
    Add reservation-specific events to events.rs:
    
    ```rust
    // Already have VehicleCreated, ToolCreated, ResourceStatusChanged
    
    // Add these:
    pub struct ReservationCreated {
        pub reservation_id: ReservationId,
        pub resource_type: ResourceType,
        pub resource_id: Uuid,
        pub user_id: UserId,
        pub site_id: Option<SiteId>,
        pub start_time: DateTime<Utc>,
        pub end_time: DateTime<Utc>,
    }
    
    pub struct ReservationUpdated {
        pub reservation_id: ReservationId,
        pub changes: Vec<String>,  // list of changed fields
    }
    
    pub struct ReservationCancelled {
        pub reservation_id: ReservationId,
        pub resource_type: ResourceType,
        pub resource_id: Uuid,
    }
    ```
    
    Add helper functions to create these events from DomainEvent::new().
  </action>
  <verify>
    <automated>grep -c "ReservationCreated" src/modules/fleet/domain/events.rs | xargs -I{} test {} -ge 1</automated>
  </verify>
  <done>
    - ReservationCreated, ReservationUpdated, ReservationCancelled events defined
    - Helper functions for event creation
    - cargo build passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Add reservation and calendar methods to repository</name>
  <files>src/modules/fleet/infrastructure/fleet_repository.rs</files>
  <read_first>
    - src/modules/fleet/infrastructure/fleet_repository.rs (current state)
    - src/modules/fleet/domain/reservation.rs (just created)
    - src/modules/sites/infrastructure/site_repository.rs (dashboard aggregation pattern)
  </read_first>
  <action>
    Add reservation and calendar methods to FleetRepository:
    
    **Reservation CRUD:**
    - async fn create_reservation(&self, reservation: &Reservation) -> Result<(), AppError>
    - async fn find_reservation_by_id(&self, tenant_id: TenantId, id: ReservationId) -> Result<Option<Reservation>, AppError>
    - async fn list_reservations(&self, tenant_id: TenantId, user_id: Option<UserId>, resource_type: Option<ResourceType>, resource_id: Option<Uuid>) -> Result<Vec<Reservation>, AppError>
    - async fn update_reservation(&self, reservation: &Reservation) -> Result<(), AppError>
    - async fn delete_reservation(&self, tenant_id: TenantId, id: ReservationId) -> Result<(), AppError>
    
    **Availability checking:**
    - async fn check_availability(&self, tenant_id: TenantId, resource_type: ResourceType, resource_id: Uuid, start_time: DateTime<Utc>, end_time: DateTime<Utc>, exclude_reservation_id: Option<ReservationId>) -> Result<bool, AppError>
      * Returns true if resource is available for the given time range
      * Query: SELECT COUNT(*) FROM reservations WHERE tenant_id = $1 AND resource_type = $2 AND resource_id = $3 AND status != 'cancelled' AND (start_time, end_time) OVERLAPS ($4, $5) AND (exclude_reservation_id IS NULL OR id != $6)
    
    **Calendar view:**
    - async fn get_calendar_data(&self, tenant_id: TenantId, start_date: DateTime<Utc>, end_date: DateTime<Utc>, resource_type: Option<ResourceType>) -> Result<Vec<CalendarEntry>, AppError>
      * CalendarEntry struct:
        - resource_type: ResourceType
        - resource_id: Uuid
        - resource_name: String
        - reservations: Vec<ReservationSummary>
      * ReservationSummary struct:
        - id: ReservationId
        - start_time, end_time: DateTime<Utc>
        - user_name: String
        - site_name: Option<String>
        - status: ReservationStatus
    
    **QR code status:**
    - async fn get_resource_status_by_qr(&self, tenant_id: TenantId, qr_code: &str) -> Result<Option<ResourceStatusInfo>, AppError>
      * ResourceStatusInfo struct:
        - resource_type: ResourceType
        - resource_id: Uuid
        - resource_name: String
        - status: ResourceStatus
        - current_reservation: Option<ReservationSummary>
        - upcoming_reservations: Vec<ReservationSummary> (next 3)
    
    **Reservation with details:**
    - async fn get_reservation_with_details(&self, tenant_id: TenantId, id: ReservationId) -> Result<Option<ReservationWithDetails>, AppError>
      * JOINs with users and sites tables to get names
    
    **CRITICAL**: All queries MUST include tenant_id filter.
  </action>
  <verify>
    <automated>cargo build 2>&1 | grep -c "error" | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>
    - Reservation CRUD methods
    - Availability checking with overlap detection
    - Calendar view with aggregation
    - QR code status lookup
    - All queries include tenant_id filter
    - cargo build passes
  </done>
</task>

<task type="auto">
  <name>Task 4: Add reservation and calendar methods to service</name>
  <files>src/modules/fleet/application/fleet_service.rs</files>
  <read_first>
    - src/modules/fleet/application/fleet_service.rs (current state)
    - src/modules/fleet/infrastructure/fleet_repository.rs (just updated)
    - src/modules/sites/application/site_service.rs (pattern reference)
  </read_first>
  <action>
    Add reservation and calendar methods to FleetService:
    
    **Reservation methods (any authenticated user can create):**
    - async fn create_reservation(&self, tenant_id: TenantId, user_id: UserId, cmd: CreateReservation) -> Result<Reservation, AppError>
      * Validate resource exists (vehicle or tool)
      * Check availability via repository
      * If not available, return error "Resource is not available for the requested time period"
      * Create reservation with status Confirmed (skip pending for V1)
      * Publish ReservationCreated event
    
    - async fn update_reservation(&self, tenant_id: TenantId, user_id: UserId, user_role: Role, reservation_id: ReservationId, cmd: UpdateReservation) -> Result<Reservation, AppError>
      * Get existing reservation
      * Check ownership or admin role
      * If time range changed, check availability (exclude current reservation)
      * Update and publish ReservationUpdated event
    
    - async fn cancel_reservation(&self, tenant_id: TenantId, user_id: UserId, user_role: Role, reservation_id: ReservationId) -> Result<(), AppError>
      * Get existing reservation
      * Check ownership or admin role
      * Set status to Cancelled
      * Publish ReservationCancelled event
    
    - async fn get_reservation(&self, tenant_id: TenantId, reservation_id: ReservationId) -> Result<ReservationWithDetails, AppError>
    - async fn list_reservations(&self, tenant_id: TenantId, user_id: Option<UserId>, resource_type: Option<ResourceType>, resource_id: Option<Uuid>) -> Result<Vec<ReservationWithDetails>, AppError>
    - async fn list_my_reservations(&self, tenant_id: TenantId, user_id: UserId) -> Result<Vec<ReservationWithDetails>, AppError>
    
    **Calendar method:**
    - async fn get_calendar(&self, tenant_id: TenantId, start_date: DateTime<Utc>, end_date: DateTime<Utc>, resource_type: Option<ResourceType>) -> Result<Vec<CalendarEntry>, AppError>
    
    **Availability method:**
    - async fn check_availability(&self, tenant_id: TenantId, resource_type: ResourceType, resource_id: Uuid, start_time: DateTime<Utc>, end_time: DateTime<Utc>) -> Result<bool, AppError>
    
    **QR code method:**
    - async fn get_status_by_qr(&self, tenant_id: TenantId, qr_code: &str) -> Result<ResourceStatusInfo, AppError>
      * Parse QR code format: `{tenant_prefix}:{resource_type}:{id}` or just lookup by qr_code field
      * Return resource status with current/upcoming reservations
  </action>
  <verify>
    <automated>cargo build 2>&1 | grep -c "error" | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>
    - Reservation CRUD with ownership checks
    - Availability checking before reservation
    - Calendar view method
    - QR code status lookup
    - EventBus integration
    - cargo build passes
  </done>
</task>

<task type="auto">
  <name>Task 5: Add reservation and calendar API endpoints</name>
  <files>src/modules/fleet/api/routes.rs</files>
  <read_first>
    - src/modules/fleet/api/routes.rs (current state)
    - src/modules/fleet/application/fleet_service.rs (just updated)
    - src/modules/sites/api/routes.rs (pattern reference)
  </read_first>
  <action>
    Add reservation, calendar, and QR endpoints to routes.rs:
    
    **Reservation endpoints:**
    - POST /api/v1/fleet/reservations -> create_reservation handler (any authenticated user)
    - GET /api/v1/fleet/reservations -> list_reservations handler
    - GET /api/v1/fleet/reservations/my -> list_my_reservations handler
    - GET /api/v1/fleet/reservations/:id -> get_reservation handler
    - PATCH /api/v1/fleet/reservations/:id -> update_reservation handler (owner or admin)
    - DELETE /api/v1/fleet/reservations/:id -> cancel_reservation handler (owner or admin)
    
    **Calendar endpoint:**
    - GET /api/v1/fleet/calendar -> get_calendar handler
      * Query params: start_date, end_date, resource_type (optional)
      * Returns resources with their reservations in the date range
    
    **Availability endpoint:**
    - GET /api/v1/fleet/availability -> check_availability handler
      * Query params: resource_type, resource_id, start_time, end_time
      * Returns { available: boolean }
    
    **QR code endpoint:**
    - GET /api/v1/fleet/qr/:code -> get_status_by_qr handler
      * Returns resource status with current/upcoming reservations
    
    **Request/Response DTOs:**
    - CreateReservationRequest { resource_type, resource_id, site_id?, start_time, end_time, notes? }
    - UpdateReservationRequest { start_time?, end_time?, site_id?, notes?, status? }
    - ReservationResponse with all fields plus resource_name, user_name, site_name
    - CalendarResponse { resources: Vec<CalendarEntry> }
    - AvailabilityResponse { available: bool }
    - QrStatusResponse { resource_type, resource_id, resource_name, status, current_reservation?, upcoming_reservations }
  </action>
  <verify>
    <automated>cargo build 2>&1 | grep -c "error" | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>
    - All reservation endpoints implemented
    - Calendar endpoint with date range filtering
    - Availability endpoint for checking
    - QR code status endpoint
    - Request/response DTOs defined
    - cargo build passes
  </done>
</task>

<task type="auto">
  <name>Task 6: Final build verification</name>
  <files>src/modules/fleet/api/routes.rs</files>
  <read_first>
    - src/modules/fleet/api/routes.rs (just updated)
  </read_first>
  <action>
    Run final build verification:
    
    1. Run `cargo build --release` to ensure everything compiles
    2. Verify all endpoints are properly registered
    3. Check that all repository methods have tenant_id filters
  </action>
  <verify>
    <automated>cargo build --release 2>&1 | grep -c "error" | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>
    - cargo build --release passes
    - All FLEET-03, FLEET-04, FLEET-05, FLEET-06, FLEET-07 requirements implemented
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted input from any HTTP client |
| API → Service | Authenticated user context required |
| Service → Repository | Tenant-isolated data access |
| Reservation → Resource | Availability check prevents double-booking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | T (Tampering) | Reservation times | mitigate | Validate end_time > start_time, start_time not in past |
| T-04-09 | T (Tampering) | Double-booking | mitigate | check_availability with overlap detection before creating reservation |
| T-04-10 | E (Elevation) | Reservation update | mitigate | Check ownership (user_id matches) or admin role |
| T-04-11 | I (Info Disclosure) | QR code lookup | mitigate | QR lookup is tenant-scoped, no cross-tenant access |
| T-04-12 | T (Tampering) | Resource status | mitigate | Status changes only via reservation lifecycle events |
</threat_model>

<verification>
1. cargo build --release compiles without errors
2. Availability check prevents overlapping reservations
3. Calendar view returns correct data structure
4. QR code lookup returns resource status with reservations
5. Role-based access control for reservation updates
6. All repository queries include tenant_id filter
</verification>

<success_criteria>
1. Mitarbeiter kann Werkzeug für Zeitraum reservieren
2. Mitarbeiter kann Fahrzeug reservieren
3. Reservierung wird mit Baustelle verknüpft (site_id)
4. Kalender zeigt Belegung von Fahrzeugen/Werkzeugen
5. QR-Code zeigt aktuellen Status und Reservierungen
6. Verfügbarkeitsprüfung verhindert Überlappungen
7. cargo build --release erfolgreich
</success_criteria>

<output>
After completion, create `.planning/phases/04-fuhrpark-werkzeuge/04-02-SUMMARY.md`
</output>
