---
phase: 28-material-history-tab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/inventory/domain/stock_entry.rs
  - src/modules/inventory/infrastructure/material_repository.rs
  - src/modules/inventory/api/routes.rs
autonomous: true
requirements:
  - HIST-03
  - HIST-04
  - HIST-05
must_haves:
  truths:
    - "Material history API for a Baustelle returns material category name"
    - "Material history API for a Baustelle returns extracting user information"
    - "API query is eager-loaded (single joined query, no per-row lookups)"
  artifacts:
    - path: "src/modules/inventory/infrastructure/material_repository.rs"
      provides: "Site-scoped material history query with joins"
    - path: "src/modules/inventory/api/routes.rs"
      provides: "Tenant-scoped HTTP endpoint for site material history"
    - path: "src/modules/inventory/domain/stock_entry.rs"
      provides: "Domain projection for enriched site material history entries"
  key_links:
    - from: "src/modules/inventory/api/routes.rs"
      to: "src/modules/inventory/infrastructure/material_repository.rs"
      via: "new site history handler calling repository query"
      pattern: "list_stock_entries_for_site"
    - from: "src/modules/inventory/infrastructure/material_repository.rs"
      to: "stock_entries/materials/categories/users/sites tables"
      via: "single SELECT with LEFT/INNER JOIN"
      pattern: "JOIN"
---

<objective>
Deliver a backend endpoint for site-linked material history with category and user context.

Purpose: Satisfy HIST-03/HIST-04/HIST-05 by returning complete, eager-loaded material extraction metadata for one Baustelle.
Output: New inventory site-history API route + enriched repository query + domain projection.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-material-history-tab/28-RESEARCH.md

<interfaces>
From `src/modules/inventory/api/routes.rs`:
```rust
pub struct StockEntryResponse {
    pub id: String,
    pub quantity_change: i32,
    pub quantity_after: i32,
    pub notes: Option<String>,
    pub site_id: Option<String>,
    pub site_name: Option<String>,
    pub created_at: String,
}
```

From `src/modules/inventory/domain/stock_entry.rs`:
```rust
pub struct StockEntryWithSite {
    pub id: uuid::Uuid,
    pub material_id: MaterialId,
    pub user_id: UserId,
    pub quantity_change: i32,
    pub site_id: Option<SiteId>,
    pub site_name: Option<String>,
    pub created_at: DateTime<Utc>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define enriched site-history contracts</name>
  <files>src/modules/inventory/domain/stock_entry.rs, src/modules/inventory/api/routes.rs</files>
  <read_first>src/modules/inventory/domain/stock_entry.rs, src/modules/inventory/api/routes.rs</read_first>
  <behavior>
    - Test 1: Site history DTO includes category_name and extracted_by fields.
    - Test 2: Existing material-centric history endpoint remains backward compatible.
  </behavior>
  <action>Add a dedicated domain projection (for site-scoped rows) carrying `material_name`, `category_name`, `site_id`, `site_name`, and `extracted_by` (resolved display value). Add a new response DTO in routes with these exact fields and `#[derive(Serialize, TS)] #[ts(export, export_to = "frontend/src/types/generated.ts")]`. Keep existing `StockEntryResponse` for `/materials/{id}/history` unchanged.</action>
  <acceptance_criteria>
    - `src/modules/inventory/domain/stock_entry.rs` contains a new exported struct for site history projection with `category_name` and `extracted_by` fields.
    - `src/modules/inventory/api/routes.rs` contains a new TS-exported response struct for site history rows.
    - Existing `StockEntryResponse` still exists for material-specific history endpoint.
  </acceptance_criteria>
  <verify>
    <automated>cargo test stock_entry --lib</automated>
  </verify>
  <done>Contracts for enriched site history are explicit and TS-exportable without breaking existing endpoint types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement eager-loaded site history repository query and route</name>
  <files>src/modules/inventory/infrastructure/material_repository.rs, src/modules/inventory/api/routes.rs</files>
  <read_first>src/modules/inventory/infrastructure/material_repository.rs, src/modules/inventory/api/routes.rs</read_first>
  <behavior>
    - Test 1: `GET /api/v1/inventory/sites/{site_id}/history` returns rows for that site and tenant only.
    - Test 2: Each row includes material_name + category_name + extracted_by + site_name.
  </behavior>
  <action>Add route wiring for `GET /api/v1/inventory/sites/{site_id}/history`. Implement repository method `list_stock_entries_for_site(site_id, tenant_id, limit)` with one SQL query joining `stock_entries se`, `materials m`, `categories c`, `users u`, and `sites s`, filtered by `se.site_id = $1` and `se.tenant_id = $2`, ordered by `se.created_at DESC`. Resolve `extracted_by` as `COALESCE(u.name, u.email, se.user_id::text)` to avoid null display values. Do not perform per-row follow-up queries.</action>
  <acceptance_criteria>
    - `routes.rs` exposes `/api/v1/inventory/sites/{site_id}/history`.
    - `material_repository.rs` contains `list_stock_entries_for_site` and SQL with JOINs to materials/categories/users/sites.
    - SQL includes tenant filter and site filter in WHERE clause.
    - No loop-based DB lookup pattern exists after fetch (`for` with nested query) in handler/repo method.
  </acceptance_criteria>
  <verify>
    <automated>cargo test inventory --lib</automated>
  </verify>
  <done>Backend returns site-scoped enriched material history using a single eager-loaded query.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for tenant and enrichment invariants</name>
  <files>src/modules/inventory/infrastructure/material_repository.rs, src/modules/inventory/api/routes.rs</files>
  <read_first>src/modules/inventory/infrastructure/material_repository.rs, src/modules/inventory/api/routes.rs</read_first>
  <action>Add tests (unit/integration style consistent with existing inventory module) that validate tenant scoping and enriched fields for site history rows; include a case where user name is null and fallback path is used. Keep test fixtures minimal and deterministic.</action>
  <acceptance_criteria>
    - At least one automated test covers tenant isolation for site history endpoint/query.
    - At least one automated test asserts presence of `category_name` and `extracted_by` values.
    - Test includes fallback behavior when user display name is missing.
  </acceptance_criteria>
  <verify>
    <automated>cargo test inventory --lib -- --nocapture</automated>
  </verify>
  <done>Critical backend invariants for HIST-03/04/05 are protected by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→inventory API | Untrusted `site_id` and auth context enter backend |
| inventory API→database | Query must enforce tenant/site filters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | I | site history endpoint | mitigate | Enforce `tenant_id` filter in SQL WHERE for every row returned. |
| T-28-02 | E | site history endpoint | mitigate | Resolve tenant from JWT context only; do not trust client tenant input. |
| T-28-03 | T | repository query | mitigate | Use parameterized SQL binds for `site_id`, `tenant_id`, `limit`. |
| T-28-04 | D | site history endpoint | accept | Keep bounded default limit in handler to reduce oversized responses. |
</threat_model>

<verification>
- `cargo test inventory --lib`
- `cargo test stock_entry --lib`
</verification>

<success_criteria>
- Site material history endpoint exists and returns enriched fields.
- Query uses eager loading joins and tenant scoping.
- Backend tests cover enrichment + tenant isolation.
</success_criteria>

<output>
After completion, create `.planning/phases/28-material-history-tab/28-material-history-tab-01-SUMMARY.md`
</output>
