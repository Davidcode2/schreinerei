---
phase: 31-settings-editing-stock-in
plan: 04
type: execute
wave: 3
depends_on:
  - 31-02
files_modified:
  - frontend/src/pages/settings/InventorySettingsPage.tsx
  - frontend/src/pages/settings/InventorySettingsPage.test.tsx
  - frontend/src/test/mocks/handlers.ts
autonomous: true
requirements:
  - CATS-02
gap_closure: true
must_haves:
  truths:
    - "When category deletion is blocked, the settings page shows the fixed German FK message instead of raw backend text."
    - "The blocked category row stays visible after the failed delete attempt."
    - "Automated tests exercise the real backend conflict payload shape against the current absolute API request base."
  artifacts:
    - path: frontend/src/pages/settings/InventorySettingsPage.tsx
      provides: Category delete conflict translation before inline rendering
    - path: frontend/src/pages/settings/InventorySettingsPage.test.tsx
      provides: Regression coverage for blocked-delete translation using raw backend conflict strings
    - path: frontend/src/test/mocks/handlers.ts
      provides: MSW handlers that match the app's current absolute API request URLs
  key_links:
    - from: frontend/src/pages/settings/InventorySettingsPage.tsx
      to: frontend/src/lib/api/hooks/useInventory.ts
      via: useDeleteCategory onError translation
      pattern: BLOCKED_DELETE_MESSAGE
    - from: frontend/src/pages/settings/InventorySettingsPage.test.tsx
      to: frontend/src/test/mocks/handlers.ts
      via: MSW category handlers and delete override
      pattern: inventory/categories
---

<objective>
Close the Phase 31 verification gap around blocked category deletion messaging and restore trustworthy regression coverage for the settings surface.

Purpose: satisfy CATS-02 with the exact fixed FK-conflict copy the UI spec requires, using tests that prove the real backend error strings are translated correctly.
Output: translated blocked-delete handling in the settings page plus passing settings/list regression tests against the current API base.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-settings-editing-stock-in/31-VERIFICATION.md
@.planning/phases/31-settings-editing-stock-in/31-UI-SPEC.md
@.planning/phases/31-settings-editing-stock-in/31-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: repair the regression harness for real delete-conflict payloads</name>
  <files>frontend/src/pages/settings/InventorySettingsPage.test.tsx, frontend/src/test/mocks/handlers.ts</files>
  <read_first>
frontend/src/pages/settings/InventorySettingsPage.test.tsx
frontend/src/test/mocks/handlers.ts
frontend/src/pages/settings/InventorySettingsPage.tsx
.planning/phases/31-settings-editing-stock-in/31-VERIFICATION.md
.planning/phases/31-settings-editing-stock-in/31-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: category list/settings queries still resolve when the app sends absolute `http://localhost:3009/api/v1/...` requests in Vitest.
    - Test 2: blocked delete returns a raw backend conflict payload such as `Cannot delete category: material history must be preserved` or `Cannot delete category: materials still reference it`.
    - Test 3: the page still renders the fixed German copy `Kategorie konnte nicht gelöscht werden. Entfernen oder verschieben Sie zuerst alle Materialien dieser Kategorie und versuchen Sie es dann erneut.` and keeps the category row visible.
  </behavior>
  <action>Update `frontend/src/test/mocks/handlers.ts` so inventory/material/category handlers match both relative and absolute API URLs by using wildcard MSW route patterns for `/api/v1/...` instead of only bare relative paths. In `frontend/src/pages/settings/InventorySettingsPage.test.tsx`, change the delete-conflict override to use the same wildcard route pattern and return a raw backend conflict payload (`message: 'Cannot delete category: material history must be preserved'` or `message: 'Cannot delete category: materials still reference it'`) rather than the desired German copy. Keep the assertions on the fixed German UI copy and the surviving `Holz` row so the test proves translation instead of echoing mocked output.</action>
  <acceptance_criteria>
    - `frontend/src/test/mocks/handlers.ts` contains wildcard MSW route patterns for inventory categories/materials under `/api/v1`.
    - `frontend/src/pages/settings/InventorySettingsPage.test.tsx` contains a raw English backend delete-conflict payload and still asserts the fixed German blocked-delete copy.
    - `frontend/src/pages/settings/InventorySettingsPage.test.tsx` still asserts that the affected category row remains visible after the failed delete.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- src/pages/settings/InventorySettingsPage.test.tsx src/pages/inventory/InventoryListPage.test.tsx</automated>
  </verify>
  <done>The tests fail only if real backend conflict strings leak to the UI or if the current API base stops matching the MSW handlers.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: translate blocked delete conflicts before inline rendering</name>
  <files>frontend/src/pages/settings/InventorySettingsPage.tsx</files>
  <read_first>
frontend/src/pages/settings/InventorySettingsPage.tsx
frontend/src/lib/api/client.ts
frontend/src/pages/settings/InventorySettingsPage.test.tsx
.planning/phases/31-settings-editing-stock-in/31-VERIFICATION.md
.planning/phases/31-settings-editing-stock-in/31-UI-SPEC.md
  </read_first>
  <behavior>
    - Raw backend conflict strings never render directly in the category row.
    - Known backend delete-conflict messages map to the fixed German UI-spec copy before being stored in `conflictMessages`.
    - The affected category row remains mounted and still shows the inline error under that row after the alert dialog closes.
  </behavior>
  <action>Add a small translation helper inside `InventorySettingsPage.tsx` for category-delete failures. The helper must detect the known backend conflict shapes from verification — `Cannot delete category: material history must be preserved` and `Cannot delete category: materials still reference it` — plus the existing `Cannot delete category:` prefix, and convert those cases to `BLOCKED_DELETE_MESSAGE`. Preserve `error.message` only for unrelated failures. Replace the current `error.message || BLOCKED_DELETE_MESSAGE` assignment in `handleDelete` with the helper result so the inline row message always uses the required German copy for FK conflicts while still closing the alert dialog and leaving the category row in place.</action>
  <acceptance_criteria>
    - `frontend/src/pages/settings/InventorySettingsPage.tsx` no longer contains `error.message || BLOCKED_DELETE_MESSAGE`.
    - The file contains the raw backend conflict strings or prefix check and maps them to `BLOCKED_DELETE_MESSAGE` before writing `conflictMessages[deleteCategoryId]`.
    - `BLOCKED_DELETE_MESSAGE` remains the exact UI-spec string from `31-UI-SPEC.md`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- src/pages/settings/InventorySettingsPage.test.tsx src/pages/inventory/InventoryListPage.test.tsx</automated>
  </verify>
  <done>Blocked category deletes satisfy CATS-02 in production-facing UI behavior instead of leaking backend conflict text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inventory settings delete flow → inventory API | Untrusted backend error payloads cross into user-facing UI text |
| Vitest/MSW request layer → regression tests | Test requests must match the app's current absolute API base to verify real behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-13 | Information disclosure | `InventorySettingsPage.tsx` delete conflict rendering | mitigate | Translate known backend conflict strings to the fixed German copy before storing them in `conflictMessages`, so raw backend internals never reach the user-facing row message. |
| T-31-14 | Repudiation | blocked category delete feedback | mitigate | Keep the failed category row visible and attach the inline conflict message to that exact `category.id`, making it explicit that deletion did not occur. |
| T-31-15 | Denial of service | `frontend/src/test/mocks/handlers.ts` | mitigate | Match wildcard `/api/v1/...` routes so absolute-request tests stop falling into unhandled-request failures and continue to guard the real settings/list flows. |
</threat_model>

<verification>
- From `frontend/`: `npm run test:run -- src/pages/settings/InventorySettingsPage.test.tsx src/pages/inventory/InventoryListPage.test.tsx`
</verification>

<success_criteria>
- Phase 31's only failed truth now passes: blocked category deletion shows the required German FK-conflict message.
- The settings page does not leak raw backend conflict text for known category-delete guards.
- Regression tests cover the real backend conflict payload shape and pass under the current absolute API request base.
</success_criteria>

<output>
After completion, create `.planning/phases/31-settings-editing-stock-in/31-04-SUMMARY.md`
</output>
