---
phase: 23-frontend-ui-auto-assignment
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - frontend/src/components/sites/SiteCard.tsx
  - frontend/src/components/dashboard/SiteCard.tsx
  - frontend/src/pages/sites/SitesListPage.tsx
  - frontend/src/pages/DashboardPage.tsx
autonomous: true
requirements: [ACTV-02, ACTV-03, ACTV-04]
must_haves:
  truths:
    - User can set/clear active Baustelle from the sites overview.
    - User can set/clear active Baustelle from dashboard site cards.
    - Only one active Baustelle is represented in UI at a time.
  artifacts:
    - path: frontend/src/components/sites/SiteCard.tsx
      provides: overview-page toggle action
    - path: frontend/src/components/dashboard/SiteCard.tsx
      provides: dashboard toggle action
  key_links:
    - from: frontend/src/components/sites/SiteCard.tsx
      to: frontend/src/lib/api/hooks/usePreferences.ts
      via: set/clear mutation
      pattern: useUpdatePreferences\(
    - from: frontend/src/pages/DashboardPage.tsx
      to: frontend/src/components/dashboard/SiteCard.tsx
      via: card rendering with toggle affordance
      pattern: SiteCard
---

<objective>
Add explicit active-site toggle controls in the two required entry points (overview and dashboard).

Purpose: Let users intentionally control their active Baustelle where they already manage/inspect sites.
Output: Toggle UI and mutation wiring in site overview + dashboard.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-frontend-ui-auto-assignment/23-01-PLAN.md

<interfaces>
From frontend/src/lib/api/hooks/usePreferences.ts (Plan 23-01 output):
```ts
export function usePreferences()
export function useUpdatePreferences()
```

From frontend/src/types/sites.ts:
```ts
export interface Site { id: string; name: string; status: SiteStatus }
export interface DashboardSite { id: string; name: string; status: string }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add active toggle action to overview site cards</name>
  <files>frontend/src/components/sites/SiteCard.tsx, frontend/src/pages/sites/SitesListPage.tsx</files>
  <behavior>
    - Test 1: Clicking toggle on inactive site sets it as active via PATCH preferences.
    - Test 2: Clicking toggle on active site clears active preference.
  </behavior>
  <action>Integrate active-site state into overview cards with explicit set/clear action (per ACTV-02). Prevent nested-link click conflicts so toggle does not navigate unintentionally. Display exactly one active marker based on current preference (per ACTV-04).</action>
  <verify>
    <automated>cd frontend && npm run test:run -- src/components/sites</automated>
  </verify>
  <done>Overview page supports deterministic set/clear active behavior and visual active marker per card.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add active toggle action to dashboard site cards</name>
  <files>frontend/src/components/dashboard/SiteCard.tsx, frontend/src/pages/DashboardPage.tsx</files>
  <behavior>
    - Test 1: Dashboard card can set active site without leaving page context.
    - Test 2: Existing active site is visually distinct and can be cleared/switched.
  </behavior>
  <action>Expose equivalent toggle control on dashboard active-site cards (per ACTV-03), reusing the same mutation and active-resolution logic introduced in 23-01/Task 1 to maintain a single-active-user model (ACTV-04). Keep existing dashboard KPIs and links functional.</action>
  <verify>
    <automated>cd frontend && npm run test:run -- src/components/dashboard src/pages/DashboardPage.tsx</automated>
  </verify>
  <done>Dashboard offers reliable set/clear/switch active-site actions with no regression to card navigation behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Card click handlers → preferences mutation | User-triggered state change requests cross into API boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | Spoofing | Toggle target site identity | mitigate | Always use backend-provided site IDs from loaded site lists; do not accept arbitrary user-entered IDs. |
| T-23-05 | Tampering | UI state divergence | mitigate | Drive active-marker rendering from preference query source of truth, not optimistic local-only state. |
| T-23-06 | Repudiation | Ambiguous set/clear action | accept | No audit log requirement in phase scope; backend mutation path remains deterministic per user. |
</threat_model>

<verification>
- `cd frontend && npm run test:run -- src/components/sites src/components/dashboard`
- `cd frontend && npm run build`
</verification>

<success_criteria>
- Active site can be toggled from overview and dashboard.
- Switching active site updates visible marker to exactly one site.
- Clearing active site removes active marker everywhere after refetch.
</success_criteria>

<output>
After completion, create `.planning/phases/23-frontend-ui-auto-assignment/23-02-SUMMARY.md`
</output>
