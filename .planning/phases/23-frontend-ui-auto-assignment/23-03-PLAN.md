---
phase: 23-frontend-ui-auto-assignment
plan: 03
type: execute
wave: 3
depends_on: [23-01, 23-02]
files_modified:
  - frontend/src/pages/inventory/WithdrawDialog.tsx
  - frontend/src/pages/inventory/InventoryDetailPage.tsx
  - frontend/src/pages/fleet/ReservationDialog.tsx
  - frontend/src/pages/sites/TimeEntryDialog.tsx
  - frontend/src/lib/api/hooks/useInventory.ts
requirements: [AUTO-01, AUTO-02, AUTO-03, AUTO-04]
autonomous: true
must_haves:
  truths:
    - Withdraw form starts with active site selected and remains changeable.
    - Reservation form starts with active site selected and remains changeable.
    - Time entry form pre-fills active site when work_type is `site` and remains editable.
  artifacts:
    - path: frontend/src/pages/inventory/WithdrawDialog.tsx
      provides: site prefill + override UI for material withdrawal
    - path: frontend/src/pages/fleet/ReservationDialog.tsx
      provides: site prefill + override UI for reservations
    - path: frontend/src/pages/sites/TimeEntryDialog.tsx
      provides: conditional prefill for site work entries
  key_links:
    - from: frontend/src/pages/inventory/InventoryDetailPage.tsx
      to: frontend/src/lib/api/hooks/useInventory.ts
      via: mutation payload with site_id
      pattern: site_id
    - from: frontend/src/pages/sites/TimeEntryDialog.tsx
      to: API create payload
      via: work_type === site conditional
      pattern: workType === "site"
---

<objective>
Wire active-site auto-assignment defaults into material, reservation, and time-entry forms while preserving user control.

Purpose: Reduce repetitive site selection work without removing manual override capabilities.
Output: Prefill + editable site assignment behavior in all three target dialogs/forms.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-frontend-ui-auto-assignment/23-01-PLAN.md

<interfaces>
From frontend/src/types/generated.ts:
```ts
export type WithdrawRequest = { quantity: number; notes: string | null; site_id: string | null }
export type CreateReservationRequest = { site_id: string | null; ... }
export type CreateTimeEntryRequest = { site_id: string | null; work_type: string; ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auto-prefill and override for material withdrawal</name>
  <files>frontend/src/pages/inventory/WithdrawDialog.tsx, frontend/src/pages/inventory/InventoryDetailPage.tsx, frontend/src/lib/api/hooks/useInventory.ts</files>
  <behavior>
    - Test 1: Opening withdraw flow with active site pre-fills selected site.
    - Test 2: User can clear/change site before submit and payload reflects choice.
  </behavior>
  <action>Extend withdraw flow to carry `site_id` in request payload (AUTO-01) and add explicit selection/removal control in dialog (AUTO-04). Keep quantity/notes behavior unchanged and backward compatible when no active site exists.</action>
  <verify>
    <automated>cd frontend && npm run test:run -- src/pages/inventory</automated>
  </verify>
  <done>Withdraw API call includes selected/null `site_id` and UI allows user override before submission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto-prefill and override for reservation dialog</name>
  <files>frontend/src/pages/fleet/ReservationDialog.tsx</files>
  <behavior>
    - Test 1: Create-mode reservation initializes `siteId` from active preference.
    - Test 2: User can replace or clear prefill before create/update submission.
  </behavior>
  <action>Use active-site preference as initial `siteId` for create-mode reservations (AUTO-02) while preserving editable state and existing availability checks (AUTO-04). Ensure edit mode keeps existing reservation-specific site data as priority.</action>
  <verify>
    <automated>cd frontend && npm run test:run -- src/pages/fleet/ReservationDialog.tsx</automated>
  </verify>
  <done>Reservation create payload defaults from active site and remains user-adjustable with no edit-mode regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Auto-prefill and override for time entry dialog</name>
  <files>frontend/src/pages/sites/TimeEntryDialog.tsx</files>
  <behavior>
    - Test 1: For `work_type=site`, create flow pre-fills active site.
    - Test 2: For non-site work types, no forced site assignment is submitted.
  </behavior>
  <action>Integrate active-site default into time-entry creation only when `work_type` is `site` (AUTO-03), and keep explicit user ability to clear/change assignment before submit (AUTO-04). Maintain current behavior for workshop/travel/other types.</action>
  <verify>
    <automated>cd frontend && npm run test:run -- src/pages/sites/TimeEntryDialog.tsx</automated>
  </verify>
  <done>Time-entry payload follows conditional prefill rules and preserves user override controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prefilled client state → mutation payloads | Derived defaults become API input unless user overrides |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | Tampering | Form payload enrichment with `site_id` | mitigate | Explicitly bind `site_id` to validated selection state; send `null` when removed instead of stale hidden value. |
| T-23-08 | Information Disclosure | Site labels in dialogs | accept | Site names are already visible in authorized site lists; no additional sensitive data exposed. |
| T-23-09 | Denial of Service | Repeated prefill resets | mitigate | Initialize defaults on dialog open/create-mode only; avoid effect loops that overwrite user edits mid-session. |
</threat_model>

<verification>
- `cd frontend && npm run test:run -- src/pages/inventory src/pages/fleet src/pages/sites`
- `cd frontend && npm run build`
</verification>

<success_criteria>
- Material, reservation, and time-entry forms prefill active site where required.
- User can override or clear assignment before submit in each flow.
- Payloads carry expected `site_id` values and preserve non-site time-entry behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/23-frontend-ui-auto-assignment/23-03-SUMMARY.md`
</output>
