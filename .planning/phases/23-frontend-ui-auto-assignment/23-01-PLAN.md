---
phase: 23-frontend-ui-auto-assignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/api/hooks/usePreferences.ts
  - frontend/src/lib/api/hooks/index.ts
  - frontend/src/lib/active-site/siteColor.ts
  - frontend/src/components/layout/ActiveSiteIndicator.tsx
  - frontend/src/components/layout/DesktopSidebar.tsx
  - frontend/src/components/layout/MobileNav.tsx
autonomous: true
requirements: [ACTV-01, ACTV-05, ACTV-06, ACTV-07]
must_haves:
  truths:
    - User sees active Baustelle indicator in persistent app chrome.
    - Indicator color is deterministic for the same Baustelle across renders.
    - Active state survives navigation and refresh by syncing from backend preferences.
  artifacts:
    - path: frontend/src/lib/api/hooks/usePreferences.ts
      provides: GET/PATCH preferences query+mutation hooks
    - path: frontend/src/lib/active-site/siteColor.ts
      provides: deterministic color mapping utility
    - path: frontend/src/components/layout/ActiveSiteIndicator.tsx
      provides: reusable persistent indicator UI
  key_links:
    - from: frontend/src/components/layout/ActiveSiteIndicator.tsx
      to: frontend/src/lib/api/hooks/usePreferences.ts
      via: usePreferences hook
      pattern: usePreferences\(
    - from: frontend/src/components/layout/DesktopSidebar.tsx
      to: frontend/src/components/layout/ActiveSiteIndicator.tsx
      via: component composition
      pattern: ActiveSiteIndicator
---

<objective>
Create the active-site data/visual foundation so every page can show the same persisted active Baustelle.

Purpose: Establish one canonical frontend integration for preferences + color identity before page-level toggles and form prefill.
Output: Shared hooks, color utility, and persistent layout indicator.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-frontend-ui-auto-assignment/23-RESEARCH.md

<interfaces>
From frontend/src/types/generated.ts:
```ts
export type PreferencesResponse = { active_site_id: string | null }
export type UpdatePreferencesRequest = { active_site_id: string | null }
```

From frontend/src/lib/api/hooks/useSites.ts:
```ts
export function useSites(query?: ListSitesQuery)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add preferences hooks and cache invalidation wiring</name>
  <files>frontend/src/lib/api/hooks/usePreferences.ts, frontend/src/lib/api/hooks/index.ts</files>
  <behavior>
    - Test 1: GET hook returns `active_site_id` from `/api/v1/preferences`.
    - Test 2: PATCH hook sends `active_site_id` and invalidates dependent queries after success.
  </behavior>
  <action>Create `usePreferences` and `useUpdatePreferences` hooks using existing React Query patterns; on successful update invalidate preferences + site/dashboard query keys to ensure UI sync (per ACTV-07). Do not duplicate HTTP client logic; use `apiClient` helper.</action>
  <verify>
    <automated>cd frontend && npm run test:run -- src/lib/api/hooks</automated>
  </verify>
  <done>Preferences hooks exist, exported via hooks index, and updates trigger deterministic refetch-driven sync.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic site color utility and indicator component</name>
  <files>frontend/src/lib/active-site/siteColor.ts, frontend/src/components/layout/ActiveSiteIndicator.tsx</files>
  <behavior>
    - Test 1: Same site input always maps to the same color token.
    - Test 2: Null active site renders neutral "keine aktive Baustelle" state.
  </behavior>
  <action>Implement hash-based site color mapping utility (per ACTV-05) and an `ActiveSiteIndicator` component that reads active preference + resolved site name and displays name/color in a compact persistent badge (per ACTV-01). Avoid random/color-picker behavior; output must be deterministic.</action>
  <verify>
    <automated>cd frontend && npm run test:run -- src/components/layout src/lib/active-site</automated>
  </verify>
  <done>Indicator component renders stable color-coded active-site state and handles loading/empty state safely.</done>
</task>

<task type="auto">
  <name>Task 3: Wire indicator into desktop and mobile layout shells</name>
  <files>frontend/src/components/layout/DesktopSidebar.tsx, frontend/src/components/layout/MobileNav.tsx</files>
  <action>Embed `ActiveSiteIndicator` in both desktop and mobile navigation shells so active context persists across route changes and refresh-driven rehydration (per ACTV-06, ACTV-07). Keep existing offline/sync controls intact; do not remove navigation items.</action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Both nav variants show the same active-site indicator consistently without breaking layout responsiveness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser UI → Preferences API | Untrusted client interactions trigger preference mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | usePreferences mutation payload | mitigate | Restrict payload to `active_site_id` only and rely on backend tenant validation; do not send full site objects. |
| T-23-02 | Information Disclosure | ActiveSiteIndicator rendering | mitigate | Render only site name/color metadata from already-authorized `/preferences` + `/sites` responses; no hidden IDs in visible text. |
| T-23-03 | Denial of Service | Re-render/query loops | mitigate | Use stable query keys and mutation invalidation only on success to avoid uncontrolled refetch churn. |
</threat_model>

<verification>
- `cd frontend && npm run test:run -- src/lib/api/hooks src/components/layout`
- `cd frontend && npm run build`
</verification>

<success_criteria>
- Persistent indicator is visible in desktop + mobile shells.
- Indicator reflects backend preference after refresh.
- Color identity is deterministic and consistent across renders.
</success_criteria>

<output>
After completion, create `.planning/phases/23-frontend-ui-auto-assignment/23-01-SUMMARY.md`
</output>
