---
phase: 45-unified-project-timeline
plan: 02
type: execute
wave: 2
depends_on:
  - 45-01
files_modified:
  - frontend/src/pages/sites/SiteDetailPage.tsx
  - frontend/src/pages/sites/SiteDetailPage.test.tsx
  - frontend/src/pages/sites/ActivityFeed.tsx
  - frontend/src/pages/sites/ActivityFeed.test.tsx
autonomous: true
requirements:
  - PROJ-18
  - PROJ-20
must_haves:
  truths:
    - The project detail screen presents the timeline as the main execution-context surface rather than a side activity widget.
    - Timeline cards show consistent timestamp treatment and attachment previews across note, legacy photo, and status entries.
    - Material history remains reachable from the same project timeline surface instead of moving to a parallel communication area.
  artifacts:
    - path: frontend/src/pages/sites/SiteDetailPage.tsx
      provides: Canonical project timeline placement and entry CTA wiring on the detail page
    - path: frontend/src/pages/sites/ActivityFeed.tsx
      provides: Consistent timeline rendering, copy, and timestamp presentation across entry types
  key_links:
    - from: frontend/src/pages/sites/SiteDetailPage.tsx
      to: frontend/src/pages/sites/CreateNoteModal.tsx
      via: primary timeline entry CTA opens the unified composer from plan 45-01
      pattern: CreateNoteModal|showNoteModal
    - from: frontend/src/pages/sites/ActivityFeed.tsx
      to: frontend/src/pages/sites/mediaViewerRoute.ts
      via: attachment tiles and legacy photo tiles continue to deep-link into the protected media viewer
      pattern: buildMediaViewerPath|extractAttachmentIdFromPhotoUrl
---

<objective>
Promote the project timeline to the primary context surface on project detail and polish rendering so timestamps and previews stay consistent across all entry types.

Purpose: Deliver PROJ-18 and PROJ-20 by making the timeline visibly canonical while preserving the shipped material-history and media-viewer behaviors.
Output: Updated detail-page layout/copy, consistent timeline-card rendering, and regression tests for timeline-first behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/FEATURES.md
@.planning/REQUIREMENTS.md
@.planning/requirement-reviews/projects/PROJ-18.md
@.planning/requirement-reviews/projects/PROJ-20.md
@.planning/MOBILE-FIRST-CHECKLIST.md
@.planning/phases/44-project-model-foundation/44-02-SUMMARY.md
@.planning/phases/44-project-model-foundation/44-03-SUMMARY.md
@.planning/phases/45-unified-project-timeline/45-01-PLAN.md
@frontend/src/pages/sites/SiteDetailPage.tsx
@frontend/src/pages/sites/ActivityFeed.tsx
@frontend/src/pages/sites/mediaViewerRoute.ts

<interfaces>
From frontend/src/pages/sites/SiteDetailPage.tsx:
```ts
const [showNoteModal, setShowNoteModal] = useState(false)
const [showCameraFlow, setShowCameraFlow] = useState(false)
const { data: activities, refetch: refetchActivities } = useActivities(id!)
```

From frontend/src/pages/sites/ActivityFeed.tsx:
```ts
interface ActivityFeedProps {
  activities: Activity[]
  siteId: string
  maxItems?: number
}

function formatRelativeTime(dateString: string): string
function buildLegacyPhotoAttachment(activity: Activity): ActivityAttachment | null
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: make the project timeline the primary context surface on detail pages</name>
  <files>frontend/src/pages/sites/SiteDetailPage.tsx, frontend/src/pages/sites/SiteDetailPage.test.tsx</files>
  <behavior>
    - Test 1: the detail page labels the main feed as `Projekt-Timeline` and explains it as the place for notes, photos, and documents.
    - Test 2: the main entry CTA opens the unified composer from plan 45-01, while the camera shortcut remains available as a fast path into the same model.
    - Test 3: material history stays reachable in the same timeline component instead of moving into a second communication widget.
  </behavior>
  <action>Reorder and relabel `SiteDetailPage` so the timeline card sits directly in the main execution-context flow, ahead of secondary management sections when needed to communicate canonically that this is the job memory per PROJ-18. Rename the card and helper copy from generic `Aktivitäten` wording to explicit project-timeline language, keep the mobile tap targets from the checklist, and avoid adding any new side-channel widget (chat, second notes area, or separate documents section). Keep the existing wrappers from plan 45-01 for the entry buttons so this page only wires the canonical timeline surface.</action>
  <verify>
    <automated>npm --prefix frontend run test -- SiteDetailPage.test.tsx</automated>
  </verify>
  <done>The project detail page clearly communicates one canonical timeline surface for project context, and the unified composer is reachable there without duplicating communication entrypoints.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: normalize timeline timestamps and preview rendering across entry types</name>
  <files>frontend/src/pages/sites/ActivityFeed.tsx, frontend/src/pages/sites/ActivityFeed.test.tsx</files>
  <behavior>
    - Test 1: note, legacy photo, and status-change cards all show the same exact timestamp treatment, with absolute date/time plus the current relative context where it adds value.
    - Test 2: attachment previews remain available for image and PDF entries, and legacy photo entries still route through the protected media viewer.
    - Test 3: the empty state and tab labels describe the feed as project timeline context while preserving the material-history tab.
  </behavior>
  <action>Update `ActivityFeed` so every project-timeline card renders timestamps consistently using the existing `created_at` data from the API, rather than relying on relative time alone. Keep the existing attachment tile and protected blob-fetch behavior, but make copy/headings consistent so attachment-backed note entries and legacy `photo` entries feel like one timeline. Preserve the material tab inside the same component, because PROJ-18 requires related execution history to stay reachable from this canonical context channel.</action>
  <verify>
    <automated>npm --prefix frontend run test -- ActivityFeed.test.tsx</automated>
  </verify>
  <done>The timeline feed reads as one coherent project memory with consistent timestamps, consistent attachment previews, and no regression to legacy photo/media-viewer behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project detail UI → worker understanding | Copy and placement changes influence which surface users treat as canonical project context |
| timeline cards → protected media viewer | Attachment and legacy photo links must still route through authenticated viewer paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-05 | Spoofing | timeline status/copy hierarchy | mitigate | Use explicit `Projekt-Timeline` wording on the detail page and empty state so users do not mistake a secondary widget for the canonical context surface. |
| T-45-06 | Information disclosure | attachment preview rendering | mitigate | Preserve the existing protected-attachment blob-fetch and media-viewer deep-link path; do not replace it with public image URLs while normalizing preview rendering. |
| T-45-07 | Repudiation | timestamp presentation | mitigate | Derive both absolute and relative display values from the existing `created_at` field so each card communicates one authoritative creation moment. |
| T-45-08 | Denial of service | detail-page mobile layout | mitigate | Promote the timeline by layout/copy changes only; do not add extra tabs or a heavyweight parallel activity surface that increases mobile navigation cost. |
</threat_model>

<verification>
- `npm --prefix frontend run test -- SiteDetailPage.test.tsx`
- `npm --prefix frontend run test -- ActivityFeed.test.tsx`
- `npm --prefix frontend run build`
</verification>

<success_criteria>
- The detail page visibly treats the timeline as the canonical project memory.
- Mixed attachment entries, legacy photos, and status changes all show consistent timestamp presentation.
- Material history and media-viewer access remain available from the same project context surface.
</success_criteria>

<output>
After completion, create `.planning/phases/45-unified-project-timeline/45-02-SUMMARY.md`
</output>
