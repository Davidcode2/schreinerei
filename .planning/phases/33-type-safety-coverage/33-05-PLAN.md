---
phase: 33-type-safety-coverage
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/inventory/MaterialHistoryFeed.tsx
  - frontend/src/pages/inventory/InventoryDetailPage.test.tsx
  - frontend/tests/inventory.spec.ts
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
gap_closure: true
must_haves:
  truths:
    - "Withdrawal history entries render with the required red badge treatment from HIST-01."
    - "Automated tests prove history badges render the expected color classes along with attribution and Baustelle links."
    - "Browser coverage keeps checking the enriched history feed against the same rendered contract users rely on."
  artifacts:
    - path: frontend/src/pages/inventory/MaterialHistoryFeed.tsx
      provides: Correct withdrawal badge styling for enriched history entries
    - path: frontend/src/pages/inventory/InventoryDetailPage.test.tsx
      provides: Component assertions for badge classes, attribution, and site links
    - path: frontend/tests/inventory.spec.ts
      provides: Browser regression coverage for enriched history badge rendering including color assertions
  key_links:
    - from: frontend/src/pages/inventory/MaterialHistoryFeed.tsx
      to: frontend/src/pages/inventory/InventoryDetailPage.test.tsx
      via: rendered badge class assertions
      pattern: bg-red
    - from: frontend/tests/inventory.spec.ts
      to: frontend/src/pages/inventory/MaterialHistoryFeed.tsx
      via: browser-visible history badge verification
      pattern: Entnommen
---

<objective>
Close the remaining history-rendering gap by restoring the required withdrawal badge color and pinning that visual contract in automated tests.

Purpose: make HIST-01 true in the shipped UI and ensure future regressions are caught in both component and browser coverage instead of slipping through label-only assertions.
Output: red withdrawal badge styling plus unit and Playwright assertions for enriched history colors, attribution, and site links.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-type-safety-coverage/33-VERIFICATION.md
@.planning/phases/33-type-safety-coverage/33-03-SUMMARY.md
@frontend/src/pages/inventory/MaterialHistoryFeed.tsx
@frontend/src/pages/inventory/InventoryDetailPage.test.tsx
@frontend/tests/inventory.spec.ts

<interfaces>
History-feed rendering must keep these observable contracts together:

- `material_added` entries render green badges.
- Withdrawal entries render red badges per `HIST-01`.
- History rows render `von {user_name}` attribution when available.
- Withdrawal rows render a `/sites/{id}` link when site data exists.

This gap closes only when color assertions exist in automated tests; label/link checks alone are insufficient.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: restore HIST-01 withdrawal badge styling and pin it in component tests</name>
  <files>frontend/src/pages/inventory/MaterialHistoryFeed.tsx, frontend/src/pages/inventory/InventoryDetailPage.test.tsx</files>
  <behavior>
    - Test 1: withdrawal history entries render the required red badge classes instead of amber.
    - Test 2: component tests still verify attribution text and Baustelle links while asserting the badge classes.
  </behavior>
  <read_first>
frontend/src/pages/inventory/MaterialHistoryFeed.tsx
frontend/src/pages/inventory/InventoryDetailPage.test.tsx
.planning/REQUIREMENTS.md
.planning/phases/33-type-safety-coverage/33-VERIFICATION.md
  </read_first>
  <action>Update `frontend/src/pages/inventory/MaterialHistoryFeed.tsx` so withdrawal entries use a red badge treatment that satisfies `HIST-01` exactly, then extend `frontend/src/pages/inventory/InventoryDetailPage.test.tsx` to assert the rendered class names for withdrawal badges alongside the existing enriched-history label, attribution, and `/sites/{id}` link expectations. Preserve the current German badge copy and link behavior; this fix corrects styling and adds explicit test coverage.</action>
  <verify>
    <automated>npm run test:run -- src/pages/inventory/InventoryDetailPage.test.tsx</automated>
  </verify>
  <done>Withdrawal badges render red in the component, and the detail-page unit tests fail if the history badge color contract regresses again.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: add browser-level history badge color assertions to the inventory suite</name>
  <files>frontend/tests/inventory.spec.ts</files>
  <behavior>
    - Test 1: the enriched-history Playwright scenario asserts the withdrawal badge's red styling in addition to label, attribution, and link behavior.
    - Test 2: the browser suite keeps verifying rendered history against authenticated API-backed setup rather than toast-only UI checks.
  </behavior>
  <read_first>
frontend/tests/inventory.spec.ts
.planning/phases/33-type-safety-coverage/33-03-SUMMARY.md
.planning/phases/33-type-safety-coverage/33-VERIFICATION.md
  </read_first>
  <action>Extend the enriched-history scenario in `frontend/tests/inventory.spec.ts` with explicit assertions for the rendered withdrawal badge classes after the seeded history entry loads, keeping the existing API-backed setup, attribution assertion, and `/sites/{id}` link check from Plan 33-03 intact. Do not replace API-backed verification with toast or text-only checks; the browser test must prove the user-visible color contract and the linked history data in one scenario.</action>
  <verify>
    <automated>npm run test:e2e -- inventory.spec.ts</automated>
  </verify>
  <done>The browser suite fails if withdrawal history styling drifts away from the required red treatment, while still verifying attribution and site-link rendering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API-provided enriched history -> rendered badge UI | Untrusted backend entry types and metadata drive visible badge state |
| component rendering -> Playwright assertions | Browser tests must reflect the same classes users see, not synthetic assumptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-13 | Tampering | `frontend/src/pages/inventory/MaterialHistoryFeed.tsx` | mitigate | Encode the withdrawal badge with the required red class set from `HIST-01` and cover it with component and browser assertions so a styling regression is caught immediately. |
| T-33-14 | Repudiation | `frontend/tests/inventory.spec.ts` | mitigate | Keep API-backed history setup and add browser-visible color checks so evidence ties the rendered badge contract to real enriched-history data. |
| T-33-15 | Information disclosure | history attribution/link rendering | accept | The tests assert only already-authorized data shown in the protected inventory detail view and do not widen access beyond existing authenticated behavior. |
</threat_model>

<verification>
- From `frontend/`: `npm run test:run -- src/pages/inventory/InventoryDetailPage.test.tsx`
- From `frontend/`: `npm run test:e2e -- inventory.spec.ts`
- Inspect `frontend/src/pages/inventory/MaterialHistoryFeed.tsx` to confirm withdrawal badges use red, not amber, classes.
</verification>

<success_criteria>
- `HIST-01` is satisfied by red withdrawal badges in the inventory history feed.
- Component tests assert badge styling, attribution, and site-link rendering together.
- Playwright asserts the browser-visible history badge color in addition to attribution and link behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/33-type-safety-coverage/33-05-SUMMARY.md`
</output>
