---
phase: 33-type-safety-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/inventory.ts
  - frontend/src/types/generated.ts
  - frontend/src/lib/api/hooks/useInventory.ts
  - frontend/src/lib/api/hooks/useInventory.test.tsx
  - frontend/src/pages/inventory/InventoryDetailPage.tsx
  - frontend/src/pages/inventory/MaterialHistoryFeed.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Inventory request/response DTOs come from ts-rs generated bindings instead of being manually duplicated in the frontend."
    - "Running the same regeneration path as CI leaves `frontend/src/types/generated.ts` in sync with git."
    - "Inventory UI code still has ergonomic local imports after the generated-type switch."
  artifacts:
    - path: frontend/src/types/inventory.ts
      provides: Thin inventory type facade that reuses generated DTOs
    - path: frontend/src/types/generated.ts
      provides: Regenerated source-of-truth DTO bindings from Rust
    - path: frontend/src/lib/api/hooks/useInventory.ts
      provides: Inventory API hooks typed against generated DTO contracts
  key_links:
    - from: frontend/src/types/inventory.ts
      to: frontend/src/types/generated.ts
      via: type import/re-export
      pattern: generated inventory DTOs
    - from: .github/workflows/ci.yml
      to: frontend/src/types/generated.ts
      via: git diff drift check
      pattern: Check generated TypeScript types
---

<objective>
Make the inventory frontend consume the same DTO contract the Rust backend exports so Phase 30-32 no longer rely on duplicated handwritten request/response shapes.

Purpose: eliminate drift risk between Rust DTOs and inventory frontend code before adding more coverage on top.
Output: regenerated bindings, generated-type-backed inventory type imports, and regression tests proving the hooks still compile and behave correctly.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-type-safety-coverage/33-RESEARCH.md
@.github/workflows/ci.yml
@frontend/src/types/generated.ts
@frontend/src/types/inventory.ts
@frontend/src/lib/api/hooks/useInventory.ts
@frontend/src/lib/api/hooks/usePreferences.ts
@.planning/phases/31-settings-editing-stock-in/31-03-SUMMARY.md
@.planning/phases/32-enriched-history/32-02-SUMMARY.md

<interfaces>
Generated inventory DTOs already exported from Rust include:

```ts
export type UpdateCategoryRequest = { name?: string, description?: string }
export type UpdateMaterialRequest = { location?: string, min_quantity?: number, clear_location?: boolean }
export type AdjustStockRequest = { quantity: number, reason: string }
export type StockInRequest = { quantity: number, notes: string | null }
export type EntryType = "withdrawn" | "adjusted" | "material_added" | "location_changed" | "min_quantity_changed"
export type EnrichedStockHistoryResponse = { ... }
```

Current handwritten duplicates live in `frontend/src/types/inventory.ts` and should not remain the source of truth for API DTOs after this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: move inventory DTO imports to generated bindings</name>
  <files>frontend/src/types/inventory.ts, frontend/src/lib/api/hooks/useInventory.ts, inventory consumers that import handwritten DTOs</files>
  <read_first>
frontend/src/types/inventory.ts
frontend/src/types/generated.ts
frontend/src/lib/api/hooks/useInventory.ts
frontend/src/lib/api/hooks/usePreferences.ts
  </read_first>
  <action>Refactor the inventory frontend type surface so the shared API DTOs come from `@/types/generated`. Keep `frontend/src/types/inventory.ts` only as a thin inventory-specific facade for local aliases that add value, such as mapping `CategoryResponse` to `Category`, `MaterialResponse` to `Material`, or re-exporting generated DTO names for stable local imports. Remove handwritten redefinitions of request/response DTOs that already exist in `generated.ts`, then update `useInventory.ts` and any touched inventory components/tests to consume the generated-backed exports without changing runtime behavior.</action>
  <acceptance_criteria>
    - `frontend/src/types/inventory.ts` no longer manually redefines `UpdateCategoryRequest`, `UpdateMaterialRequest`, `AdjustStockRequest`, `StockInRequest`, or `EntryType`.
    - `frontend/src/lib/api/hooks/useInventory.ts` type-checks against generated inventory DTOs.
    - Inventory page/test imports continue to resolve through either `@/types/generated` directly or the thin `@/types/inventory` facade.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- src/lib/api/hooks/useInventory.test.tsx src/pages/inventory/InventoryDetailPage.test.tsx</automated>
  </verify>
  <done>The inventory frontend now consumes backend-generated DTO types instead of handwritten duplicates.</done>
</task>

<task type="auto">
  <name>task 2: regenerate bindings and lock the drift check to the CI path</name>
  <files>frontend/src/types/generated.ts</files>
  <read_first>
Cargo.toml
.github/workflows/ci.yml
frontend/src/types/generated.ts
src/modules/inventory/api/routes.rs
  </read_first>
  <action>Run the repo's current ts-rs regeneration path used by CI, commit the resulting `frontend/src/types/generated.ts` changes if any, and confirm the working tree stays clean for that file after a fresh regeneration pass. Do not reintroduce the outdated feature-flag command from older docs; use the same `cargo test --all` plus `git diff --exit-code frontend/src/types/generated.ts` style verification CI already enforces.</action>
  <acceptance_criteria>
    - `frontend/src/types/generated.ts` contains the current inventory DTO exports used by Phase 30-32.
    - A fresh regeneration pass does not leave additional drift in `frontend/src/types/generated.ts`.
    - Plan notes and verification use the CI-aligned regeneration command path.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --all</automated>
    <automated>git diff --exit-code frontend/src/types/generated.ts</automated>
  </verify>
  <done>The generated inventory bindings are current and the repo's official drift check passes locally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust DTO exports -> frontend type layer | Generated backend contracts are transformed into frontend compile-time types |
| frontend type facade -> inventory UI/hooks | API contract types flow into user-driven mutations and history rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | `frontend/src/types/inventory.ts` | mitigate | Remove handwritten duplicate DTO definitions so frontend payload shapes cannot silently diverge from Rust exports. |
| T-33-02 | Denial of service | generated-type regeneration workflow | mitigate | Verify using the exact CI regeneration path so local success matches CI behavior and avoids repeated broken reruns. |
| T-33-03 | Repudiation | inventory mutation hooks | accept | This plan changes compile-time contracts only; runtime audit/event behavior remains owned by the backend endpoints shipped in Phase 30. |
</threat_model>

<verification>
- From repo root: `cargo test --all`
- From repo root: `git diff --exit-code frontend/src/types/generated.ts`
- From `frontend/`: `npm run test:run -- src/lib/api/hooks/useInventory.test.tsx src/pages/inventory/InventoryDetailPage.test.tsx`
</verification>

<success_criteria>
- Inventory API DTOs are sourced from `generated.ts` instead of duplicated by hand.
- Regenerating bindings leaves `frontend/src/types/generated.ts` in sync with git.
- Existing inventory hook and enriched-history tests still pass with the generated-backed type surface.
</success_criteria>

<output>
After completion, create `.planning/phases/33-type-safety-coverage/33-01-SUMMARY.md`
</output>
