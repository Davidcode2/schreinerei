---
phase: 33-type-safety-coverage
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - src/modules/inventory/api/routes.rs
  - src/modules/inventory/infrastructure/material_repository.rs
  - src/modules/inventory/domain/category.rs
  - src/modules/inventory/domain/material.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Inventory backend tests cover the new category and material DTO semantics introduced in Phase 30-31."
    - "Category delete conflict behavior remains pinned for history-preserving FK enforcement."
    - "No real-Postgres integration suite is introduced in this phase."
  artifacts:
    - path: src/modules/inventory/api/routes.rs
      provides: Route/DTO conversion coverage for category, material update, and stock-in endpoints
    - path: src/modules/inventory/infrastructure/material_repository.rs
      provides: Delete-category conflict guard coverage
  key_links:
    - from: src/modules/inventory/api/routes.rs
      to: src/modules/inventory/domain/category.rs
      via: request -> domain conversion
      pattern: UpdateCategoryRequest / UpdateMaterialRequest / StockInRequest
    - from: src/modules/inventory/infrastructure/material_repository.rs
      to: Phase 31 category delete UI behavior
      via: conflict contract
      pattern: Cannot delete category: materials still reference it
---

<objective>
Add focused backend validation coverage for the Phase 30-31 inventory API contract without expanding scope into real database integration tests.

Purpose: prove the backend still enforces the category/edit/stock-in rules the frontend now depends on.
Output: colocated tests for DTO semantics, command validation edges, and category delete conflict preservation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-type-safety-coverage/33-RESEARCH.md
@src/modules/inventory/api/routes.rs
@src/modules/inventory/infrastructure/material_repository.rs
@src/modules/inventory/domain/category.rs
@src/modules/inventory/domain/material.rs
@.planning/phases/30-backend-api-foundation/30-01-SUMMARY.md
@.planning/phases/30-backend-api-foundation/30-02-SUMMARY.md
@.planning/phases/31-settings-editing-stock-in/31-02-SUMMARY.md

<interfaces>
Phase 30 introduced these DTO semantics that must stay pinned:

```rust
pub struct UpdateCategoryRequest {
    pub name: Option<String>,
    pub description: Option<String>,
}

pub struct UpdateMaterialRequest {
    pub location: Option<String>,
    pub min_quantity: Option<i32>,
    pub clear_location: Option<bool>,
}

pub struct StockInRequest {
    pub quantity: i32,
    pub notes: Option<String>,
}
```

Deletion semantics from the repository/service contract must continue to preserve history by returning a conflict while any material rows still reference the category.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: add inventory route and DTO validation tests</name>
  <files>src/modules/inventory/api/routes.rs</files>
  <read_first>
src/modules/inventory/api/routes.rs
src/modules/inventory/domain/category.rs
src/modules/inventory/domain/material.rs
  </read_first>
  <action>Add a `#[cfg(test)]` block to `src/modules/inventory/api/routes.rs` that directly exercises the Phase 30 inventory DTO contract and conversions. Cover at least these cases: `UpdateCategoryRequest` preserves PATCH semantics for `None` vs `Some("")`, `UpdateMaterialRequest` preserves `clear_location` behavior without fabricating unrelated fields, `StockInRequest` converts notes unchanged and rejects invalid quantity through existing domain validation, and `EnrichedStockHistoryResponse::from(...)` continues to expose the `entry_type`, `user_name`, `site_id`, `site_name`, and `category_name` fields the Phase 32 UI relies on.</action>
  <acceptance_criteria>
    - `src/modules/inventory/api/routes.rs` contains focused tests for `UpdateCategoryRequest`, `UpdateMaterialRequest`, and `StockInRequest` semantics.
    - The tests explicitly protect optional/PATCH behavior and stock-in validation edges.
    - The enriched history response contract used by the frontend is asserted in at least one conversion test.
  </acceptance_criteria>
  <verify>
    <automated>cargo test inventory::api::routes</automated>
  </verify>
  <done>The inventory API layer now has direct regression coverage for the contract shipped to the frontend.</done>
</task>

<task type="auto">
  <name>task 2: strengthen category CRUD guard coverage without adding integration tests</name>
  <files>src/modules/inventory/infrastructure/material_repository.rs, optionally src/modules/inventory/domain/category.rs or src/modules/inventory/domain/material.rs</files>
  <read_first>
src/modules/inventory/infrastructure/material_repository.rs
src/modules/inventory/domain/category.rs
src/modules/inventory/domain/material.rs
  </read_first>
  <action>Extend the existing lightweight backend tests so the Phase 31 category-management guarantees stay pinned. Keep the existing no-soft-delete delete guard assertion, then add any missing direct validation cases needed to fully cover the create/update/delete surface for categories and the edit/stock-in edge cases that back the frontend dialogs. Stay colocated and unit-level; do not create a real-Postgres suite or new integration harness in this phase.</action>
  <acceptance_criteria>
    - The repository test coverage still asserts that category deletion does not filter out soft-deleted materials when checking conflicts.
    - Category create/update validation edge cases are fully covered in colocated unit tests.
    - Phase 31/32 frontend flows depend only on backend rules that now have direct automated validation coverage.
  </acceptance_criteria>
  <verify>
    <automated>cargo test inventory</automated>
  </verify>
  <done>The backend validation surface for category management, editing, and stock-in is covered without leaving Phase 33's intended scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON request payloads -> inventory route DTOs | Untrusted request bodies become domain commands |
| repository delete guard -> category management UI | Backend conflict enforcement determines whether destructive UI actions are allowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04 | Tampering | `src/modules/inventory/api/routes.rs` | mitigate | Add tests that pin PATCH optional semantics so empty/absent values are not silently reinterpreted during conversion. |
| T-33-05 | Elevation of privilege | category delete path | accept | Authz remains outside this phase; Phase 33 validates only the existing conflict and validation rules already behind authenticated routes. |
| T-33-06 | Information disclosure | enriched history DTO conversion | mitigate | Assert only the intended frontend-visible fields are exposed by the response conversion the UI consumes. |
</threat_model>

<verification>
- From repo root: `cargo test inventory::api::routes`
- From repo root: `cargo test inventory`
</verification>

<success_criteria>
- Inventory route tests cover category patch semantics, material edit semantics, stock-in validation, and enriched-history response fields.
- Category delete conflict behavior remains pinned for history preservation.
- Backend validation coverage increases without adding the deferred real-Postgres integration suite.
</success_criteria>

<output>
After completion, create `.planning/phases/33-type-safety-coverage/33-02-SUMMARY.md`
</output>
