---
phase: 33-type-safety-coverage
plan: 03
type: execute
wave: 3
depends_on:
  - 33-01
  - 33-02
files_modified:
  - frontend/tests/inventory.spec.ts
  - frontend/tests/helpers/api.ts
  - frontend/tests/helpers/data.ts
autonomous: true
requirements:
  - CATS-01
  - CATS-02
  - CATS-03
  - EDIT-01
  - EDIT-02
  - EDIT-03
  - STOCK-01
  - STOCK-02
  - HIST-01
  - HIST-02
  - HIST-03
must_haves:
  truths:
    - "Playwright covers the shipped inventory settings, material edit, stock-in, and enriched-history flows end to end."
    - "E2E tests verify persistence or rendered API effects through authenticated API calls, not only UI toasts."
    - "History assertions cover badge text, attribution, and Baustelle linking for the Phase 32 feed."
  artifacts:
    - path: frontend/tests/inventory.spec.ts
      provides: API-verified E2E coverage for Phase 31-32 inventory flows
    - path: frontend/tests/helpers/api.ts
      provides: Inventory helper coverage for update, stock-in, and history assertions needed by E2E tests
  key_links:
    - from: frontend/tests/inventory.spec.ts
      to: /settings/inventory
      via: Playwright navigation and page interaction
      pattern: Inventar-Einstellungen
    - from: frontend/tests/inventory.spec.ts
      to: /api/v1/inventory/materials/{id}/history/enriched
      via: helper/API verification
      pattern: enriched history
---

<objective>
Prove the Phase 31-32 inventory work behaves correctly in the browser with API-backed verification for the exact user flows that shipped.

Purpose: catch regressions across routing, dialogs, persistence, and history rendering that unit tests alone cannot see.
Output: focused Playwright coverage for settings management, material editing, stock-in, and enriched history.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-type-safety-coverage/33-RESEARCH.md
@frontend/tests/README.md
@frontend/tests/inventory.spec.ts
@frontend/tests/helpers/api.ts
@frontend/tests/helpers/data.ts
@.planning/phases/31-settings-editing-stock-in/31-01-SUMMARY.md
@.planning/phases/31-settings-editing-stock-in/31-02-SUMMARY.md
@.planning/phases/31-settings-editing-stock-in/31-03-SUMMARY.md
@.planning/phases/32-enriched-history/32-02-SUMMARY.md

<interfaces>
The E2E suite should verify these observable contracts:

- `/settings/inventory` exists and allows category management behavior from Phase 31.
- Material edit flow updates `location` and `min_quantity`.
- Stock-in flow posts positive quantity plus optional notes and creates a `material_added` history entry.
- Enriched history renders German badge labels, `von {user_name}` attribution, and a `/sites/{id}` link for withdrawals with site data.

Use the existing authenticated `page.request` helpers for persistence checks and add missing helper functions rather than writing raw API calls inline in every test.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: extend inventory E2E helpers for phase 31-32 verification</name>
  <files>frontend/tests/helpers/api.ts, frontend/tests/helpers/data.ts</files>
  <read_first>
frontend/tests/helpers/api.ts
frontend/tests/helpers/data.ts
frontend/tests/README.md
  </read_first>
  <action>Add the minimal authenticated helper functions needed to verify the new inventory flows through the API, such as category update/delete helpers if missing, material patch helpers, stock-in helpers, and enriched-history fetch helpers. Keep the existing cleanup pattern intact and extend tracking only if the new tests create additional resources like dedicated sites for withdrawal-history assertions.</action>
  <acceptance_criteria>
    - Inventory E2E tests can verify edit, stock-in, and history effects via helpers instead of ad-hoc inline `page.request` code.
    - Cleanup support remains deterministic for any new categories, materials, or sites created by the new tests.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- inventory.spec.ts</automated>
  </verify>
  <done>The E2E harness exposes the API-level checks needed for the inventory quality gate.</done>
</task>

<task type="auto">
  <name>task 2: add focused Playwright coverage for settings, editing, stock-in, and enriched history</name>
  <files>frontend/tests/inventory.spec.ts</files>
  <read_first>
frontend/tests/inventory.spec.ts
frontend/tests/helpers/api.ts
.planning/phases/31-settings-editing-stock-in/31-02-SUMMARY.md
.planning/phases/31-settings-editing-stock-in/31-03-SUMMARY.md
.planning/phases/32-enriched-history/32-02-SUMMARY.md
  </read_first>
  <action>Expand `frontend/tests/inventory.spec.ts` with small, isolated scenarios instead of one giant workflow. Cover at least: navigating to `/settings/inventory` from the inventory page and editing a category name; attempting to delete a referenced category and asserting the conflict feedback path; editing a material's `location` and `min_quantity` through the UI and verifying the persisted material via API; recording stock-in through the dialog and verifying the material quantity plus a `material_added` enriched-history entry via API; and verifying the detail-page history feed renders the expected badge label, attribution, and Baustelle link for seeded enriched-history data. Reuse `uniqueName(...)`, `track.*(...)`, and API-backed assertions for every data-changing scenario.</action>
  <acceptance_criteria>
    - `frontend/tests/inventory.spec.ts` contains distinct E2E coverage for settings/category management, material edit, stock-in, and enriched history.
    - Data-changing tests verify persisted state or history entries through API helpers after UI interaction.
    - History assertions include the rendered German badge text, `von ...` attribution, and a `/sites/{id}` link when site data exists.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- inventory.spec.ts</automated>
  </verify>
  <done>The shipped v1.9 inventory flows now have end-to-end browser coverage with API-backed verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright browser actions -> authenticated app routes | Tests exercise protected inventory UI flows as a real user |
| UI effects -> API verification helpers | Browser-visible changes are verified against backend state and history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-07 | Repudiation | inventory E2E assertions | mitigate | Verify persisted outcomes through API helpers so success is tied to backend state, not only transient UI feedback. |
| T-33-08 | Denial of service | shared test environment | mitigate | Use unique test data plus cleanup tracking to avoid cross-test collisions and residual-state failures. |
| T-33-09 | Information disclosure | history-feed assertions | accept | Tests operate under authenticated user context and assert only data already surfaced in the protected UI. |
</threat_model>

<verification>
- From `frontend/`: `npm run test:e2e -- inventory.spec.ts`
- Inspect `frontend/tests/inventory.spec.ts` to confirm each scenario uses API-backed persistence/history assertions after UI actions.
</verification>

<success_criteria>
- Inventory settings/category management has browser-level regression coverage.
- Material edit and stock-in dialogs are covered end to end with API verification.
- Enriched history rendering is covered for badge text, attribution, and Baustelle linking.
</success_criteria>

<output>
After completion, create `.planning/phases/33-type-safety-coverage/33-03-SUMMARY.md`
</output>
