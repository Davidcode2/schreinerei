---
phase: 33-type-safety-coverage
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/inventory.ts
  - frontend/src/lib/api/hooks/useInventory.ts
  - frontend/src/lib/api/hooks/useInventory.test.tsx
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - "Inventory order request/response DTOs come from ts-rs generated bindings instead of handwritten frontend duplicates."
    - "Inventory order hooks consume generated DTOs end to end without a handwritten order facade in the API layer."
    - "The CI-aligned ts-rs regeneration path still leaves frontend/src/types/generated.ts drift-free after the type cleanup."
  artifacts:
    - path: frontend/src/types/inventory.ts
      provides: Thin inventory facade that re-exports generated order DTOs instead of redefining them
    - path: frontend/src/lib/api/hooks/useInventory.ts
      provides: Inventory order hooks typed directly against generated request and response DTOs
    - path: frontend/src/lib/api/hooks/useInventory.test.tsx
      provides: Focused regression coverage for generated-backed inventory hook typing and order hook behavior
  key_links:
    - from: frontend/src/types/inventory.ts
      to: frontend/src/types/generated.ts
      via: type re-export layer
      pattern: OrderRequest
    - from: frontend/src/lib/api/hooks/useInventory.ts
      to: frontend/src/types/generated.ts
      via: hook request/response typing
      pattern: useOrderRequests
---

<objective>
Close the remaining Phase 33 type-safety gap by removing the last handwritten order DTO duplication from the inventory frontend.

Purpose: make `generated.ts` the single DTO source of truth for inventory order flows so future Rust/frontend contract drift is caught by the existing CI gate instead of hiding behind local aliases.
Output: generated-backed order DTO re-exports, generated-typed order hooks, and focused hook regression coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-type-safety-coverage/33-RESEARCH.md
@.planning/phases/33-type-safety-coverage/33-VERIFICATION.md
@.planning/phases/33-type-safety-coverage/33-01-SUMMARY.md
@frontend/src/types/inventory.ts
@frontend/src/lib/api/hooks/useInventory.ts
@frontend/src/lib/api/hooks/useInventory.test.tsx

<interfaces>
The verification gap names the remaining generated DTOs that must flow through the inventory facade and hooks without handwritten copies:

- `OrderRequest`
- `CreateOrderRequestDto`
- `ApproveOrderRequestDto`
- `FulfillOrderRequestDto`
- `OrderStatusQuery`

These types already exist in `frontend/src/types/generated.ts`; this plan removes the duplicate definitions and uses the generated exports at the API boundary.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: replace the remaining handwritten inventory order DTO aliases with generated re-exports</name>
  <files>frontend/src/types/inventory.ts, frontend/src/lib/api/hooks/useInventory.ts</files>
  <behavior>
    - Test 1: inventory order hooks compile and return generated-backed `OrderRequest` data without a handwritten facade type.
    - Test 2: create, approve, fulfill, and query request payloads use generated DTO/request types instead of local duplicate declarations.
  </behavior>
  <read_first>
frontend/src/types/inventory.ts
frontend/src/lib/api/hooks/useInventory.ts
.planning/phases/33-type-safety-coverage/33-VERIFICATION.md
  </read_first>
  <action>Replace the manual `OrderRequest`, `CreateOrderRequestDto`, `ApproveOrderRequestDto`, `FulfillOrderRequestDto`, and `OrderStatusQuery` declarations in `frontend/src/types/inventory.ts` with re-exports from `frontend/src/types/generated.ts`, then update `useOrderRequests`, `useCreateOrderRequest`, and any sibling order helpers in `frontend/src/lib/api/hooks/useInventory.ts` to consume those generated types end to end. Preserve the existing endpoint paths, query keys, and runtime payload/response shapes; this fix is about type sourcing, not behavior changes.</action>
  <verify>
    <automated>npm run test:run -- src/lib/api/hooks/useInventory.test.tsx</automated>
  </verify>
  <done>`frontend/src/types/inventory.ts` no longer hand-defines the order DTOs called out in the verification gap, and the inventory order hooks compile against generated request/response contracts only.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: add a regression test for generated-backed order hooks and rerun the CI drift gate</name>
  <files>frontend/src/lib/api/hooks/useInventory.test.tsx</files>
  <behavior>
    - Test 1: the order-query/mutation hook tests exercise the generated-backed contract names now exported from `@/types/inventory`.
    - Test 2: the ts-rs regeneration path remains clean after the order-type cleanup.
  </behavior>
  <read_first>
frontend/src/lib/api/hooks/useInventory.test.tsx
.planning/phases/33-type-safety-coverage/33-01-SUMMARY.md
.planning/phases/33-type-safety-coverage/33-VERIFICATION.md
  </read_first>
  <action>Extend `frontend/src/lib/api/hooks/useInventory.test.tsx` so the order-related hooks covered by this gap are exercised through the generated-backed type facade, then rerun the same drift check Phase 33 already standardized on: `cargo test --all` followed by `git diff --exit-code frontend/src/types/generated.ts`. Do not reintroduce the deprecated `--features ts-rs/export` path; keep verification aligned with current CI per the research notes.</action>
  <verify>
    <automated>npm run test:run -- src/lib/api/hooks/useInventory.test.tsx && cargo test --all && git diff --exit-code frontend/src/types/generated.ts</automated>
  </verify>
  <done>The order-hook test suite protects the generated-backed type path, and the CI-equivalent regeneration check finishes with no diff in `frontend/src/types/generated.ts`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust DTO exports -> frontend type facade | Backend-generated contracts cross into TypeScript and must stay authoritative |
| frontend hook typing -> API callers | UI code trusts hook request/response shapes to match backend contracts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-10 | Tampering | `frontend/src/types/inventory.ts` | mitigate | Remove handwritten order DTO definitions and re-export the generated bindings directly so the facade cannot silently diverge from Rust exports. |
| T-33-11 | Repudiation | `frontend/src/lib/api/hooks/useInventory.ts` | mitigate | Add focused hook regression coverage plus the CI-aligned `cargo test --all && git diff --exit-code frontend/src/types/generated.ts` gate so future contract drift is observable in tests. |
| T-33-12 | Denial of service | frontend inventory order flows | accept | This change is type-sourcing only and preserves existing endpoints and runtime payload shapes, so no new availability risk is introduced beyond existing hook behavior. |
</threat_model>

<verification>
- From `frontend/`: `npm run test:run -- src/lib/api/hooks/useInventory.test.tsx`
- From repo root: `cargo test --all && git diff --exit-code frontend/src/types/generated.ts`
- Inspect `frontend/src/types/inventory.ts` to confirm the verification-gap order DTO names are re-exported, not hand-declared.
</verification>

<success_criteria>
- No handwritten order DTO definitions remain in `frontend/src/types/inventory.ts` for the names listed in the verification gap.
- `useInventory.ts` order hooks use generated request/response contracts end to end.
- The CI-equivalent ts-rs regeneration check passes with zero generated-type drift.
</success_criteria>

<output>
After completion, create `.planning/phases/33-type-safety-coverage/33-04-SUMMARY.md`
</output>
