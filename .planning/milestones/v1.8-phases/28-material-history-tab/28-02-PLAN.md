---
phase: 28-material-history-tab
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - frontend/src/types/inventory.ts
  - frontend/src/lib/api/hooks/useInventory.ts
  - frontend/src/lib/api/hooks/index.ts
  - frontend/src/types/generated.ts
autonomous: true
requirements:
  - HIST-01
  - HIST-03
  - HIST-04
must_haves:
  truths:
    - "Frontend can fetch site-scoped material history for a Baustelle"
    - "Client-side types include category and extracting user fields"
    - "Data hook uses authenticated API path bound to one site"
  artifacts:
    - path: "frontend/src/lib/api/hooks/useInventory.ts"
      provides: "site history query hook"
    - path: "frontend/src/types/inventory.ts"
      provides: "material history row type with category/user fields"
  key_links:
    - from: "frontend/src/lib/api/hooks/useInventory.ts"
      to: "/api/v1/inventory/sites/{site_id}/history"
      via: "useQuery hook"
      pattern: "useSiteMaterialHistory"
---

<objective>
Wire frontend data contracts and hooks for the new site material history endpoint.

Purpose: Ensure the Material tab can consume enriched backend data safely and consistently.
Output: Updated TS contracts + query hook exports + generated type sync.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-material-history-tab/28-RESEARCH.md
@.planning/phases/28-material-history-tab/28-01-PLAN.md

<interfaces>
From `frontend/src/lib/api/hooks/useInventory.ts`:
```ts
export function useMaterialHistory(id: string) {
  return useQuery({
    queryKey: ["material-history", id],
    queryFn: () => apiClient.get<MaterialStockHistoryEntry[]>(`/api/v1/inventory/materials/${id}/history`),
  })
}
```

From `frontend/src/types/inventory.ts`:
```ts
export interface MaterialStockHistoryEntry {
  id: string
  quantity_change: number
  site_id: string | null
  site_name: string | null
  created_at: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add site material history types and hook contracts</name>
  <files>frontend/src/types/inventory.ts, frontend/src/lib/api/hooks/useInventory.ts, frontend/src/lib/api/hooks/index.ts</files>
  <read_first>frontend/src/types/inventory.ts, frontend/src/lib/api/hooks/useInventory.ts, frontend/src/lib/api/hooks/index.ts</read_first>
  <behavior>
    - Test 1: Hook queries `/api/v1/inventory/sites/{siteId}/history`.
    - Test 2: Entry type exposes `material_name`, `category_name`, `extracted_by`, `site_name`.
  </behavior>
  <action>Define a dedicated frontend interface for site-scoped material history rows with exact keys: `id`, `material_id`, `material_name`, `category_name`, `quantity_change`, `quantity_after`, `notes`, `site_id`, `site_name`, `extracted_by`, `created_at`. Add `useSiteMaterialHistory(siteId: string, limit?: number)` in `useInventory.ts` using React Query and export it through hooks index for SiteDetail page consumption.</action>
  <acceptance_criteria>
    - `useInventory.ts` contains `useSiteMaterialHistory` and API path `/api/v1/inventory/sites/${siteId}/history`.
    - `types/inventory.ts` contains a site-history interface with category and user fields.
    - `hooks/index.ts` re-exports `useSiteMaterialHistory`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- InventoryDetailPage.test.tsx</automated>
  </verify>
  <done>Frontend can fetch and typecheck enriched site material history rows.</done>
</task>

<task type="auto">
  <name>Task 2: Sync generated shared DTO types (ts-rs)</name>
  <files>frontend/src/types/generated.ts</files>
  <read_first>frontend/src/types/generated.ts, src/modules/inventory/api/routes.rs</read_first>
  <action>Run `cargo export-types` after backend DTO changes from Plan 28-01 and commit resulting `frontend/src/types/generated.ts` updates. Keep generated output aligned with Rust DTO names and field casing.</action>
  <acceptance_criteria>
    - `frontend/src/types/generated.ts` includes the new site-history DTO export.
    - No manual edits added outside generated section format.
  </acceptance_criteria>
  <verify>
    <automated>cargo export-types</automated>
  </verify>
  <done>Backend/Frontend DTO contracts are synchronized and drift-resistant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend hook→backend API | Hook requests untrusted remote payload; must be strongly typed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05 | T | frontend data mapping | mitigate | Use strict TypeScript interface to avoid unsafe `any` field access. |
| T-28-06 | I | frontend cache keys | mitigate | Scope query key by `siteId` to prevent cross-site cache bleed. |
</threat_model>

<verification>
- `cargo export-types`
- `npm run test -- InventoryDetailPage.test.tsx`
</verification>

<success_criteria>
- Site material history hook and exported types exist.
- Generated shared type file reflects backend DTO changes.
</success_criteria>

<output>
After completion, create `.planning/phases/28-material-history-tab/28-material-history-tab-02-SUMMARY.md`
</output>
