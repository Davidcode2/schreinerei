---
phase: 28-material-history-tab
plan: 03
type: execute
wave: 3
depends_on:
  - 28-01
  - 28-02
files_modified:
  - frontend/src/pages/sites/ActivityFeed.tsx
  - frontend/src/pages/sites/SiteDetailPage.tsx
  - frontend/src/pages/sites/ActivityFeed.test.tsx
autonomous: true
requirements:
  - HIST-01
  - HIST-02
must_haves:
  truths:
    - "Materials tab shows stock entries linked to the Baustelle"
    - "User can navigate via Baustelle name link"
  artifacts:
    - path: "frontend/src/pages/sites/ActivityFeed.tsx"
      provides: "Material tab rendering real history entries"
    - path: "frontend/src/pages/sites/ActivityFeed.test.tsx"
      provides: "UI regression tests for material tab"
  key_links:
    - from: "frontend/src/pages/sites/SiteDetailPage.tsx"
      to: "frontend/src/pages/sites/ActivityFeed.tsx"
      via: "siteId prop for material history hook"
      pattern: "siteId="
---

<objective>
Replace Material-tab placeholder with navigable, contextual material history UI.

Purpose: Deliver user-visible value for Phase 28 by showing real site-linked extraction history in the Baustelle activity feed.
Output: Material tab list cards with material/category/user/timestamp and site navigation link.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-material-history-tab/28-02-PLAN.md
@frontend/src/pages/inventory/InventoryDetailPage.tsx

<interfaces>
From `frontend/src/pages/sites/ActivityFeed.tsx`:
```tsx
interface ActivityFeedProps {
  activities: Activity[]
  maxItems?: number
}
```

From `frontend/src/pages/sites/SiteDetailPage.tsx`:
```tsx
<ActivityFeed activities={activities || []} />
```

From `frontend/src/pages/inventory/InventoryDetailPage.tsx` (display pattern):
```tsx
{entry.site_name && (
  <div className="mt-2 flex items-center gap-2 text-sm">
    <MapPin className="h-4 w-4 text-muted-foreground" />
    <span>{entry.site_name}</span>
  </div>
)}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire site-scoped material history into ActivityFeed</name>
  <files>frontend/src/pages/sites/ActivityFeed.tsx, frontend/src/pages/sites/SiteDetailPage.tsx</files>
  <read_first>frontend/src/pages/sites/ActivityFeed.tsx, frontend/src/pages/sites/SiteDetailPage.tsx, frontend/src/lib/api/hooks/useInventory.ts</read_first>
  <behavior>
    - Test 1: Material tab loads and displays entries for the current site.
    - Test 2: Each row shows material name + category + extracted_by + quantity + timestamp.
  </behavior>
  <action>Extend `ActivityFeedProps` with required `siteId: string`. In `SiteDetailPage.tsx`, pass `site.id` to `ActivityFeed`. In `ActivityFeed.tsx`, call `useSiteMaterialHistory(siteId)` and replace placeholder content in `TabsContent value="materials"` with a list of cards. Render `material_name`, `category_name`, `extracted_by`, `quantity_change` (withdrawal-friendly label), and formatted `created_at`.</action>
  <acceptance_criteria>
    - `ActivityFeedProps` contains `siteId` and component call site passes it.
    - Material tab no longer contains "Material-Historie folgt in Kürze" placeholder text.
    - Material cards render material, category, user, and timestamp fields.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- ActivityFeed.test.tsx</automated>
  </verify>
  <done>Material tab shows real site-linked history data instead of placeholder copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Baustelle navigation affordance and empty/loading states</name>
  <files>frontend/src/pages/sites/ActivityFeed.tsx, frontend/src/pages/sites/ActivityFeed.test.tsx</files>
  <read_first>frontend/src/pages/sites/ActivityFeed.tsx, frontend/src/pages/inventory/InventoryDetailPage.test.tsx</read_first>
  <behavior>
    - Test 1: Site name is rendered as clickable link to `/sites/{site_id}` when present.
    - Test 2: Empty and loading states are shown for material history tab.
  </behavior>
  <action>Render `site_name` as a clickable React Router `Link` to `/sites/${entry.site_id}` to satisfy HIST-02 (self-link still explicit and testable). Add material-tab loading state while hook is pending and empty-state copy when no entries exist. Create/extend `ActivityFeed.test.tsx` with MSW-backed assertions for rendered entries and link target.</action>
  <acceptance_criteria>
    - Material tab includes at least one link with href matching `/sites/` pattern when `site_id` exists.
    - Empty state copy is visible when API returns `[]`.
    - Test file contains assertions for link navigation target and enriched row fields.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- ActivityFeed.test.tsx InventoryDetailPage.test.tsx</automated>
  </verify>
  <done>User can inspect material history details and navigate via Baustelle link in the Material tab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response→UI rendering | Untrusted text enters React rendering path |
| UI link generation→router | Dynamic IDs used to construct navigation paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-07 | X (tampering-like UI injection) | ActivityFeed material row | mitigate | Render text fields as plain text nodes; never use `dangerouslySetInnerHTML`. |
| T-28-08 | S | site link generation | mitigate | Build links from trusted API UUID fields and keep route constrained to `/sites/:id`. |
| T-28-09 | D | material tab | accept | Keep default query limit bounded to avoid rendering unbounded lists in one frame. |
</threat_model>

<verification>
- `npm run test -- ActivityFeed.test.tsx`
- `npm run test -- InventoryDetailPage.test.tsx`
</verification>

<success_criteria>
- Material tab shows real site-linked history entries.
- Entry cards include category and extracting user context.
- Baustelle name link is clickable and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/28-material-history-tab/28-material-history-tab-03-SUMMARY.md`
</output>
