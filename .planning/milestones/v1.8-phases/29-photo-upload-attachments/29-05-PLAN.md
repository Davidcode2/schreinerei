---
phase: 29-photo-upload-attachments
plan: 05
type: execute
wave: 5
depends_on: [29-04]
files_modified:
  - frontend/src/lib/api/hooks/useSites.ts
  - frontend/src/lib/offline/queue.ts
  - frontend/src/lib/api/hooks/useSites.test.tsx
  - frontend/src/lib/offline/queue.test.ts
  - frontend/src/pages/sites/CreateNoteModal.tsx
  - frontend/src/pages/sites/SiteDetailPage.tsx
  - frontend/src/pages/sites/CreateNoteModal.test.tsx
autonomous: true
gap_closure: true
requirements:
  - FILE-01
  - FILE-02
  - FILE-05
  - FILE-06
must_haves:
  truths:
    - "User upload from Add note → Foto no longer fails with 400 multipart field error"
    - "Photo icon button on Site detail opens functional photo upload flow"
    - "Offline queued photo replay uses the same multipart contract and succeeds on reconnect"
  artifacts:
    - path: "frontend/src/lib/api/hooks/useSites.ts"
      provides: "Online upload FormData with backend-compatible multipart field name"
    - path: "frontend/src/lib/offline/queue.ts"
      provides: "Offline replay FormData with matching multipart field name"
    - path: "frontend/src/pages/sites/SiteDetailPage.tsx"
      provides: "Working photo icon action wired to note/photo modal"
  key_links:
    - from: "CreateNoteModal.tsx"
      to: "useSites.ts"
      via: "useUploadSitePhoto mutateAsync"
      pattern: "attachments/photo"
    - from: "queue.ts"
      to: "/api/v1/sites/{siteId}/attachments/photo"
      via: "photo_upload handler replay"
      pattern: "formData\.append\(\"photo\""
    - from: "SiteDetailPage.tsx"
      to: "CreateNoteModal.tsx"
      via: "photo icon click opens modal in photo mode"
      pattern: "setShowNoteModal\(true\)"
---

<objective>
Close Phase 29 verification gaps by fixing multipart contract mismatch and wiring the currently non-functional photo icon button.

Purpose: Restore end-to-end photo upload success for both online and offline paths and make the direct photo entrypoint usable.
Output: Updated upload/replay contract + UI wiring + regression tests covering upload field name and photo-button behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-photo-upload-attachments/29-VERIFICATION.md
@frontend/src/lib/api/hooks/useSites.ts
@frontend/src/lib/offline/queue.ts
@frontend/src/pages/sites/CreateNoteModal.tsx
@frontend/src/pages/sites/SiteDetailPage.tsx
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Fix multipart contract mismatch for online and offline photo uploads</name>
  <files>frontend/src/lib/api/hooks/useSites.ts, frontend/src/lib/offline/queue.ts, frontend/src/lib/api/hooks/useSites.test.tsx, frontend/src/lib/offline/queue.test.ts</files>
  <read_first>
    - frontend/src/lib/api/hooks/useSites.ts
    - frontend/src/lib/offline/queue.ts
    - src/modules/sites/api/routes.rs
    - .planning/phases/29-photo-upload-attachments/29-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: Online upload mutation sends multipart field name `photo` and backend accepts it.
    - Test 2: Offline queue replay sends multipart field name `photo` and reaches activity creation step.
  </behavior>
  <action>Update both upload callsites to append the file as `formData.append("photo", file)` (not `file`) so frontend and offline replay match backend route validation in `upload_site_photo_attachment` (`field.name() == Some("photo")`). Update existing tests to assert the `photo` field key explicitly and keep the request target `/api/v1/sites/{siteId}/attachments/photo` unchanged.</action>
  <acceptance_criteria>
    - `frontend/src/lib/api/hooks/useSites.ts` contains `formData.append("photo", file)` and does not contain `formData.append("file", file)` in upload mutation.
    - `frontend/src/lib/offline/queue.ts` contains `formData.append('photo', file)` and does not contain `formData.append('file', file)`.
    - `frontend/src/lib/api/hooks/useSites.test.tsx` asserts multipart payload includes key `photo`.
    - `frontend/src/lib/offline/queue.test.ts` validates replay path still creates photo activity after upload response.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- useSites offline/queue</automated>
  </verify>
  <done>Manual-testing payload no longer triggers `{"error":"Multipart field 'photo' is required"}` for online or replay uploads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire SiteDetail photo icon to functional photo modal path</name>
  <files>frontend/src/pages/sites/SiteDetailPage.tsx, frontend/src/pages/sites/CreateNoteModal.tsx, frontend/src/pages/sites/CreateNoteModal.test.tsx</files>
  <read_first>
    - frontend/src/pages/sites/SiteDetailPage.tsx
    - frontend/src/pages/sites/CreateNoteModal.tsx
    - frontend/src/pages/sites/CreateNoteModal.test.tsx
  </read_first>
  <behavior>
    - Test 1: Clicking the camera icon in site activities opens the modal.
    - Test 2: Camera-icon flow opens directly in photo mode (file input visible immediately).
  </behavior>
  <action>Add `onClick={() => setShowNoteModal(true)}` behavior for the activities camera button and extend modal opening state so photo entry can be selected immediately (e.g., pass an `initialActivityType` prop from `SiteDetailPage` to `CreateNoteModal` and reset it correctly on close). Keep existing note flow unchanged and ensure upload submit still uses the same `handleSubmit` logic.</action>
  <acceptance_criteria>
    - `frontend/src/pages/sites/SiteDetailPage.tsx` camera button includes an onClick handler that opens the modal.
    - `frontend/src/pages/sites/CreateNoteModal.tsx` supports opening in photo mode without manual tab toggle.
    - `frontend/src/pages/sites/CreateNoteModal.test.tsx` includes a test for camera-entrypoint photo mode behavior.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- CreateNoteModal</automated>
  </verify>
  <done>Photo icon is no longer a dead control; it opens a working upload entry path aligned with FILE-01/FILE-02 expectations.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/offline queue → upload API | Untrusted multipart payload crosses client→server boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-11 | T | multipart upload request | mitigate | Enforce single canonical multipart field (`photo`) across online/offline clients to prevent malformed contract bypass and silent failures. |
| T-29-12 | D | offline replay queue | mitigate | Preserve existing retry cap and payload validation so malformed replay data fails safely without infinite retries. |
| T-29-13 | R | UI photo entrypoint | accept | Photo-button wiring itself does not alter auth boundaries; backend tenant checks remain the enforcement point. |
</threat_model>

<verification>
- `npm run test:run -- useSites offline/queue CreateNoteModal`
</verification>

<success_criteria>
- Online Add note → Foto upload path succeeds without backend multipart-field validation error.
- Offline queued photo replay uses same multipart field contract and processes on reconnect.
- Site detail camera button opens a functional photo-first modal path.
</success_criteria>

<output>
After completion, create `.planning/phases/29-photo-upload-attachments/29-photo-upload-attachments-05-SUMMARY.md`
</output>
