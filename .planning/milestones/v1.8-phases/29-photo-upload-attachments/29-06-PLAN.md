---
phase: 29-photo-upload-attachments
plan: 06
type: execute
wave: 6
depends_on: [29-05]
files_modified:
  - src/modules/sites/application/site_service.rs
  - src/modules/sites/api/routes.rs
  - frontend/src/lib/api/client.ts
  - frontend/src/pages/sites/ActivityFeed.tsx
  - frontend/src/pages/sites/ActivityFeed.test.tsx
autonomous: true
gap_closure: true
requirements:
  - FILE-01
  - FILE-02
  - FILE-07
must_haves:
  truths:
    - "Submitting one photo from the upload modal creates exactly one new activity entry"
    - "Activity feed photo preview loads successfully for authenticated users without browser 401 errors"
    - "Attachment bytes remain tenant-authorized and are not exposed cross-tenant"
  artifacts:
    - path: "src/modules/sites/application/site_service.rs"
      provides: "Upload endpoint stores attachment and returns URLs without creating a second photo activity"
    - path: "frontend/src/lib/api/client.ts"
      provides: "Authenticated binary GET helper for protected attachment image endpoints"
    - path: "frontend/src/pages/sites/ActivityFeed.tsx"
      provides: "Photo preview rendering path that fetches protected image bytes with Authorization"
  key_links:
    - from: "CreateNoteModal.tsx"
      to: "site_service.rs"
      via: "upload -> createActivity flow"
      pattern: "one upload + one create_activity mutation => one stored activity"
    - from: "ActivityFeed.tsx"
      to: "api/client.ts"
      via: "authenticated attachment fetch"
      pattern: "Authorization: Bearer"
    - from: "routes.rs"
      to: "site_repository.rs"
      via: "tenant-scoped attachment lookup"
      pattern: "find_attachment_by_id\(attachment_uuid, ctx.tenant_id\)"
---

<objective>
Close Phase 29 runtime gaps found during human testing: duplicate activity creation on photo upload and browser 401 for protected attachment image URLs.

Purpose: Restore correct photo-activity behavior and authenticated preview rendering while preserving tenant-scoped authorization.
Output: Backend upload behavior fix + frontend authenticated image-loading path + regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-photo-upload-attachments/29-VERIFICATION.md
@.planning/phases/29-photo-upload-attachments/29-HUMAN-UAT.md
@src/modules/sites/application/site_service.rs
@src/modules/sites/api/routes.rs
@frontend/src/lib/api/client.ts
@frontend/src/pages/sites/CreateNoteModal.tsx
@frontend/src/pages/sites/ActivityFeed.tsx

<issues_to_close>
- Human runtime blocker A: Upload modal POST succeeds but one upload creates TWO activity entries; expected exactly one.
- Human runtime blocker B: Attachment GET /api/v1/attachments/{id} returns 401 Missing Authorization header when loaded by browser image tag.
</issues_to_close>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Remove duplicate activity creation from attachment upload path</name>
  <files>src/modules/sites/application/site_service.rs, src/modules/sites/api/routes.rs</files>
  <behavior>
    - Test 1: Calling POST /api/v1/sites/{id}/attachments/photo does not create a standalone activity row.
    - Test 2: Submitting the photo modal (upload + create activity) results in exactly one new photo activity.
  </behavior>
  <action>Refactor upload flow so `upload_photo_attachment` persists attachment bytes and returns URLs but does not create a `site_activities` record internally (remove the extra `create_activity` call currently creating a hidden second row). Keep tenant/site validation and MIME/size checks unchanged. Keep API response contract unchanged (`attachment_id`, `photo_url`, `thumbnail_url`) so CreateNoteModal continues to call `createActivity` once and becomes the single activity creation path. Add/update regression coverage in existing backend test surface for upload behavior to assert the no-implicit-activity invariant per this gap.</action>
  <verify>
    <automated>cargo test upload_photo -- --nocapture</automated>
  </verify>
  <done>A single modal photo submit produces exactly one activity entry (not two) while upload endpoint still returns valid attachment URLs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render protected activity images through authenticated fetch path</name>
  <files>frontend/src/lib/api/client.ts, frontend/src/pages/sites/ActivityFeed.tsx, frontend/src/pages/sites/ActivityFeed.test.tsx</files>
  <behavior>
    - Test 1: Attachment image requests include Authorization header via API client.
    - Test 2: Activity photo previews render from fetched blob/object URL and no longer rely on unauthenticated direct `<img src=/api/v1/attachments/...>` browser requests.
  </behavior>
  <action>Add a binary-fetch helper in `api/client.ts` (e.g., `getBlob`) that reuses token injection and fetch error handling. In `ActivityFeed.tsx`, replace direct protected URL `<img src={activity.photo_url}>` usage with an authenticated loader component/hook that fetches blob bytes for attachment URLs and renders object URLs, including cleanup via `URL.revokeObjectURL`. Keep non-attachment absolute URLs unchanged if present. Update tests to verify authenticated fetch invocation and image rendering behavior per D-runtime blocker B.</action>
  <verify>
    <automated>npm run test:run -- ActivityFeed</automated>
  </verify>
  <done>Photo previews load in authenticated sessions without 401 Missing Authorization header errors from `/api/v1/attachments/{id}`.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser UI → protected attachment endpoint | Untrusted browser runtime requests binary content; auth token must be attached |
| upload endpoint → activity persistence | Server-side flow can accidentally create duplicate business events if not explicitly bounded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-14 | T | `upload_photo_attachment` service flow | mitigate | Remove implicit `create_activity` side effect so upload endpoint cannot duplicate business events; keep explicit activity creation in one path only. |
| T-29-15 | I | Activity photo preview rendering | mitigate | Fetch protected attachment bytes with API client token injection instead of direct browser URL request without auth header. |
| T-29-16 | E | Attachment retrieval authorization | mitigate | Preserve tenant-scoped lookup (`attachment_id + tenant_id`) and authenticated route guard for GET bytes endpoints. |
</threat_model>

<verification>
- `cargo test upload_photo -- --nocapture`
- `npm run test:run -- ActivityFeed`
</verification>

<success_criteria>
- One photo upload from modal creates exactly one activity entry.
- Activity image preview no longer triggers 401 Missing Authorization header in normal authenticated browser usage.
- Tenant-scoped attachment authorization remains enforced on GET endpoints.
</success_criteria>

<output>
After completion, create `.planning/phases/29-photo-upload-attachments/29-photo-upload-attachments-06-SUMMARY.md`
</output>
