---
phase: 29-photo-upload-attachments
plan: 04
type: execute
wave: 4
depends_on: [29-03]
files_modified:
  - frontend/src/lib/offline/db.ts
  - frontend/src/lib/offline/queue.ts
  - frontend/src/lib/offline/sync.ts
  - frontend/src/pages/sites/CreateNoteModal.tsx
  - frontend/src/components/offline/PendingActionsBadge.tsx
autonomous: true
requirements:
  - FILE-05
  - FILE-06
must_haves:
  truths:
    - "Offline photo capture is queued locally"
    - "Queued photo uploads sync automatically when back online"
  artifacts:
    - path: "frontend/src/lib/offline/db.ts"
      provides: "Dexie schema support for queued photo payload metadata"
    - path: "frontend/src/lib/offline/queue.ts"
      provides: "photo-upload queue handler"
    - path: "frontend/src/lib/offline/sync.ts"
      provides: "online-triggered queue processing"
  key_links:
    - from: "CreateNoteModal.tsx"
      to: "offline/queue.ts"
      via: "enqueue when navigator is offline"
      pattern: "queueAction"
    - from: "offline/sync.ts"
      to: "offline/queue.ts"
      via: "processAction on online event"
      pattern: "syncPendingActions"
---

<objective>
Add offline queueing and deferred sync for photo uploads.

Purpose: Ensure activity photo capture works in poor connectivity environments and syncs later automatically.
Output: Extended offline DB schema, queue handlers for photo payloads, and online sync integration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-photo-upload-attachments/29-03-PLAN.md
@frontend/src/lib/offline/db.ts
@frontend/src/lib/offline/queue.ts
@frontend/src/lib/offline/sync.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend offline schema and queue payloads for photo uploads</name>
  <files>frontend/src/lib/offline/db.ts, frontend/src/lib/offline/queue.ts</files>
  <behavior>
    - Test 1: Offline photo action stores binary payload + metadata in Dexie.
    - Test 2: Queue item can be deserialized and processed after reload.
  </behavior>
  <action>Extend `PendingAction` typing and Dexie schema version to support photo upload queue items (siteId, content/caption, mime type, binary/blob reference). Add dedicated handler path in `queue.ts` that uploads file and then creates activity, preserving existing retry semantics.</action>
  <verify>
    <automated>npm run test:run -- offline/queue</automated>
  </verify>
  <done>Photo uploads can be safely queued with all data required for later sync.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Trigger automatic sync and UI feedback when connection returns</name>
  <files>frontend/src/lib/offline/sync.ts, frontend/src/pages/sites/CreateNoteModal.tsx, frontend/src/components/offline/PendingActionsBadge.tsx</files>
  <behavior>
    - Test 1: Creating photo activity while offline enqueues instead of failing.
    - Test 2: On `online` event, queued photo uploads are processed and pending badge count decreases.
  </behavior>
  <action>Wire photo creation UI to branch on online/offline state: enqueue action offline with user feedback toast; online path keeps immediate upload flow. Ensure `initSync` online listener processes photo queue and updates pending indicator components.
  </action>
  <verify>
    <automated>npm run test:run -- offline sync PendingActionsBadge</automated>
  </verify>
  <done>Offline photo capture is resilient and auto-syncs when network returns.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| offline storage→sync processor | Locally persisted payload could be stale/corrupt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-09 | D | offline queue | mitigate | Cap retries and surface permanent failures without infinite loop. |
| T-29-10 | T | queued payload processing | mitigate | Validate queued payload shape before upload attempt; reject malformed entries. |
</threat_model>

<verification>
- `npm run test:run -- offline`
</verification>

<success_criteria>
- Offline photo actions are queued locally.
- Queue processes uploads on reconnect automatically.
- Pending action UI reflects sync progress.
</success_criteria>

<output>
After completion, create `.planning/phases/29-photo-upload-attachments/29-photo-upload-attachments-04-SUMMARY.md`
</output>
