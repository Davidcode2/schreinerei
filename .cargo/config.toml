# ts-rs type generation configuration
[env]
# Keep ts-rs auto-export tests away from the committed generated file.
TS_RS_EXPORT_DIR = { value = "target/ts-rs", relative = true }

[alias]
# Write frontend/src/types/generated.ts via the deterministic exporter.
export-types = "run --bin export-types"
